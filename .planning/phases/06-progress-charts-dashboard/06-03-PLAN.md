---
phase: 06-progress-charts-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - app/(app)/(tabs)/dashboard.tsx
  - src/features/dashboard/components/TodaysWorkoutCard.tsx
  - src/features/dashboard/components/ProgressSummaryCard.tsx
  - src/features/dashboard/components/TappableAvatar.tsx
autonomous: false
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "Home screen shows Today's Workout card at the top with plan name, day label, exercise count, estimated duration, and Start Workout button"
    - "On rest days, Today's Workout card shows rest day message with next planned workout teaser and Quick Workout button"
    - "When no plan exists, Today's Workout card offers Create Plan and Quick Workout buttons"
    - "Start Workout button launches active workout directly"
    - "Progress summary card shows current streak, recent PRs, and this week's stats with sparkline trends"
    - "Dashboard card order is: Today's Workout, Progress Summary, Bodyweight, PR Baselines"
    - "Bodyweight card is integrated into the dashboard with sparkline and quick-add"
    - "Sign Out button is removed from dashboard (moved to Settings in Phase 11)"
  artifacts:
    - path: "app/(app)/(tabs)/dashboard.tsx"
      provides: "Refactored dashboard composing feature cards"
      contains: "TodaysWorkoutCard"
    - path: "src/features/dashboard/components/TodaysWorkoutCard.tsx"
      provides: "Today's planned workout card with all three states"
      exports: ["TodaysWorkoutCard"]
    - path: "src/features/dashboard/components/ProgressSummaryCard.tsx"
      provides: "Streak + PRs + weekly stats with sparklines"
      exports: ["ProgressSummaryCard"]
    - path: "src/features/dashboard/components/TappableAvatar.tsx"
      provides: "Extracted avatar component from dashboard"
      exports: ["TappableAvatar"]
  key_links:
    - from: "app/(app)/(tabs)/dashboard.tsx"
      to: "src/features/dashboard/components/TodaysWorkoutCard.tsx"
      via: "renders as first card"
      pattern: "TodaysWorkoutCard"
    - from: "src/features/dashboard/components/TodaysWorkoutCard.tsx"
      to: "src/features/dashboard/hooks/useTodaysWorkout.ts"
      via: "determines today's workout state"
      pattern: "useTodaysWorkout"
    - from: "src/features/dashboard/components/ProgressSummaryCard.tsx"
      to: "src/features/dashboard/hooks/useProgressSummary.ts"
      via: "fetches streak, PRs, stats"
      pattern: "useProgressSummary"
    - from: "src/features/dashboard/components/ProgressSummaryCard.tsx"
      to: "src/features/progress/components/Sparkline.tsx"
      via: "renders sparkline trends for key lifts"
      pattern: "Sparkline"
    - from: "src/features/dashboard/components/TodaysWorkoutCard.tsx"
      to: "app/(app)/workout"
      via: "Start Workout navigates to active workout route"
      pattern: "router.push.*workout"
---

<objective>
Refactor the dashboard into a data-driven home screen with Today's Workout card, Progress Summary card, and proper card ordering. Extract existing components, compose from feature-level cards, and wire navigation to active workout.

Purpose: Transforms the placeholder dashboard into the app's primary landing screen -- users immediately see what to do today and how they're progressing. This is the motivational feedback loop that makes the app worth opening daily.
Output: Refactored dashboard.tsx composing TodaysWorkoutCard, ProgressSummaryCard, BodyweightCard, and PRCard in the locked order.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-progress-charts-dashboard/06-CONTEXT.md
@.planning/phases/06-progress-charts-dashboard/06-RESEARCH.md
@.planning/phases/06-progress-charts-dashboard/06-01-SUMMARY.md
@.planning/phases/06-progress-charts-dashboard/06-02-SUMMARY.md

@app/(app)/(tabs)/dashboard.tsx
@src/constants/theme.ts
@src/components/ui/Card.tsx
@src/components/ui/Button.tsx

<interfaces>
<!-- From Plan 01 (hooks this plan consumes) -->

From src/features/dashboard/hooks/useTodaysWorkout.ts:
```typescript
export interface TodaysWorkoutState {
  state: 'planned' | 'rest-day' | 'no-plan';
  plan?: { id: string; name: string };
  todayDay?: { id: string; label: string; exerciseCount: number; estimatedDuration: number };
  nextDay?: { label: string; dayName: string };
}
export function useTodaysWorkout(): TodaysWorkoutState;
```

From src/features/dashboard/hooks/useProgressSummary.ts:
```typescript
export interface ProgressSummary {
  weeklyStreak: number;
  recentPRs: Array<{ exerciseName: string; weight: number; unit: string; date: string }>;
  weekWorkoutCount: number;
  weekTotalVolume: number;
}
export function useProgressSummary(): {
  summary: ProgressSummary;
  sparklines: Record<string, SparklineData[]>;
  isLoading: boolean;
  refresh: () => void;
}
```

<!-- From Plan 02 (components this plan uses) -->

From src/features/progress/components/Sparkline.tsx:
```typescript
export function Sparkline(props: { data: SparklineData[]; color: string; width: number; height: number }): JSX.Element;
```

From src/features/dashboard/components/BodyweightCard.tsx:
```typescript
export function BodyweightCard(): JSX.Element;
```

<!-- Existing dashboard.tsx exports to extract -->
From app/(app)/(tabs)/dashboard.tsx:
- TappableAvatar component: Extract to src/features/dashboard/components/TappableAvatar.tsx
- PRCard component: Keep inline or extract (used only in dashboard)
- Sign Out button: REMOVE (Phase 11 moves it to Settings tab)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TodaysWorkoutCard, ProgressSummaryCard, and extract TappableAvatar</name>
  <files>src/features/dashboard/components/TodaysWorkoutCard.tsx, src/features/dashboard/components/ProgressSummaryCard.tsx, src/features/dashboard/components/TappableAvatar.tsx</files>
  <action>
    Extract TappableAvatar from dashboard.tsx to src/features/dashboard/components/TappableAvatar.tsx:
    - Move the entire TappableAvatar function component (including pickPhoto and handlePress) as-is.
    - Export as named export. Keep identical props interface: { displayName, avatarUrl, onPhotoChanged }.
    - Keep the same styles (move relevant StyleSheet entries).

    Build TodaysWorkoutCard component:
    - Uses useTodaysWorkout() hook for state.
    - Card component wrapper with title "Today's Workout".
    - Three render states per user decisions:

    1. state === 'planned':
       - Show plan name (bold, colors.textPrimary) and day label (colors.textSecondary).
       - Stats row: exercise count (Ionicons "barbell-outline"), estimated duration (Ionicons "time-outline") in colors.textMuted.
       - "Start Workout" Button (variant="primary", full width). On press: router.push to active workout route. Pass plan day id as param: router.push({ pathname: '/(app)/workout', params: { planDayId: todayDay.id } }).
       - Tapping elsewhere on the card (Pressable wrapping Card, excluding the button): router.push to plan day detail for preview. Use plan.id and todayDay.id.

    2. state === 'rest-day':
       - Text "Rest Day" (colors.textPrimary, large).
       - Teaser: "Next: {nextDay.label} -- {nextDay.dayName}" in colors.textSecondary.
       - "Quick Workout" Button (variant="secondary"). On press: router.push('/(app)/workout') with no planDayId (freestyle).

    3. state === 'no-plan':
       - Text "No plan set up yet" (colors.textSecondary).
       - Two buttons side by side:
         - "Create a Plan" Button (variant="secondary"). On press: router.push('/(app)/plans/create') or the plan creation route.
         - "Quick Workout" Button (variant="secondary"). On press: router.push('/(app)/workout') freestyle.
       - New users can start immediately without a plan.

    Build ProgressSummaryCard component:
    - Uses useProgressSummary() hook. Calls refresh on useFocusEffect.
    - Card component wrapper with title "Progress".
    - Top section (streak + recent PRs):
      - Streak: Ionicons "flame-outline" icon + "{weeklyStreak} week streak" text. colors.warning for icon, colors.textPrimary for text. If streak is 0, show "Start your streak!" instead.
      - Recent PRs: List of up to 3 recent PRs. Each shows exercise name + weight + unit. Ionicons "trophy-outline" in colors.warning. If no PRs, show "No PRs yet -- keep lifting!" in colors.textMuted.
    - Bottom section (this week's stats):
      - Row of two stat boxes:
        - "Workouts": weekWorkoutCount with label "this week". Ionicons "calendar-outline".
        - "Volume": weekTotalVolume formatted (e.g., "12.4k kg") with label "this week". Ionicons "stats-chart-outline".
    - Sparkline trends:
      - Below stats, show small sparklines for key lifts (bench, squat, deadlift) if sparkline data exists.
      - Each: exercise name label (small text) + Sparkline component (width: 60, height: 24, color: colors.accent).
      - Row of sparklines, horizontally spaced. Only show lifts that have data.
      - If no sparkline data, omit this section entirely.

    All components use StyleSheet.create. Ionicons for all icons (no emojis). Dark theme colors.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>TodaysWorkoutCard renders all three states (planned/rest-day/no-plan) with correct CTAs and navigation. ProgressSummaryCard shows streak, recent PRs, weekly stats, and sparkline trends. TappableAvatar extracted without behavior changes.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor dashboard.tsx to compose feature cards in locked order</name>
  <files>app/(app)/(tabs)/dashboard.tsx</files>
  <action>
    Refactor dashboard.tsx to compose from extracted feature components:

    1. Remove the inline TappableAvatar component (now imported from @/features/dashboard/components/TappableAvatar).
    2. Remove the placeholder "Today's Workout", "Recent Activity", and "Progress" cards.
    3. Remove the Sign Out button entirely. Sign out will move to Settings tab in Phase 11. If there's no settings tab yet, that's fine -- the button is being proactively removed per roadmap intent.
    4. Keep the PRCard component inline (it's small and only used here).
    5. Keep the header section (avatar + greeting + display name) as-is, using imported TappableAvatar.
    6. Keep the useFocusEffect for PR baselines.

    Compose cards in the LOCKED order from user decisions:
    ```
    <TodaysWorkoutCard />     {/* 1st - action-oriented: what to do NOW */}
    <ProgressSummaryCard />   {/* 2nd - how you're doing */}
    <BodyweightCard />        {/* 3rd - daily tracking */}
    <PRCard baselines={baselines} />  {/* 4th - baseline reference */}
    ```

    Each card wrapped in the existing cardWrap style (marginBottom: 16). PRCard gets marginBottom: 32 as last item before scroll end.

    Import statements:
    ```typescript
    import { TappableAvatar } from '@/features/dashboard/components/TappableAvatar';
    import { TodaysWorkoutCard } from '@/features/dashboard/components/TodaysWorkoutCard';
    import { ProgressSummaryCard } from '@/features/dashboard/components/ProgressSummaryCard';
    import { BodyweightCard } from '@/features/dashboard/components/BodyweightCard';
    ```

    Keep the avatar photo upload logic in dashboard.tsx (handlePhotoChanged) since it uses Supabase storage directly and is tied to the auth user. TappableAvatar just receives the callback.

    Clean up unused styles from the StyleSheet after removing placeholder cards. Keep all header, avatar, and PR-related styles.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Dashboard renders 4 cards in locked order: Today's Workout, Progress Summary, Bodyweight, PR Baselines. Placeholder cards removed. Sign Out button removed. TappableAvatar imported from extracted module. All data-driven from hooks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete dashboard and navigation</name>
  <action>Human verification of the complete refactored dashboard with all feature cards and navigation.</action>
  <what-built>Refactored dashboard with Today's Workout card (3 states), Progress Summary card (streak + PRs + stats + sparklines), Bodyweight card (log + sparkline), and PR Baselines card in locked order. Sign Out removed.</what-built>
  <how-to-verify>
    1. Open the app -- dashboard should be the home screen
    2. Verify card order: Today's Workout at top, then Progress Summary, Bodyweight, PR Baselines
    3. Today's Workout card:
       a. If active plan with today's day: verify plan name, day label, exercise count, duration, "Start Workout" button
       b. Tap "Start Workout" -- should navigate to active workout (may need Phase 4 completion)
       c. If rest day: verify "Rest Day" message with next workout teaser and "Quick Workout" button
       d. If no plan: verify "Create a Plan" and "Quick Workout" buttons both appear
    4. Progress Summary card:
       a. Verify streak count displays (may be 0 if no workouts)
       b. Verify recent PRs list (if any exist from testing)
       c. Verify weekly workout count and volume stats
       d. Verify sparklines render for key lifts if data exists
    5. Bodyweight card:
       a. Verify latest weight shown or "--" placeholder
       b. Log a new weight entry using the inline input
       c. Verify card updates with new weight and sparkline (needs 2+ entries)
    6. PR Baselines card:
       a. Verify still works (tap to edit, shows Big 3 lifts)
    7. Verify Sign Out button is GONE from dashboard
    8. Profile avatar tap should still work for photo change
  </how-to-verify>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <done>User confirms dashboard shows all 4 cards in correct order with real data, navigation works for Start Workout and Quick Workout, and Sign Out has been removed.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no TypeScript errors
- `npx jest --bail` -- full test suite passes
- Dashboard renders 4 cards in locked order
- Today's Workout card shows correct state for current day
- Progress Summary shows streak, PRs, stats, sparklines
- Bodyweight card allows inline logging
- Sign Out button removed
- Navigation: Start Workout and Quick Workout buttons work
</verification>

<success_criteria>
- Dashboard card order is Today's Workout > Progress Summary > Bodyweight > PR Baselines (locked)
- TodaysWorkoutCard: planned state shows plan name + day label + exercise count + duration + Start Workout button
- TodaysWorkoutCard: rest day shows "Rest Day" + next workout teaser + Quick Workout
- TodaysWorkoutCard: no plan shows Create Plan + Quick Workout buttons
- Start Workout navigates to active workout; tapping card body goes to plan day detail
- ProgressSummaryCard: streak with flame icon, recent PRs with trophy, weekly stats, sparkline trends
- BodyweightCard: integrated with latest weight + sparkline + inline quick-add
- PRCard: unchanged, kept as separate card, tappable to edit
- Sign Out button removed from dashboard
- TappableAvatar extracted and working identically
- All components use StyleSheet.create, Ionicons, no emojis
</success_criteria>

<output>
After completion, create `.planning/phases/06-progress-charts-dashboard/06-03-SUMMARY.md`
</output>
