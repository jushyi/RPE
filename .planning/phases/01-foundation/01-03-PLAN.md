---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/(app)/onboarding/pr-baseline.tsx
  - app/(app)/(tabs)/dashboard.tsx
  - src/features/auth/components/PRBaselineForm.tsx
  - src/features/auth/hooks/usePRBaselines.ts
  - src/components/ui/Card.tsx
autonomous: false
requirements:
  - AUTH-06

must_haves:
  truths:
    - "After sign-up, user sees the PR baseline entry screen"
    - "User can enter weight values for bench press, squat, and deadlift"
    - "User can select a global unit (kg/lbs) and override per lift"
    - "User can skip the PR baseline step entirely"
    - "PR baseline values save to Supabase pr_baselines table"
    - "After completing or skipping PR baseline, user sees the dashboard"
    - "Dashboard shows placeholder sections for future features"
  artifacts:
    - path: "src/features/auth/components/PRBaselineForm.tsx"
      provides: "Big 3 lift weight entry form with global and per-lift unit selector"
      min_lines: 60
    - path: "src/features/auth/hooks/usePRBaselines.ts"
      provides: "Hook for saving PR baselines to Supabase"
      exports: ["usePRBaselines"]
    - path: "app/(app)/onboarding/pr-baseline.tsx"
      provides: "Onboarding screen that renders PRBaselineForm"
    - path: "app/(app)/(tabs)/dashboard.tsx"
      provides: "Empty state dashboard with placeholder sections"
      min_lines: 40
  key_links:
    - from: "src/features/auth/hooks/usePRBaselines.ts"
      to: "src/lib/supabase/client.ts"
      via: "supabase.from('pr_baselines').upsert()"
      pattern: "supabase.*from.*pr_baselines"
    - from: "app/(app)/onboarding/pr-baseline.tsx"
      to: "src/stores/authStore.ts"
      via: "setOnboardingComplete() after save or skip"
      pattern: "setOnboardingComplete"
---

<objective>
Build the PR baseline entry flow (Big 3 lifts with unit selection) shown after sign-up, and the empty state dashboard shell that serves as the home screen. After completing or skipping PR baselines, the user lands on a dashboard with placeholder sections for features built in later phases.

Purpose: PR baselines establish the user's starting point for PR detection (AUTH-06). The dashboard shell gives authenticated users a home to land on and sets expectations for what the app will become.
Output: Working onboarding flow and dashboard home screen.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from prior plans that this plan depends on -->

From src/lib/supabase/client.ts:
```typescript
export const supabase: SupabaseClient;
```

From src/stores/authStore.ts:
```typescript
export const useAuthStore: () => {
  hasCompletedOnboarding: boolean;
  preferredUnit: 'kg' | 'lbs';
  userId: string | null;
  setOnboardingComplete: () => void;
  setPreferredUnit: (unit: 'kg' | 'lbs') => void;
};
```

From src/types/database.ts:
```typescript
export interface PRBaseline {
  id: string;
  user_id: string;
  exercise_name: string; // 'bench_press' | 'squat' | 'deadlift'
  weight: number;
  unit: 'kg' | 'lbs';
  created_at: string;
  updated_at: string;
}
```

From src/components/ui/Button.tsx:
```typescript
export function Button(props: { title: string; onPress: () => void; variant?: 'primary' | 'secondary' | 'ghost'; loading?: boolean; disabled?: boolean }): JSX.Element;
```

From src/components/ui/Input.tsx:
```typescript
export function Input(props: { label?: string; error?: string; placeholder?: string; secureTextEntry?: boolean; value: string; onChangeText: (text: string) => void; keyboardType?: string }): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: PR baseline form, save hook, and onboarding screen</name>
  <files>
    src/features/auth/components/PRBaselineForm.tsx,
    src/features/auth/hooks/usePRBaselines.ts,
    app/(app)/onboarding/pr-baseline.tsx
  </files>
  <action>
    1. Create `usePRBaselines` hook (`src/features/auth/hooks/usePRBaselines.ts`):
       - `savePRBaselines(baselines: { exercise_name: string, weight: number, unit: 'kg' | 'lbs' }[])`:
         - Get userId from authStore
         - For each baseline with weight > 0, upsert to Supabase `pr_baselines` table (using the UNIQUE(user_id, exercise_name) constraint for conflict resolution)
         - Return success/error
       - `getPRBaselines()`: Fetch current user's baselines from Supabase
       - Handle offline gracefully: if offline, store baselines in MMKV and queue for sync (simple approach: save to MMKV immediately, attempt Supabase write, if fails the data is at least locally persisted)

    2. Create `PRBaselineForm` component (`src/features/auth/components/PRBaselineForm.tsx`):
       - LOCKED DECISIONS:
         - Big 3 only: bench press, squat, deadlift
         - Skippable step
         - Weight number field per lift
         - Global unit selector (kg/lbs) that defaults all fields, but each lift can override
       - Layout:
         - Title: "Set Your Starting PRs" with subtitle "Enter your current 1RM for each lift (optional)"
         - Global unit toggle: segmented control or pill buttons for "kg" / "lbs" (defaults to user's preferredUnit from authStore)
         - Three lift rows, each with:
           - Lift name (Bench Press / Squat / Deadlift) with appropriate icon or emoji
           - Numeric weight input (keyboardType="numeric")
           - Per-lift unit override: small toggle that defaults to global unit but can be independently set to the other
         - "Save & Continue" primary button (saves non-zero values)
         - "Skip" ghost button (navigates without saving)
       - On save: call usePRBaselines.savePRBaselines(), update authStore.setPreferredUnit() with the global unit, then call onComplete callback
       - On skip: call onComplete callback directly
       - Dark/bold styling: surface background cards for each lift, accent blue save button

    3. Wire onboarding screen (`app/(app)/onboarding/pr-baseline.tsx`):
       - Render PRBaselineForm
       - onComplete callback: call `authStore.setOnboardingComplete()`, then `router.replace('/(app)/(tabs)/dashboard')`
       - Full-screen dark background with safe area padding
       - Back button should NOT be available (this is a one-way onboarding step)
       - Set `headerShown: false` or configure Stack.Screen options to hide back
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - PRBaselineForm renders three lift inputs with global and per-lift unit toggles
    - Save writes non-zero values to Supabase pr_baselines table
    - Skip navigates to dashboard without writing
    - Onboarding screen marks completion in authStore
    - All files compile with zero TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Empty state dashboard shell with placeholder sections</name>
  <files>
    app/(app)/(tabs)/dashboard.tsx,
    src/components/ui/Card.tsx
  </files>
  <action>
    1. Create Card component (`src/components/ui/Card.tsx`):
       - Reusable dark-themed card with NativeWind: surface background, rounded corners, subtle border or elevation
       - Props: title (optional header), children, className (for overrides)
       - Used for dashboard placeholder sections

    2. Build empty state dashboard (`app/(app)/(tabs)/dashboard.tsx`):
       - LOCKED DECISION: Empty state dashboard has placeholder sections that future phases fill in
       - DISCRETION: Layout and placeholder design (make it feel like a real screen, not a blank page)
       - Layout (scrollable):
         - Header area: "Welcome, {displayName}" greeting (read from Supabase profile or auth user metadata). Show the user's avatar (from ProfilePhotoPicker) or default.
         - "Today's Workout" card: placeholder with icon and text "No plan set up yet -- coming soon!" (Phase 3-4 will fill this)
         - "Recent Activity" card: placeholder with "Start logging workouts to see your history here" (Phase 5)
         - "Progress" card: placeholder with chart icon and "Your progress charts will appear here" (Phase 6)
         - "Personal Records" card: If user entered PR baselines, show them (bench/squat/deadlift with weights). If skipped, show "Set your starting PRs to track progress" with a button that navigates back to PR baseline screen.
         - Sign-out button at the bottom (or in header) -- clear and accessible from this screen (AUTH-03: user can log out from any screen)
       - Dark/bold aesthetic: #0a0a0a background, surface cards, clean typography, accent colors on interactive elements
       - The dashboard should feel intentional and inviting, not empty or broken

    3. Ensure the sign-out action works from dashboard:
       - Import useAuth hook
       - Sign-out button calls signOut(), which triggers Stack.Protected to redirect to login
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Dashboard renders with welcome greeting using user's display name
    - Four placeholder cards visible for future features
    - PR baselines displayed if user entered them during onboarding
    - Sign-out button present and functional
    - Card component reusable for future phases
    - Dark/bold aesthetic applied throughout
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete auth flow end-to-end</name>
  <files>None -- verification only</files>
  <action>
    Human verifies the complete Phase 1 auth flow works end-to-end on a real device or simulator. This checkpoint covers ALL Phase 1 requirements (AUTH-01 through AUTH-06) across all 3 plans.

    What was built:
    - Complete Phase 1 authentication flow: sign-up with profile photo, PR baseline onboarding, dashboard with placeholders, sign-in persistence, sign-out, and offline resilience.

    Steps to verify:
    1. Start the dev server: `npx expo start` (use development build, not Expo Go -- MMKV requires native modules)
    2. Open on simulator/device
    3. Sign-up flow:
       - Verify single screen with sign-in/sign-up toggle
       - Switch to sign-up mode
       - Enter email, password, display name
       - Pick a profile photo (camera or gallery) -- verify preview shows
       - Tap "Create Account"
       - Verify navigation to PR baseline screen
    4. PR baseline flow:
       - Verify Big 3 lifts shown (bench, squat, deadlift)
       - Toggle global unit to kg -- verify all fields update
       - Override one lift to lbs -- verify it stays independent
       - Enter weights for at least one lift
       - Tap "Save & Continue"
       - Verify navigation to dashboard
    5. Dashboard:
       - Verify welcome greeting with your display name
       - Verify avatar shows (or default)
       - Verify PR baselines displayed with correct values/units
       - Verify placeholder cards for future features
       - Verify cloud icon in header shows connected (green check)
    6. Session persistence:
       - Close the app completely (swipe away)
       - Reopen -- verify you land on dashboard without re-logging in
    7. Sign-out:
       - Tap sign-out button
       - Verify return to login screen
       - Verify sign-in mode works with the same credentials
    8. Offline behavior:
       - Sign in while online
       - Enable airplane mode
       - Close and reopen app -- verify no crash, no forced logout
       - Verify cloud icon shows disconnected (red X)
       - Verify toast banner appears saying "You're offline"
       - Disable airplane mode -- verify "Back online" toast appears
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - Human has verified all 8 verification steps pass
    - User typed "approved" or issues have been addressed
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- Complete sign-up -> PR baseline -> dashboard flow works
- Session persists across app restart
- Sign-out returns to login from dashboard
- App opens offline without crash
- Connectivity indicators (cloud icon + toast banner) reflect real state
- PR baselines save to Supabase with correct user_id, exercise_name, weight, unit
</verification>

<success_criteria>
- AUTH-06 satisfied: User can enter 1RM for Big 3 lifts during setup, values saved as PR baselines
- Dashboard shell provides home screen for authenticated users
- Complete Phase 1 auth flow verified by human tester
- All 6 AUTH requirements demonstrably working
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
