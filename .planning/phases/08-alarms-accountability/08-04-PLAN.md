---
phase: 08-alarms-accountability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/features/plans/components/PlanDaySection.tsx]
autonomous: true
requirements: [ALRM-01]
gap_closure: true

must_haves:
  truths:
    - "Alarm time is visible in plan details view when alarm_enabled is true"
    - "No alarm row shown when alarm_enabled is false or alarm_time is null"
  artifacts:
    - path: "src/features/plans/components/PlanDaySection.tsx"
      provides: "Alarm time display in read-only plan view"
      contains: "alarm_enabled"
  key_links:
    - from: "src/features/plans/components/PlanDaySection.tsx"
      to: "PlanDay.alarm_enabled / PlanDay.alarm_time"
      via: "conditional render in day header area"
      pattern: "alarm_enabled.*alarm_time"
---

<objective>
Add alarm time display to the read-only plan details view (PlanDaySection).

Purpose: Users currently cannot see whether an alarm is set after saving a plan because PlanDaySection ignores the alarm_enabled and alarm_time fields. This closes the UAT gap from test 2.
Output: PlanDaySection renders an alarm icon + formatted time below the day header when alarm_enabled is true.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/alarm-time-not-visible.md

<interfaces>
<!-- PlanDay type from src/features/plans/types.ts -->
```typescript
// Relevant fields on PlanDay:
alarm_time: string | null;   // e.g. "06:30" or ISO time string
alarm_enabled: boolean;
```

<!-- PlanDaySection current structure (src/features/plans/components/PlanDaySection.tsx) -->
```typescript
interface PlanDaySectionProps {
  day: PlanDay;
  defaultExpanded?: boolean;
  onStartWorkout?: (day: PlanDay) => void;
}
// Component renders: header (day_name + weekday) > body (exercises list + Start Workout button)
// Already imports: Ionicons, colors from theme
// alarm_enabled and alarm_time are available on day prop but not rendered
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add alarm time display row to PlanDaySection</name>
  <files>src/features/plans/components/PlanDaySection.tsx</files>
  <action>
Add a conditional alarm display row in PlanDaySection between the header and the body content (inside the expanded section, before the exercises list).

When `day.alarm_enabled && day.alarm_time`:
1. Render a row with an Ionicons "alarm-outline" icon (size 16, color colors.accent) followed by the formatted alarm time text
2. Format the alarm_time string for display: if it contains ":", parse as HH:MM and format to 12-hour with AM/PM (e.g., "06:30" becomes "6:30 AM", "14:00" becomes "2:00 PM"). Use a simple inline formatter -- no external date library needed.
3. Style the row: flexDirection row, alignItems center, gap 6, marginBottom 10, paddingHorizontal 0 (inherits from body padding)
4. Text style: color colors.textSecondary, fontSize 13, fontWeight '500'

When `!day.alarm_enabled || !day.alarm_time`: render nothing (no empty space).

Add the new styles to the existing StyleSheet.create block:
- alarmRow: { flexDirection: 'row', alignItems: 'center', gap: 6, marginBottom: 10 }
- alarmText: { color: colors.textSecondary, fontSize: 13, fontWeight: '500' }

Do NOT change any existing functionality -- only add the alarm display.
  </action>
  <verify>
    <automated>cd "C:/Users/maser/Projects/Gym-App" && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>PlanDaySection shows alarm icon + formatted time (e.g., "6:30 AM") when alarm_enabled is true; shows nothing when alarm is off. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors
2. Open a plan with alarm_enabled days -- alarm time row visible below day header
3. Open a plan with no alarms -- no alarm row shown
</verification>

<success_criteria>
- PlanDaySection renders alarm icon + time when alarm_enabled is true and alarm_time is set
- No visual change when alarm is disabled
- No regressions to existing plan detail view functionality
</success_criteria>

<output>
After completion, create `.planning/phases/08-alarms-accountability/08-04-SUMMARY.md`
</output>
