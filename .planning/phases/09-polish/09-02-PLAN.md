---
phase: 09-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - assets/images/icon.png
  - assets/images/android-icon-foreground.png
  - assets/images/android-icon-background.png
  - assets/images/android-icon-monochrome.png
  - assets/images/splash-icon.png
  - assets/images/favicon.png
  - app/(app)/_layout.tsx
  - app/(app)/plans/_layout.tsx
  - app/(app)/history/_layout.tsx
  - app/(app)/progress/_layout.tsx
  - app/(app)/workout/_layout.tsx
  - app/(auth)/_layout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "App displays as 'RPE' (not 'Gym App') in app.json name field"
    - "App icon features a dumbbell/barbell on dark background with magenta accent"
    - "Splash screen shows icon centered on #0a0a0a dark background"
    - "Navigation transitions are consistent across all stack navigators"
  artifacts:
    - path: "app.json"
      provides: "App rename and icon/splash configuration"
      contains: "\"name\": \"RPE\""
    - path: "assets/images/icon.png"
      provides: "iOS app icon (1024x1024)"
    - path: "assets/images/splash-icon.png"
      provides: "Splash screen icon"
    - path: "assets/images/android-icon-foreground.png"
      provides: "Android adaptive icon foreground layer"
  key_links:
    - from: "app.json"
      to: "assets/images/"
      via: "icon and splash image path references"
      pattern: "assets/images/"
    - from: "all _layout.tsx files"
      to: "Stack screenOptions.animation"
      via: "animation prop"
      pattern: "animation:"
---

<objective>
Create the RPE app icon and splash screen, rename the app from "Gym App" to "RPE", and add consistent navigation transitions across all stack navigators.

Purpose: The app should look branded and intentional -- a proper name, a gym-relevant icon, smooth transitions. This is the visual identity pass.
Output: App icon PNGs (iOS + Android adaptive), splash icon, app rename in app.json, consistent stack animations.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-polish/09-CONTEXT.md
@.planning/phases/09-polish/09-RESEARCH.md

@app.json
@app/(app)/_layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate app icon PNGs and splash screen, rename app to RPE</name>
  <files>app.json, assets/images/icon.png, assets/images/android-icon-foreground.png, assets/images/android-icon-background.png, assets/images/android-icon-monochrome.png, assets/images/splash-icon.png, assets/images/favicon.png</files>
  <action>
1. Create a Node.js script (scripts/generate-icons.js) that programmatically generates the app icon:
   - Design: A stylized dumbbell/barbell icon -- clean, bold, simple geometry
   - Colors: Magenta (#ec4899) dumbbell on dark (#0a0a0a) background
   - Style: Minimal, geometric -- think bold lines and circles, not realistic
   - Use the `sharp` library (install as dev dependency: npm install -D sharp) to generate PNGs from SVG markup

2. Generate all required icon variants:
   - icon.png: 1024x1024, no transparency, no rounded corners (iOS masks automatically)
   - android-icon-foreground.png: 108x108dp (432x432px at xxxhdpi), dumbbell centered in 72dp safe zone, transparent background
   - android-icon-background.png: 108x108dp (432x432px), solid #0a0a0a
   - android-icon-monochrome.png: 108x108dp (432x432px), white dumbbell silhouette on transparent background
   - splash-icon.png: Dumbbell icon centered, sized for splash display (~200x200px)
   - favicon.png: 48x48 or 64x64 for web

3. Place all generated PNGs in assets/images/ (overwriting existing placeholder images).

4. Update app.json:
   - Change "name" from "Gym App" to "RPE"
   - Keep "slug" as "gym-app" (preserves EAS project identity)
   - Verify icon/splash paths are correct (they should already be)
   - Add iOS splash screen configuration in plugins if not present

5. Clean up: Delete the generation script after icons are created (it's a one-time tool), or keep it in scripts/ for future reference.

IMPORTANT: The icon design should be SIMPLE -- a geometric dumbbell with two circles connected by a bar is sufficient. Do not over-engineer. Bold, clean, recognizable at small sizes.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Desktop/Gym-App && node -e "const fs=require('fs'); const files=['assets/images/icon.png','assets/images/android-icon-foreground.png','assets/images/android-icon-background.png','assets/images/android-icon-monochrome.png','assets/images/splash-icon.png','assets/images/favicon.png']; const results=files.map(f=>({file:f,exists:fs.existsSync(f),size:fs.existsSync(f)?fs.statSync(f).size:0})); console.log(JSON.stringify(results,null,2)); const appJson=JSON.parse(fs.readFileSync('app.json','utf8')); console.log('App name:', appJson.expo.name);"</automated>
  </verify>
  <done>All 6 icon/splash PNGs exist with non-zero file sizes. app.json name is "RPE".</done>
</task>

<task type="auto">
  <name>Task 2: Add consistent navigation transitions across all stack layouts</name>
  <files>app/(app)/_layout.tsx, app/(app)/plans/_layout.tsx, app/(app)/history/_layout.tsx, app/(app)/progress/_layout.tsx, app/(app)/workout/_layout.tsx, app/(auth)/_layout.tsx</files>
  <action>
1. Set default animation on the main app layout (app/(app)/_layout.tsx):
   - Add `animation: 'slide_from_right'` to screenOptions (iOS-native push feel)
   - This becomes the default for all child stack navigators

2. For each sub-layout, add consistent animation if not already set:
   - plans/_layout.tsx: `animation: 'slide_from_right'` (push navigation)
   - history/_layout.tsx: `animation: 'slide_from_right'` (push navigation)
   - progress/_layout.tsx: `animation: 'slide_from_right'` (push navigation)
   - workout/_layout.tsx: KEEP existing `animation: 'slide_from_bottom'` (modal behavior is intentional for workout focus mode)
   - auth/_layout.tsx: `animation: 'fade'` (auth screens feel more polished with fade)

3. Ensure all layouts have consistent header styling:
   - headerStyle: { backgroundColor: colors.surface }
   - headerTintColor: colors.textPrimary
   - contentStyle: { backgroundColor: colors.background }
   - Import colors from '@/constants/theme' where not already imported

4. Do NOT change any screen registration or route structure -- only add/update screenOptions.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Desktop/Gym-App && grep -rn "animation:" app/ --include="*.tsx" | head -20</automated>
  </verify>
  <done>All stack navigators have explicit animation props. Workout keeps slide_from_bottom, auth uses fade, all others use slide_from_right.</done>
</task>

</tasks>

<verification>
- App name in app.json is "RPE"
- All 6 icon PNG files exist and have non-zero size
- Every _layout.tsx file has an animation prop in screenOptions
- npx jest --bail passes (no regression from layout changes)
</verification>

<success_criteria>
The app is branded as "RPE" with a magenta dumbbell icon, proper splash screen, and smooth consistent transitions between all screens.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-02-SUMMARY.md`
</output>
