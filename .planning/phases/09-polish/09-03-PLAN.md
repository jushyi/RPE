---
phase: 09-polish
plan: 03
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - app.json
  - eas.json
  - package.json
  - src/components/ui/Skeleton.tsx
  - app/(app)/_layout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "EAS Update is configured with runtimeVersion policy and update URL in app.json"
    - "eas.json has channel configuration for preview and production builds"
    - "expo-updates is installed as a dependency"
    - "A reusable Skeleton loading component exists"
  artifacts:
    - path: "app.json"
      provides: "EAS Update configuration (runtimeVersion + updates.url)"
      contains: "runtimeVersion"
    - path: "eas.json"
      provides: "Channel configuration for builds"
      contains: "channel"
    - path: "src/components/ui/Skeleton.tsx"
      provides: "Reusable loading skeleton component"
      exports: ["Skeleton"]
  key_links:
    - from: "app.json runtimeVersion"
      to: "eas.json channels"
      via: "EAS Update pipeline"
      pattern: "runtimeVersion"
    - from: "app/(app)/_layout.tsx"
      to: "expo-updates"
      via: "OTA update check on app launch"
      pattern: "Updates"
---

<objective>
Configure the EAS Update OTA pipeline for rapid iteration and add edge case polish (loading skeleton component, keyboard handling improvements).

Purpose: The friend group needs to receive updates without App Store resubmission. The app needs graceful loading states for a polished feel.
Output: Working EAS Update configuration, reusable Skeleton component, keyboard handling improvements.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-polish/09-CONTEXT.md
@.planning/phases/09-polish/09-RESEARCH.md

@app.json
@eas.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure EAS Update OTA pipeline</name>
  <files>app.json, eas.json, package.json</files>
  <action>
1. Install expo-updates:
   ```
   npx expo install expo-updates
   ```

2. Update app.json to add EAS Update configuration:
   - Add runtimeVersion with appVersion policy:
     ```json
     "runtimeVersion": {
       "policy": "appVersion"
     }
     ```
   - Add updates URL (using the existing EAS project ID 8ea209dd-0ed7-4d73-9aee-3a6fe3993657):
     ```json
     "updates": {
       "url": "https://u.expo.dev/8ea209dd-0ed7-4d73-9aee-3a6fe3993657"
     }
     ```
   - These go at the top level of the "expo" object

3. Update eas.json to add channel configuration:
   - preview profile: add "channel": "preview"
   - production profile: add "channel": "production"
   - development profile: leave as-is (dev client doesn't use channels)

4. Per Claude's discretion decisions (from RESEARCH.md):
   - Update behavior: Auto-update on launch (default expo-updates behavior, no custom code needed)
   - Channel strategy: Single production channel (simplest for friend group)
   - Runtime version policy: appVersion (safest, predictable)
   - Rollback: Skip explicit setup (EAS provides republish-based rollback by default)

5. Add expo-updates to the plugins array in app.json if needed by SDK 55.

6. Do NOT add any custom update-checking UI or code. The default behavior (check on launch, apply on next restart) is sufficient for the friend group use case.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Desktop/Gym-App && node -e "const p=require('./package.json'); const a=require('./app.json'); const e=require('./eas.json'); console.log('expo-updates installed:', !!p.dependencies['expo-updates']); console.log('runtimeVersion:', JSON.stringify(a.expo.runtimeVersion)); console.log('updates.url:', a.expo.updates?.url); console.log('preview channel:', e.build?.preview?.channel); console.log('production channel:', e.build?.production?.channel);"</automated>
  </verify>
  <done>expo-updates is installed. app.json has runtimeVersion (appVersion policy) and updates URL. eas.json has preview and production channels configured.</done>
</task>

<task type="auto">
  <name>Task 2: Add Skeleton component and edge case polish</name>
  <files>src/components/ui/Skeleton.tsx, app/(app)/_layout.tsx</files>
  <action>
1. Create src/components/ui/Skeleton.tsx:
   - Reusable loading skeleton with Reanimated opacity pulse animation
   - Props: width (number | string), height (number), borderRadius (optional, default 8)
   - Uses colors.surfaceElevated as the skeleton background
   - Animated opacity oscillates between 0.3 and 0.7 with 800ms duration, infinite repeat
   - Export as named export: `export function Skeleton(...)`
   - Follow the exact pattern from RESEARCH.md code example
   - Import React for useEffect

2. Add OTA update check to app layout (app/(app)/_layout.tsx):
   - Import Updates from expo-updates
   - Add a useEffect that calls Updates.checkForUpdateAsync() on mount (wrapped in try/catch)
   - If update available, call Updates.fetchUpdateAsync() then Updates.reloadAsync()
   - Guard with __DEV__ check: only run update logic in production (skip in development)
   - This ensures the friend group always gets the latest version on app launch

3. Verify all existing tests still pass after changes.

NOTE: Do NOT create loading skeletons in every screen -- just create the reusable component. Screens can adopt it incrementally as needed. The Skeleton component is the deliverable, not a full skeleton integration across all screens.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Desktop/Gym-App && node -e "const fs=require('fs'); console.log('Skeleton exists:', fs.existsSync('src/components/ui/Skeleton.tsx'));" && npx jest --bail 2>&1 | tail -10</automated>
  </verify>
  <done>Skeleton.tsx exists with Reanimated animation. OTA update check added to app layout. All tests pass.</done>
</task>

</tasks>

<verification>
- expo-updates in package.json dependencies
- app.json has runtimeVersion and updates.url
- eas.json has channel configs for preview and production
- Skeleton.tsx exists and exports Skeleton component
- App layout checks for updates on mount (production only)
- npx jest --bail passes
</verification>

<success_criteria>
EAS Update pipeline is fully configured and ready for OTA delivery. A reusable Skeleton component exists for loading states. The app checks for updates on launch in production builds.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-03-SUMMARY.md`
</output>
