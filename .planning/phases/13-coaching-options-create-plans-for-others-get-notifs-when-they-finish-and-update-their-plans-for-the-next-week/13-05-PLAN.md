---
phase: 13-coaching-options
plan: 05
type: execute
wave: 4
depends_on: ["13-02", "13-04"]
files_modified:
  - src/features/coaching/utils/notifyCoach.ts
  - src/features/coaching/utils/notifyTrainee.ts
  - src/features/workout/hooks/useWorkoutSession.ts
  - src/features/workout/hooks/usePRDetection.ts
  - src/features/coaching/hooks/useCoachPlans.ts
  - supabase/functions/weekly-summary/index.ts
  - app/(app)/_layout.tsx
autonomous: true
requirements: [COACH-13, COACH-14, COACH-15, COACH-16]
user_setup:
  - service: expo-push
    why: "Push notifications require Expo Access Token for server-side dispatch"
    env_vars:
      - name: EXPO_ACCESS_TOKEN
        source: "Expo Dashboard -> Account Settings -> Access Tokens -> Create token"
    dashboard_config:
      - task: "Store EXPO_ACCESS_TOKEN as Supabase Edge Function secret"
        location: "Supabase Dashboard -> Edge Functions -> Secrets -> Add EXPO_ACCESS_TOKEN"
  - service: supabase-cron
    why: "Weekly coaching summary requires pg_cron extension enabled"
    dashboard_config:
      - task: "Enable pg_cron and pg_net extensions"
        location: "Supabase Dashboard -> Database -> Extensions -> search pg_cron, pg_net -> enable both"
      - task: "Add weekly summary cron job via SQL editor"
        location: "Supabase Dashboard -> SQL Editor -> run the pg_cron schedule query from the migration"

must_haves:
  truths:
    - "Coach receives push notification when trainee completes a workout"
    - "Coach receives push notification when trainee achieves a PR"
    - "Trainee receives push notification when coach updates their plan"
    - "Weekly summary Edge Function aggregates adherence data"
    - "Push token is registered on app startup"
  artifacts:
    - path: "src/features/coaching/utils/notifyCoach.ts"
      provides: "Helper to notify coaches on workout completion and PRs"
      exports: ["notifyCoachWorkoutComplete", "notifyCoachPR"]
    - path: "src/features/coaching/utils/notifyTrainee.ts"
      provides: "Helper to notify trainee on plan update"
      exports: ["notifyTraineePlanUpdate"]
    - path: "supabase/functions/weekly-summary/index.ts"
      provides: "Cron-triggered weekly adherence summary Edge Function"
      exports: ["Deno.serve handler"]
  key_links:
    - from: "src/features/workout/hooks/useWorkoutSession.ts"
      to: "src/features/coaching/utils/notifyCoach.ts"
      via: "Called after workout finish"
      pattern: "notifyCoachWorkoutComplete"
    - from: "src/features/workout/hooks/usePRDetection.ts"
      to: "src/features/coaching/utils/notifyCoach.ts"
      via: "Called when PR detected"
      pattern: "notifyCoachPR"
    - from: "src/features/coaching/hooks/useCoachPlans.ts"
      to: "src/features/coaching/utils/notifyTrainee.ts"
      via: "Called after coach saves plan update"
      pattern: "notifyTraineePlanUpdate"
---

<objective>
Wire up all notification triggers: workout completion notifies coach, PR detection notifies coach, plan update notifies trainee, and create the weekly summary Edge Function. Register push tokens on app startup.

Purpose: Close the notification loop so coaches are informed and trainees know when plans change.
Output: Notification utility functions wired into existing flows, weekly summary Edge Function, push token registration on app launch.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-CONTEXT.md
@.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-RESEARCH.md
@.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-01-SUMMARY.md
@.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-02-SUMMARY.md
@.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-04-SUMMARY.md

<interfaces>
From src/features/workout/hooks/useWorkoutSession.ts (existing):
```typescript
// finishWorkout() saves session, navigates to summary
// Has access to activeSession with session details
```

From src/features/workout/hooks/usePRDetection.ts (existing):
```typescript
// Detects PRs during workout logging
// Has access to exercise info and PR data
```

From src/features/coaching/hooks/useCoachPlans.ts (Plan 04):
```typescript
export function useCoachPlans(traineeId: string) {
  // createPlanForTrainee(name, days, note?)
  // updateTraineePlan(planId, name, days, note?)
}
```

From src/features/notifications/hooks/usePushToken.ts (Plan 01):
```typescript
export function usePushToken(): { token: string | null }
```

Edge Function pattern from supabase/functions/send-push/index.ts (Plan 02):
```typescript
// Accepts: { recipient_ids, title, body, data }
// Dispatches via Expo Push API
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Notification utility functions and workout/PR trigger wiring</name>
  <files>src/features/coaching/utils/notifyCoach.ts, src/features/coaching/utils/notifyTrainee.ts, src/features/workout/hooks/useWorkoutSession.ts, src/features/workout/hooks/usePRDetection.ts, src/features/coaching/hooks/useCoachPlans.ts, app/(app)/_layout.tsx</files>
  <action>
1. **src/features/coaching/utils/notifyCoach.ts**: Export two async functions:

   a. `notifyCoachWorkoutComplete(traineeId: string, traineeName: string, workoutTitle: string, hasPR: boolean)`:
      - Query coaching_relationships where trainee_id = traineeId to get coach_ids.
      - If no coaches, return silently.
      - Call supabase.functions.invoke('send-push', { body: { recipient_ids: coachIds, title: hasPR ? 'PR Alert' : 'Workout Complete', body: `${traineeName} finished ${workoutTitle}`, data: { type: 'workout_complete', trainee_id: traineeId } } }).
      - Fire-and-forget: wrap in try/catch, console.warn on failure, never throw.

   b. `notifyCoachPR(traineeId: string, traineeName: string, exerciseName: string)`:
      - Same pattern: find coaches, invoke send-push with title "New PR", body `${traineeName} hit a PR on ${exerciseName}`.
      - This is called separately from workout complete when a PR is detected mid-workout (real-time notification). The workout-complete notification also has hasPR flag for end-of-session summary.
      - Fire-and-forget.

2. **src/features/coaching/utils/notifyTrainee.ts**: Export async function:

   `notifyTraineePlanUpdate(traineeId: string, coachName: string, planName: string, note?: string)`:
   - Call supabase.functions.invoke('send-push', { body: { recipient_ids: [traineeId], title: 'Plan Updated', body: note ? `${coachName} updated ${planName}: "${note}"` : `${coachName} updated ${planName}`, data: { type: 'plan_update' } } }).
   - Fire-and-forget.

3. **Update useWorkoutSession.ts finishWorkout()**: After the existing workout save logic (near the end of finishWorkout), add a fire-and-forget call:
   ```
   // Notify coaches if this is a plan-based workout
   notifyCoachWorkoutComplete(userId, userName, session.title, sessionHadPR).catch(() => {});
   ```
   Import notifyCoachWorkoutComplete. Get userName from useAuthStore or profiles. Get sessionHadPR from the PR detection state (check if any sets in the session were flagged as PRs).

4. **Update usePRDetection.ts**: When a PR is detected (the existing PR celebration logic), add a fire-and-forget call to notifyCoachPR. Import and call `notifyCoachPR(userId, userName, exerciseName).catch(() => {})`. This sends a real-time notification to coaches mid-workout.

5. **Update useCoachPlans.ts**: In both `createPlanForTrainee` and `updateTraineePlan`, after successful save, call `notifyTraineePlanUpdate(traineeId, coachName, planName, note)`. Get coachName from useAuthStore or profiles. Fire-and-forget.

6. **Update app/(app)/_layout.tsx**: Add `usePushToken()` hook call at the top level of the app layout so push token is registered on every app launch after auth. Import from `@/features/notifications/hooks/usePushToken`.

All notification calls are fire-and-forget (try/catch with console.warn). Notification failures must NEVER block the primary action (workout save, plan save, etc.).
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/Gym-App && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Workout completion notifies coaches. PR detection notifies coaches. Plan update notifies trainee. Push token registered on app startup. All notifications are fire-and-forget.</done>
</task>

<task type="auto">
  <name>Task 2: Weekly summary Edge Function</name>
  <files>supabase/functions/weekly-summary/index.ts</files>
  <action>
Create the weekly-summary Edge Function following the send-push pattern:

1. **Auth**: This function is called by pg_cron (no user JWT). Verify auth via the Authorization header containing service_role_key. If the header does not match service_role, return 401.

2. **Logic**:
   a. Using service_role client, query all coaching_relationships grouped by coach_id.
   b. For each coach, get their trainees' workout_sessions from the past 7 days (ended_at >= now - 7 days).
   c. For each trainee, compute: sessions_completed (count), total exercises, any PRs achieved.
   d. Also get each trainee's plan_days count with weekday assignments to compute adherence (sessions / planned_days).
   e. Build a summary message per coach: "Weekly Summary: {traineeName} completed {N}/{M} planned sessions ({pct}%). {PR count if any}." Concatenate all trainee summaries.

3. **Dispatch**: For each coach, call the Expo Push API directly (same pattern as send-push) with:
   - title: "Weekly Training Summary"
   - body: Concatenated trainee summaries, max ~200 chars (truncate if needed)
   - data: { type: 'weekly_summary' }

4. **Return**: `{ coaches_notified: N }`.

5. **Include pg_cron setup SQL as a comment at the top of the file** so it can be run manually in Supabase SQL editor:
   ```sql
   -- Run in Supabase SQL Editor to schedule weekly summary (Sunday 6pm UTC):
   -- select cron.schedule('weekly-coaching-summary', '0 18 * * 0', $$
   --   select net.http_post(
   --     url := '<SUPABASE_URL>/functions/v1/weekly-summary',
   --     headers := '{"Content-type":"application/json","Authorization":"Bearer <SERVICE_ROLE_KEY>"}'::jsonb,
   --     body := '{"trigger":"cron"}'::jsonb
   --   ) as request_id;
   -- $$);
   ```

Keep the function self-contained. It does not call send-push Edge Function -- it dispatches directly to Expo Push API to avoid nested function calls.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/Gym-App && test -f supabase/functions/weekly-summary/index.ts && grep -c "Deno.serve\|coaching_relationships\|weekly\|exp.host" supabase/functions/weekly-summary/index.ts</automated>
  </verify>
  <done>Weekly summary Edge Function exists, aggregates per-trainee adherence data, dispatches summary push notification to each coach. Includes pg_cron setup SQL as comments.</done>
</task>

</tasks>

<verification>
- Notification helpers fire-and-forget on all trigger points
- Workout finish triggers coach notification
- PR detection triggers coach notification
- Plan update triggers trainee notification
- Push token registered in app layout on startup
- Weekly summary Edge Function computes adherence and notifies coaches
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Coach receives push notification when trainee finishes a workout
- Coach receives push notification when trainee hits a PR
- Trainee receives push notification when coach updates their plan (with optional note text)
- Weekly summary Edge Function aggregates adherence for all trainees and notifies each coach
- Push token is registered on every app launch
- No notification failure blocks any primary action
</success_criteria>

<output>
After completion, create `.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-05-SUMMARY.md`
</output>
