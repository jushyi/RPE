---
phase: 11-add-settings-tab-move-sign-out-to-it-and-have-a-delete-account-option-with-data-export
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/settings/utils/csvExport.ts
  - src/features/settings/hooks/useDataExport.ts
  - tests/settings/csvExport.test.ts
  - tests/__mocks__/expo-file-system.ts
  - tests/__mocks__/expo-sharing.ts
autonomous: true
requirements:
  - SETT-02

must_haves:
  truths:
    - "CSV generation produces valid comma-separated output with proper escaping"
    - "All data categories are exported: workout sessions+sets, plans, body metrics+bodyweight, PR baselines+records"
    - "Export triggers OS share sheet with CSV file"
  artifacts:
    - path: "src/features/settings/utils/csvExport.ts"
      provides: "Pure CSV generation functions for all data categories"
      exports: ["escapeCSVField", "toCSV", "exportWorkoutSessions", "exportPlans", "exportBodyMetrics", "exportPRData"]
    - path: "src/features/settings/hooks/useDataExport.ts"
      provides: "Hook that queries Supabase, generates CSV, writes file, opens share sheet"
      exports: ["useDataExport"]
    - path: "tests/settings/csvExport.test.ts"
      provides: "Unit tests for CSV generation"
  key_links:
    - from: "src/features/settings/hooks/useDataExport.ts"
      to: "src/features/settings/utils/csvExport.ts"
      via: "imports CSV generation functions"
      pattern: "from.*csvExport"
    - from: "src/features/settings/hooks/useDataExport.ts"
      to: "expo-file-system"
      via: "writeAsStringAsync to cacheDirectory"
      pattern: "writeAsStringAsync"
    - from: "src/features/settings/hooks/useDataExport.ts"
      to: "expo-sharing"
      via: "shareAsync with CSV mimeType"
      pattern: "shareAsync"
---

<objective>
Build CSV data export: pure CSV generation utilities, a hook that queries all user data from Supabase and exports via the OS share sheet, plus unit tests for the CSV logic.

Purpose: Users can back up their data anytime as a standalone action from Settings.
Output: Working data export triggered from the Export Data row in AccountSection.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-add-settings-tab-move-sign-out-to-it-and-have-a-delete-account-option-with-data-export/11-CONTEXT.md
@.planning/phases/11-add-settings-tab-move-sign-out-to-it-and-have-a-delete-account-option-with-data-export/11-RESEARCH.md

<interfaces>
<!-- Data tables to export (from Supabase schema): -->
<!-- workout_sessions: id, user_id, plan_id, plan_day_id, title, started_at, finished_at, notes -->
<!-- session_exercises: id, session_id, exercise_id, order_index -->
<!-- set_logs: id, session_exercise_id, set_number, weight, reps, rpe, unit, is_pr, estimated_1rm -->
<!-- plans: id, user_id, name, is_active, created_at -->
<!-- plan_days: id, plan_id, day_of_week, order_index -->
<!-- plan_day_exercises: id, plan_day_id, exercise_id, target_sets, target_reps, target_weight, target_rpe, notes, order_index -->
<!-- bodyweight_logs: id, user_id, weight, unit, logged_at -->
<!-- body_measurements: id, user_id, chest, chest_unit, waist, waist_unit, hips, hips_unit, body_fat_pct, logged_at -->
<!-- pr_baselines: id, user_id, exercise_id, weight, unit -->
<!-- exercises: id, name, muscle_group, equipment (for joining) -->

From src/lib/supabase/client.ts:
```typescript
export const supabase: SupabaseClient;
```

Research recommendation: Single CSV file with section headers separating data categories (expo-sharing only shares one file at a time).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CSV generation utilities with tests</name>
  <files>src/features/settings/utils/csvExport.ts, tests/settings/csvExport.test.ts</files>
  <behavior>
    - escapeCSVField: null/undefined returns empty string
    - escapeCSVField: plain string returns as-is
    - escapeCSVField: string with comma wraps in double quotes
    - escapeCSVField: string with double quote escapes by doubling and wraps
    - escapeCSVField: string with newline wraps in double quotes
    - toCSV: produces header line + data lines joined by newline
    - toCSV: empty rows array produces header line only
    - generateWorkoutCSV: produces section with headers Date, Title, Exercise, Set, Weight, Reps, RPE, Unit, PR, Est1RM
    - generatePlansCSV: produces section with headers Plan, Day, Exercise, Sets, Reps, Weight, RPE, Notes
    - generateBodyMetricsCSV: produces section for bodyweight and body measurements
    - generatePRDataCSV: produces section for PR baselines
    - combineExportSections: joins all sections with blank line separators and section headers
  </behavior>
  <action>
1. **Create `tests/__mocks__/expo-file-system.ts`** and **`tests/__mocks__/expo-sharing.ts`** with Jest mocks (cacheDirectory = '/mock-cache/', writeAsStringAsync = jest.fn(), shareAsync = jest.fn(), isAvailableAsync = jest.fn().mockResolvedValue(true)).

2. **Create `src/features/settings/utils/csvExport.ts`:**
   - `escapeCSVField(field: string | number | null | undefined): string` -- RFC 4180 escaping
   - `toCSV(headers: string[], rows: (string | number | null)[][]): string` -- joins headers and rows
   - `generateWorkoutCSV(sessions: any[]): string` -- flattens sessions > session_exercises > set_logs into rows
   - `generatePlansCSV(plans: any[]): string` -- flattens plans > plan_days > plan_day_exercises
   - `generateBodyMetricsCSV(bodyweight: any[], measurements: any[]): string` -- two sub-sections
   - `generatePRDataCSV(baselines: any[]): string` -- PR baselines with exercise name
   - `combineExportSections(sections: { title: string; csv: string }[]): string` -- joins with `\n\n=== {title} ===\n` separators

3. **Create `tests/settings/csvExport.test.ts`:**
   - Test all behaviors listed above with concrete input/output examples
   - Test edge cases: empty data arrays, special characters in exercise names, null values

4. Run tests: `npm test -- tests/settings/csvExport.test.ts --bail`
  </action>
  <verify>
    <automated>npm test -- tests/settings/csvExport.test.ts --bail</automated>
  </verify>
  <done>All CSV generation tests pass. Pure functions handle escaping, section generation, and combining correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Data export hook with Supabase queries and share sheet</name>
  <files>src/features/settings/hooks/useDataExport.ts</files>
  <action>
1. **Install dependencies:** `npx expo install expo-file-system expo-sharing`

2. **Create `src/features/settings/hooks/useDataExport.ts`:**
   - Export `useDataExport()` hook returning `{ exportData: () => Promise<void>, isExporting: boolean }`
   - `exportData` implementation:
     a. Set isExporting = true
     b. Query Supabase for user's data (use `supabase.auth.getUser()` for user_id):
        - `workout_sessions` with nested `session_exercises(*, set_logs(*), exercises:exercise_id(name))`
        - `plans` with nested `plan_days(*, plan_day_exercises(*, exercises:exercise_id(name)))`
        - `bodyweight_logs` ordered by logged_at
        - `body_measurements` ordered by logged_at
        - `pr_baselines` with exercise join for names
     c. Generate CSV sections using csvExport utils
     d. Combine with `combineExportSections`
     e. Write to `FileSystem.cacheDirectory + 'rpe-export.csv'` using `FileSystem.writeAsStringAsync`
     f. Check `Sharing.isAvailableAsync()`, then call `Sharing.shareAsync(fileUri, { mimeType: 'text/csv', UTI: 'public.comma-separated-values-text', dialogTitle: 'Export RPE Data' })`
     g. Set isExporting = false in finally block
   - Wrap in try/catch, show Alert.alert on error

Note: This hook will be wired into AccountSection's onExport prop by the executor when Plan 01 is also complete. If Plan 01 runs first, the executor for Plan 02 should update `app/(app)/(tabs)/settings.tsx` to pass `exportData` to AccountSection's onExport prop.
  </action>
  <verify>
    <automated>npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>useDataExport hook queries all user data from Supabase, generates combined CSV, and presents share sheet. expo-file-system and expo-sharing installed.</done>
</task>

</tasks>

<verification>
- CSV generation tests pass
- expo-file-system and expo-sharing are installed
- useDataExport hook compiles and handles all data categories
- Share sheet integration uses correct mimeType and UTI
</verification>

<success_criteria>
Export Data functionality is complete: CSV generation is tested, hook queries all data categories, writes to cache, and presents the OS share sheet. Ready to wire into AccountSection onExport prop.
</success_criteria>

<output>
After completion, create `.planning/phases/11-add-settings-tab-move-sign-out-to-it-and-have-a-delete-account-option-with-data-export/11-02-SUMMARY.md`
</output>
