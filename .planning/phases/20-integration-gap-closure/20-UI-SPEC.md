---
phase: 20
slug: integration-gap-closure
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-18
---

# Phase 20 — UI Design Contract

> Visual and interaction contract for Phase 20: Integration Gap Closure. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Phase UI Scope

Phase 20 is a **bug-fix / gap-closure phase** with no new screens and no new features. Five of the six tasks are code-only (no visual changes). The single UI-visible change is a **warning banner on the videos gallery screen** surfacing failed video uploads.

| Task | UI Change? | Description |
|------|-----------|-------------|
| AUTH-03 sign-out data isolation | No | Array addition in `clearUserData.ts` |
| ALRM-02 alarm deep link | No | Data payload addition + router case; target screen already exists |
| VID-03 video queue retry | **Yes** | Failed-upload warning banner on videos gallery screen |
| NOTIF-02/04 social deep links | Minimal | `GroupTabs` gains `initialTab` prop for chat deep link; no visual change |
| Body-metrics unit defaults | No | `useState` initial value reads from store instead of hardcoded |
| VERIFICATION.md generation | No | Documentation only |

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (React Native + NativeWind/Tailwind) |
| Preset | not applicable |
| Component library | none (custom `src/components/ui/`) |
| Icon library | `@expo/vector-icons` (Ionicons) |
| Font | System default (platform native) |

Source: Codebase scan -- no `components.json`, no shadcn. Project uses `StyleSheet.create` + `src/constants/theme.ts`.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, icon-text gap, banner margin bottom |
| md | 16px | Default element spacing, list horizontal padding |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- **12px** -- Used for card padding and border radius throughout the videos gallery screen (`galleryItem` style uses `padding: 12`, `borderRadius: 12`; `storageHeader` uses `paddingVertical: 12`). Justified as an established codebase pattern predating this phase. The `FailedUploadBanner` uses 12px padding and 12px border radius to match adjacent `galleryItem` cards.

Source: Existing codebase patterns -- `padding: 12` (cards), `paddingHorizontal: 16` (lists), `gap: 8` (inline rows), `paddingVertical: 60` (empty states).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 15px | 400 (regular) | 1.5 |
| Label / Caption | 13px | 400 (regular) | 1.4 |
| Small caption | 12px | 400 (regular) | 1.4 |
| Heading / Nav title | 16px | 700 (bold) | 1.2 |

Source: Existing codebase -- `videos.tsx` uses 12px (date), 13px (storage/set info), 15px (exercise name), 16px (nav title). Weights: 400 default, 700 (bold for nav titles).

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0a0a0a` (`colors.background`) | Screen backgrounds |
| Secondary (30%) | `#1a1a1a` (`colors.surface`) | Cards, list items, banners |
| Surface elevated | `#252525` (`colors.surfaceElevated`) | Nested elevated elements |
| Accent (10%) | `#ec4899` (`colors.accent`) | Primary CTA buttons, activity indicators, pull-to-refresh tint |
| Text primary | `#f5f5f5` (`colors.textPrimary`) | Main text, nav titles |
| Text secondary | `#a3a3a3` (`colors.textSecondary`) | Supporting text, metadata |
| Text muted | `#737373` (`colors.textMuted`) | Disabled text, timestamps, empty state icons |
| Success | `#22c55e` (`colors.success`) | Success states |
| Warning | `#f59e0b` (`colors.warning`) | Warning banner background/text, connectivity alerts |
| Error / Destructive | `#ef4444` (`colors.error`) | Delete actions, error text, error borders, failed-upload indicator |

Accent reserved for: Primary CTA button fills, loading spinners, pull-to-refresh indicator. Never used for error/warning states.

Source: `src/constants/theme.ts` (runtime colors) and `tailwind.config.js` (NativeWind classes).

---

## Video Upload Failed Banner (VID-03)

The only new UI element in this phase. Appears at the top of the videos gallery FlatList, between the existing storage header and the first gallery item.

### Layout

```
+--------------------------------------------------+
| [warning-outline icon]  X video(s) failed to     |
|                         upload. Tap to retry.     |
+--------------------------------------------------+
```

### Specifications

| Property | Value | Rationale |
|----------|-------|-----------|
| Container | `View` with `flexDirection: 'row'`, `alignItems: 'center'` | Matches `storageHeader` pattern |
| Background | `colors.surface` (`#1a1a1a`) | Consistent with card elements |
| Border | 1px solid `colors.warning` (`#f59e0b`) | Draws attention without overwhelming |
| Border radius | 12px (exception) | Matches `galleryItem` radius |
| Padding | 12px horizontal, 12px vertical (exception) | Matches `galleryItem` padding |
| Margin bottom | 8px (sm token) | Separates banner from first gallery item; aligns to spacing scale |
| Icon | `Ionicons` name `warning-outline`, size 20, color `colors.warning` | Warning severity, not error |
| Icon margin right | 8px | Standard sm token |
| Text | 13px, weight 400, color `colors.warning` | Label/Caption role from type scale |
| Pressable | Entire banner is a `Pressable` that triggers manual retry of all failed items | Single tap target |
| Pressed state | `opacity: 0.7` | Matches existing press feedback pattern |
| Retry behavior | On tap: reset `retryCount` to 0 for all failed items and call `flushVideoQueue()` | Per CONTEXT.md retry specification |

### Visibility Rule

- Show only when at least 1 item in `videoUploadQueue` has `retryCount >= 3`
- Count label is dynamic: `"1 video failed to upload"` (singular) / `"3 videos failed to upload"` (plural)
- Banner disappears when queue drains or retry succeeds

### Empty State (no change)

Existing empty state in videos gallery remains unchanged:
- Icon: `videocam-off-outline`, size 48, color `colors.textMuted`
- Text: "No videos recorded yet", 16px, color `colors.textMuted`
- Vertical padding: 60px

---

## GroupTabs initialTab Prop (NOTIF-02/04)

### Interaction Contract

- `GroupTabs` component accepts new optional prop: `initialTab?: 'feed' | 'chat'`
- Default value: `'feed'` (no change to existing behavior)
- `GroupFeedScreen` reads `tab` query parameter from route: `useLocalSearchParams<{ groupId: string; groupName?: string; tab?: 'feed' | 'chat' }>()`
- Passes `tab` value to `GroupTabs` as `initialTab`
- No visual change -- same tab UI, just correct initial selection on deep link

### Deep Link Route Mapping

| Notification type | Route | Initial tab |
|-------------------|-------|-------------|
| `group_share` | `/(app)/social/group-feed?groupId={id}` | `feed` (default) |
| `chat_message` | `/(app)/social/group-feed?groupId={id}&tab=chat` | `chat` |
| `alarm` / `nudge` | `/(app)/plans/{plan_id}` | n/a |
| `alarm` / `nudge` (fallback) | `/(app)` | n/a |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Video failed banner (singular) | "1 video failed to upload. Tap to retry." |
| Video failed banner (plural) | "{N} videos failed to upload. Tap to retry." |
| Video delete confirmation title | "Delete Video?" (existing, no change) |
| Video delete confirmation body | "This will permanently remove this video." (existing, no change) |
| Videos empty state | "No videos recorded yet" (existing, no change) |

No new CTAs, empty states, or error states beyond the failed-upload banner. All other phase tasks are code-only with no user-facing copy.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| Not applicable | none | No shadcn in project; no registry dependencies |

This is a React Native project using `@expo/vector-icons` for icons and `StyleSheet.create` for styling. No component registries are in use.

---

## Component Inventory (New / Modified)

| Component | Status | Purpose |
|-----------|--------|---------|
| `FailedUploadBanner` | **New** | Warning banner for videos gallery; inline in `videos.tsx` or extracted to `src/features/videos/components/` |
| `GroupTabs` | **Modified** | Add `initialTab` prop (optional, defaults to `'feed'`) |
| `GroupFeedScreen` | **Modified** | Read `tab` query param, pass to `GroupTabs` |

All other changes are to stores, utilities, types, and hooks with no UI surface.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
