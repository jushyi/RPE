---
phase: 15.1-reverse-bar-loading
plan: 02
type: execute
wave: 2
depends_on: ["15.1-01"]
files_modified:
  - src/features/calculator/components/PlateCalculator.tsx
  - src/features/calculator/components/PlateStepperList.tsx
autonomous: false
requirements:
  - PCALC-04
  - PCALC-05

must_haves:
  truths:
    - "User can switch between Weight > Plates and Plates > Weight modes via segmented control"
    - "In reverse mode, user sees +/- steppers for each enabled plate size"
    - "Tapping + adds a plate per side; tapping - removes one; count shown between buttons"
    - "Total weight updates live above the barbell diagram as plates are added/removed"
    - "Barbell diagram updates live showing the plates from stepper counts"
    - "Clear All button resets all steppers to zero"
    - "Disabled plates from inventory are hidden from stepper list (not grayed out)"
  artifacts:
    - path: "src/features/calculator/components/PlateStepperList.tsx"
      provides: "Reverse mode plate stepper rows with +/- controls"
      exports: ["PlateStepperList"]
    - path: "src/features/calculator/components/PlateCalculator.tsx"
      provides: "Mode toggle and dual-mode plate calculator"
      contains: "mode.*Weight.*Plates"
  key_links:
    - from: "src/features/calculator/components/PlateCalculator.tsx"
      to: "src/features/calculator/utils/reverseCalc.ts"
      via: "calculateTotalWeight and countsToBreakdown for reverse mode"
      pattern: "calculateTotalWeight|countsToBreakdown"
    - from: "src/features/calculator/components/PlateStepperList.tsx"
      to: "src/features/calculator/constants/plates.ts"
      via: "plate colors for stepper chips"
      pattern: "PLATE_COLORS"
---

<objective>
Add reverse bar loading mode (Plates > Weight) with mode toggle and stepper UI.

Purpose: Users at the gym often need to work the other direction -- they have plates loaded and want to know the total weight. The reverse mode lets them tap +/- steppers per plate size to build up a bar and see the total weight in real time.
Output: PlateStepperList component, mode toggle segmented control, fully working dual-mode PlateCalculator.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-reverse-bar-loading-for-plates-tab-in-calc-also-ability-to-choose-which-plates-i-actually-have-some-users-may-not-have-the-red-55-lbs-or-35kg-plates-adaptable-for-situations/15.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/features/calculator/utils/reverseCalc.ts (created in Plan 01):
```typescript
export function calculateTotalWeight(plateCounts: Record<number, number>, barWeight: number): number;
export function countsToBreakdown(plateCounts: Record<number, number>): { weight: number; count: number }[];
export function getMissingPlateMessage(remainder: number, enabledPlates: number[], allPlates: number[], unitLabel: string): string;
```

From src/features/calculator/hooks/usePlateInventory.ts (created in Plan 01):
```typescript
export function usePlateInventory(unit: 'kg' | 'lbs'): {
  enabledPlates: number[];
  allPlates: number[];
  toggle: (weight: number) => void;
  enabledCount: number;
  totalCount: number;
};
```

From src/features/calculator/types.ts (updated in Plan 01):
```typescript
export type PlateCount = Record<number, number>;
```

From src/features/calculator/constants/plates.ts:
```typescript
export const PLATE_COLORS_LB: Record<number, string>;
export const PLATE_COLORS_KG: Record<number, string>;
```

From src/features/calculator/components/PlateCalculator.tsx (existing toggle style):
```typescript
// toggleRow, toggleBtn, toggleBtnActive, toggleText, toggleTextActive styles
// These are the styles to reuse/extend for the mode toggle segmented control
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlateStepperList component and reverse mode integration in PlateCalculator</name>
  <files>
    src/features/calculator/components/PlateStepperList.tsx,
    src/features/calculator/components/PlateCalculator.tsx
  </files>
  <action>
    1. Create src/features/calculator/components/PlateStepperList.tsx:
       - Props: `enabledPlates: number[]`, `plateCounts: Record<number, number>`, `unit: 'kg' | 'lbs'`, `onIncrement: (weight: number) => void`, `onDecrement: (weight: number) => void`
       - Renders one row per enabled plate (sorted descending by weight)
       - Each row has: colored chip with plate weight + unit label (background from PLATE_COLORS_LB/KG), then stepper controls on right side
       - Stepper controls: minus button (Ionicons "remove-circle-outline"), count text, plus button (Ionicons "add-circle-outline")
       - Minus button disabled (muted color) when count is 0
       - Count text shows per-side count (e.g., "2" means 2 plates per side)
       - Chip colors match BarbellDiagram plate colors for visual consistency
       - Note under stepper list: "Counts are per side" in textSecondary
       - No emojis per CLAUDE.md

    2. Modify PlateCalculator.tsx to add dual-mode support:
       a. Add mode state: `const [mode, setMode] = useState<'weight-to-plates' | 'plates-to-weight'>('weight-to-plates')`
       b. Add plateCounts state: `const [plateCounts, setPlateCounts] = useState<Record<number, number>>({})` -- reset when unit changes
       c. Add mode toggle segmented control ABOVE the unit toggle:
          - Two segments: "Weight > Plates" and "Plates > Weight"
          - Reuse existing toggleRow/toggleBtn/toggleBtnActive style pattern
          - Slightly wider buttons for longer text (paddingHorizontal: 16)
       d. When mode is 'weight-to-plates': show existing UI (weight input, bar picker, diagram, summary, warning) -- no changes
       e. When mode is 'plates-to-weight':
          - Hide weight TextInput
          - Show total weight prominently: large text showing `calculateTotalWeight(plateCounts, barWeight)` with unit label
          - Show BarWeightPicker (shared between modes)
          - Show PlateStepperList with enabledPlates from usePlateInventory
          - Show BarbellDiagram with plates from `countsToBreakdown(plateCounts)`
          - Show "Clear All" button (Pressable, accent border, centered text) that resets plateCounts to {}
       f. Both modes share: unit toggle, bar weight picker, My Plates section, BarbellDiagram
       g. Reset plateCounts when unit changes (useEffect on unit)
       h. Increment handler: `setPlateCounts(prev => ({ ...prev, [weight]: (prev[weight] || 0) + 1 }))`
       i. Decrement handler: `setPlateCounts(prev => { const c = (prev[weight] || 0) - 1; return { ...prev, [weight]: Math.max(0, c) }; })`
       j. Import calculateTotalWeight, countsToBreakdown from reverseCalc.ts
  </action>
  <verify>
    <automated>npx jest tests/calculator/ --bail</automated>
  </verify>
  <done>Mode toggle switches between Weight > Plates and Plates > Weight. Reverse mode shows stepper rows for each enabled plate. Total weight updates live. BarbellDiagram shows plates matching stepper counts. Clear All resets to empty bar. Disabled plates hidden from steppers. All calculator tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dual-mode plate calculator</name>
  <files>src/features/calculator/components/PlateCalculator.tsx</files>
  <action>
    Human verifies the complete dual-mode plate calculator with customizable plate inventory.
    Weight > Plates mode now respects inventory.
    Plates > Weight mode lets users build a bar with +/- steppers and see total weight.
  </action>
  <verify>
    1. Open the app and navigate to the Calc tab > Plates sub-tool
    2. Verify mode toggle at top: "Weight > Plates" and "Plates > Weight"
    3. In "Weight > Plates" mode:
       a. Expand "My Plates" section, disable 55 lb plate
       b. Enter 225 lb -- verify it uses alternative plates (no 55s) and warning mentions "55 lb plates not in your inventory"
       c. Re-enable 55 lb, verify calculation updates
    4. Switch to "Plates > Weight" mode:
       a. Verify stepper rows for each enabled plate size with colored chips
       b. Add 2x45 lb plates (tap + twice) -- verify total shows "225 lb" (45 bar + 2*2*45)
       c. Add 1x25 lb -- verify total updates to "275 lb"
       d. Verify barbell diagram shows plates matching your selections
       e. Tap "Clear All" -- verify everything resets to bar weight only
    5. Switch unit to kg:
       a. Verify stepper list shows kg plates
       b. Verify "My Plates" shows kg inventory (independent from lb)
       c. Switch back to lb -- verify lb inventory preserved
    6. Verify no emojis anywhere in the UI
  </verify>
  <done>User confirms both modes work correctly, inventory persists per unit, and diagram renders properly. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- Mode toggle switches cleanly between both modes
- Reverse mode total weight calculation is mathematically correct
- Barbell diagram renders correctly in both modes
- Plate inventory affects both modes
- Unit switching preserves per-unit inventory
- No visual glitches or clipping with many plates
- All calculator tests pass: `npx jest tests/calculator/ --bail`
</verification>

<success_criteria>
- Segmented mode toggle visible and functional
- Reverse mode shows stepper rows with colored plate chips
- Total weight updates live as plates are added/removed
- Clear All resets reverse mode
- Both modes share bar picker, unit toggle, inventory, and diagram
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-reverse-bar-loading-for-plates-tab-in-calc-also-ability-to-choose-which-plates-i-actually-have-some-users-may-not-have-the-red-55-lbs-or-35kg-plates-adaptable-for-situations/15.1-02-SUMMARY.md`
</output>
