---
phase: 17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260318000001_create_social.sql
  - src/features/social/types.ts
  - src/features/social/utils/handleValidation.ts
  - src/features/social/utils/friendInviteCode.ts
  - src/features/social/utils/sharePayload.ts
  - src/features/social/utils/reactionIcons.ts
  - src/stores/friendshipStore.ts
  - src/stores/socialStore.ts
  - tests/social/handleValidation.test.ts
  - tests/social/friendInviteCode.test.ts
  - tests/social/sharePayload.test.ts
  - tests/social/reactionIcons.test.ts
autonomous: true
requirements:
  - SOCL-01
  - SOCL-13
  - SOCL-14

must_haves:
  truths:
    - "Database tables exist for friendships, groups, shared items, and reactions with RLS"
    - "Handle column exists on profiles with unique constraint"
    - "TypeScript types cover all social entities"
    - "Pure utility functions are tested for handle validation, invite codes, share payloads, reaction icons"
  artifacts:
    - path: "supabase/migrations/20260318000001_create_social.sql"
      provides: "All social tables, RLS policies, helper functions, handle column"
      contains: "friend_requests"
    - path: "src/features/social/types.ts"
      provides: "Friendship, FriendRequest, Group, GroupMember, SharedItem, Reaction types"
      exports: ["Friendship", "FriendRequest", "Group", "GroupMember", "SharedItem", "Reaction"]
    - path: "src/stores/friendshipStore.ts"
      provides: "Friend list and friend request state"
    - path: "src/stores/socialStore.ts"
      provides: "Groups, feed, reactions state"
  key_links:
    - from: "src/features/social/types.ts"
      to: "supabase/migrations/20260318000001_create_social.sql"
      via: "types mirror DB schema"
      pattern: "interface.*Friendship|Group|SharedItem|Reaction"
---

<objective>
Create the database foundation and core utilities for the social feature: migration with all tables (friend_requests, friendships, groups, group_members, shared_items, reactions) and RLS policies, TypeScript types, pure utility functions with tests, and Zustand stores.

Purpose: Everything else in Phase 17 depends on this schema and type layer.
Output: Migration SQL, types, tested utils, two Zustand stores.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-CONTEXT.md
@.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-RESEARCH.md

<interfaces>
<!-- Existing patterns to follow -->

From src/features/coaching/types.ts:
```typescript
export interface CoachingRelationship {
  id: string;
  coach_id: string;
  trainee_id: string;
  created_at: string;
}
export interface InviteCode {
  id: string;
  coach_id: string;
  code: string;
  expires_at: string;
  redeemed_by: string | null;
  created_at: string;
}
```

From src/features/coaching/utils/inviteCode.ts:
```typescript
const INVITE_CHARS = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
export const INVITE_CODE_EXPIRY_HOURS = 24;
export function generateInviteCode(): string;
```

From src/stores/coachingStore.ts (pattern):
```typescript
// Zustand + MMKV persist pattern used by all stores
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration + types + pure utility functions with tests</name>
  <files>
    supabase/migrations/20260318000001_create_social.sql,
    src/features/social/types.ts,
    src/features/social/utils/handleValidation.ts,
    src/features/social/utils/friendInviteCode.ts,
    src/features/social/utils/sharePayload.ts,
    src/features/social/utils/reactionIcons.ts,
    tests/social/handleValidation.test.ts,
    tests/social/friendInviteCode.test.ts,
    tests/social/sharePayload.test.ts,
    tests/social/reactionIcons.test.ts
  </files>
  <behavior>
    - handleValidation: validates 3-20 chars, lowercase alphanumeric + underscores, must start with letter; returns null on valid, error string on invalid
    - friendInviteCode: generates 6-char code from unambiguous charset (reuse INVITE_CHARS), returns string
    - sharePayload: buildWorkoutPayload(session) returns {content_type:'workout', payload:{exercise_names, total_sets, total_volume, duration_minutes}}; buildPRPayload(exercise_name, weight, reps, unit) returns correct shape; buildVideoPayload(video_url, exercise_name, weight, reps, unit, set_number) returns correct shape
    - reactionIcons: REACTION_ICONS array has 5 entries each with key, icon (Ionicons name), label; getReactionIcon(key) returns correct icon entry
  </behavior>
  <action>
    1. Write tests first (RED phase) for all 4 utility modules.

    2. Create the migration SQL `20260318000001_create_social.sql`:
       - ALTER profiles ADD COLUMN handle TEXT UNIQUE with index
       - Profile search RPC function `search_profiles_by_handle(query TEXT)` with SECURITY DEFINER returning only id, display_name, avatar_url, handle
       - friend_requests table (id, sender_id, receiver_id, status CHECK pending/accepted/rejected, created_at, UNIQUE sender+receiver, CHECK sender!=receiver)
       - friendships table (id, user_a, user_b, created_at, UNIQUE user_a+user_b, CHECK user_a < user_b for canonical ordering)
       - friend_invite_codes table (id, user_id, code UNIQUE, expires_at, redeemed_by nullable, created_at)
       - groups table (id, name, created_by, created_at)
       - group_members table (id, group_id, user_id, muted BOOLEAN default false, joined_at, UNIQUE group_id+user_id)
       - shared_items table (id, group_id, user_id, content_type CHECK workout/pr/video, payload JSONB, created_at) with index on (group_id, created_at DESC)
       - reactions table (id, shared_item_id, user_id, emoji TEXT, created_at, UNIQUE shared_item_id+user_id+emoji)
       - Enable RLS on ALL tables
       - RLS helper: is_group_member(target_group_id UUID) SECURITY DEFINER STABLE
       - RLS helper: is_friend_with(target_user_id UUID) checks friendships table both directions
       - RLS policies per RESEARCH.md Pattern 2: group members can view group data, author can insert shared items, members can add/view/remove reactions
       - Friend request RLS: sender and receiver can view, authenticated can insert own requests
       - Friendship RLS: either party can view, system manages inserts (via trigger on friend_request accept)
       - Trigger: on friend_request status update to 'accepted', auto-insert into friendships with canonical ordering

    3. Create `src/features/social/types.ts` mirroring the schema:
       - FriendRequest, Friendship, FriendInviteCode, Group, GroupMember, SharedItem (with content_type discriminator), Reaction, WorkoutSharePayload, PRSharePayload, VideoSharePayload, FriendProfile (id, display_name, avatar_url, handle)

    4. Implement the 4 utility modules to make tests pass (GREEN phase):
       - handleValidation.ts: validateHandle(handle: string) => string | null, HANDLE_REGEX
       - friendInviteCode.ts: reuse exact same INVITE_CHARS and generateInviteCode() pattern from coaching, export FRIEND_INVITE_CODE_EXPIRY_HOURS = 24
       - sharePayload.ts: buildWorkoutPayload, buildPRPayload, buildVideoPayload
       - reactionIcons.ts: REACTION_ICONS array using Ionicons names (flame-outline, fitness-outline, thumbs-up-outline, trophy-outline, heart-outline), getReactionIcon(key) helper. Per CLAUDE.md: NO emoji characters -- use icon key strings only.
  </action>
  <verify>
    <automated>npx jest --bail --testPathPattern=social</automated>
  </verify>
  <done>Migration SQL creates all tables with RLS. Types exported. All 4 utility test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Zustand stores for friendships and social data</name>
  <files>
    src/stores/friendshipStore.ts,
    src/stores/socialStore.ts
  </files>
  <action>
    Create two Zustand stores following the exact same pattern as coachingStore.ts (Zustand + MMKV persist).

    1. `friendshipStore.ts`:
       - State: friends (FriendProfile[]), pendingRequests (FriendRequest[]), sentRequests (FriendRequest[]), myHandle (string | null), loading (boolean)
       - Actions: fetchFriends(), fetchPendingRequests(), fetchSentRequests(), sendFriendRequest(receiverId: string), acceptRequest(requestId: string), rejectRequest(requestId: string), unfriend(friendshipId: string), generateFriendInviteCode(), redeemFriendInviteCode(code: string), searchByHandle(query: string) => Promise<FriendProfile[]>, setMyHandle(handle: string), fetchMyHandle()
       - Use `as any` pattern for Supabase .from() calls on tables not in generated types
       - searchByHandle calls the `search_profiles_by_handle` RPC function
       - acceptRequest triggers the DB trigger which auto-creates friendship, then refetch friends
       - MMKV persist key: 'friendship-store'

    2. `socialStore.ts`:
       - State: groups (Group[]), groupMembers (Map<string, GroupMember[]>), feedItems (Map<string, SharedItem[]>), feedCursors (Map<string, string>), reactions (Map<string, Reaction[]>), loading (boolean)
       - Actions: fetchGroups(), createGroup(name: string, memberIds: string[]), leaveGroup(groupId: string), addMemberToGroup(groupId: string, userId: string), removeMemberFromGroup(groupId: string, userId: string), toggleMuteGroup(groupId: string), fetchFeed(groupId: string, cursor?: string), shareToGroups(groupIds: string[], items: SharedItem[]), addReaction(sharedItemId: string, emoji: string), removeReaction(reactionId: string), fetchReactions(sharedItemId: string)
       - createGroup must also insert creator as group_member in the same call (Pitfall 3 from RESEARCH.md)
       - Feed pagination: cursor-based using created_at, 20 items per page per RESEARCH.md Pattern 5
       - MMKV persist key: 'social-store'
  </action>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <done>Both stores export all listed state and actions. Full test suite passes (no regressions).</done>
</task>

</tasks>

<verification>
- Migration SQL file exists and contains all 7 tables + RLS policies
- Types file exports all social entity interfaces
- All utility tests pass: `npx jest --bail --testPathPattern=social`
- Full suite passes: `npx jest --bail`
- Both stores follow Zustand + MMKV pattern
</verification>

<success_criteria>
- Database schema covers friendships, groups, shared items, reactions with RLS
- Handle column on profiles with unique constraint and search RPC
- All pure utility functions tested
- Stores ready for UI consumption in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-01-SUMMARY.md`
</output>
