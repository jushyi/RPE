---
phase: 09-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [app/(app)/plans/[id].tsx]
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Tapping Edit on plan detail triggers a visible transition animation before edit mode appears"
    - "Saving in edit mode triggers a visible transition animation before returning to view mode"
  artifacts:
    - path: "app/(app)/plans/[id].tsx"
      provides: "Animated view/edit mode toggle"
      contains: "LayoutAnimation"
  key_links:
    - from: "app/(app)/plans/[id].tsx"
      to: "LayoutAnimation"
      via: "configureNext called before setIsEditing"
      pattern: "LayoutAnimation\\.configureNext"
---

<objective>
Add a visual transition animation when toggling between view and edit modes on the plan detail screen.

Purpose: UAT Test 5 reported no animation when clicking Edit or saving. The root cause is that edit/save are in-screen state toggles (isEditing boolean), not router navigations. Adding LayoutAnimation.configureNext() before each state change will produce a smooth fade/layout transition.
Output: Plan detail screen with animated view/edit mode transitions.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-polish/09-UAT.md
@app/(app)/plans/[id].tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LayoutAnimation transitions to plan detail view/edit toggle</name>
  <files>app/(app)/plans/[id].tsx</files>
  <action>
    Import LayoutAnimation from react-native (already available, no new dependency).

    In enterEditMode(), add `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut);` BEFORE the state changes (setDraftName, setDraftDays, setIsEditing).

    In handleSave(), add `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut);` BEFORE `setIsEditing(false)` inside the success branch.

    In cancelEdit(), add `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut);` BEFORE `setIsEditing(false)`.

    This is the same pattern used in Phase 03 for collapsible sections (per decision: "LayoutAnimation for collapsible sections (simpler than Reanimated per RESEARCH.md)").

    Note: LayoutAnimation is already imported from react-native in the imports block -- just not used yet. No new imports needed beyond adding LayoutAnimation to the existing react-native import destructure.
  </action>
  <verify>
    <automated>cd c:/Users/maser/Projects/Gym-App && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Entering edit mode (tap Edit), saving, and canceling all trigger a smooth easeInEaseOut layout animation. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- Open plan detail screen, tap Edit -- content transitions smoothly (not instant swap)
- Make a change and tap Save -- transitions back to view mode with animation
- Enter edit mode and tap X (cancel) -- transitions back with animation
- No TypeScript compilation errors
</verification>

<success_criteria>
Plan detail view/edit toggle has a visible layout animation on every state change, closing the UAT gap from Test 5.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-04-SUMMARY.md`
</output>
