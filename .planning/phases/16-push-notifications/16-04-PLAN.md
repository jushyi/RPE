---
phase: 16-push-notifications
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_layout.tsx
autonomous: true
gap_closure: true
requirements: [NOTIF-05]

must_haves:
  truths:
    - "Dev tools trigger buttons deliver actual visible notifications to the device"
    - "Notifications table exists in remote Supabase and fetch succeeds"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Notification handler enabling foreground presentation"
      contains: "Notifications.setNotificationHandler"
  key_links:
    - from: "app/_layout.tsx"
      to: "expo-notifications foreground handler"
      via: "Notifications.setNotificationHandler at module level"
      pattern: "setNotificationHandler"
---

<objective>
Fix two blockers from UAT: (1) the notifications table is missing from remote Supabase so the inbox fetch crashes, and (2) expo-notifications suppresses all foreground notifications because setNotificationHandler was never called.

Purpose: Unblock the notification inbox (pull-to-refresh returns data) and make dev tools trigger buttons actually deliver visible notifications to the device.
Output: Remote DB has notifications table; foreground notifications appear on device.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-push-notifications/16-01-SUMMARY.md
@.planning/phases/16-push-notifications/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply notifications migration to remote Supabase</name>
  <files>supabase/migrations/20260318000000_create_notifications.sql</files>
  <action>
    The migration file `supabase/migrations/20260318000000_create_notifications.sql` exists locally but has never been pushed to the remote Supabase project. The fetch in notificationStore fails at runtime with "Could not find the table 'public.notifications' in the schema cache".

    Run the following command from the project root to apply the pending migration:

    ```
    npx supabase db push
    ```

    If `supabase db push` prompts for confirmation, confirm. If it reports the migration is already applied (unlikely given the UAT error), the table already exists and no further action is needed for this task.

    Do NOT run `supabase db reset` — that would wipe production data. Only push the delta migration.
  </action>
  <verify>
    <automated>npx supabase migration list 2>&1 | grep "20260318000000"</automated>
  </verify>
  <done>Migration 20260318000000_create_notifications shows as "applied" in `supabase migration list` output</done>
</task>

<task type="auto">
  <name>Task 2: Add setNotificationHandler to app/_layout.tsx</name>
  <files>app/_layout.tsx</files>
  <action>
    expo-notifications silently suppresses all foreground notifications unless `Notifications.setNotificationHandler` is configured. The debug log in dev tools fires (listeners work) but nothing appears on screen or in the OS notification tray because the handler is absent.

    In `app/_layout.tsx`, add the handler call at module level — BEFORE the `RootLayout` function definition, immediately after the imports:

    ```typescript
    // Configure foreground notification presentation
    Notifications.setNotificationHandler({
      handleNotification: async () => ({
        shouldShowAlert: true,
        shouldPlaySound: true,
        shouldSetBadge: false,
      }),
    });
    ```

    Place this between the last import line and the `export default function RootLayout()` declaration. Do not place it inside a useEffect or component body — it must run synchronously at module load time.

    The file already imports `* as Notifications from 'expo-notifications'` so no new import is needed.
  </action>
  <verify>
    <automated>grep -n "setNotificationHandler" /c/Users/maser/Projects/Gym-App/app/_layout.tsx</automated>
  </verify>
  <done>`setNotificationHandler` call is present at module level in app/_layout.tsx with shouldShowAlert: true, shouldPlaySound: true, shouldSetBadge: false</done>
</task>

</tasks>

<verification>
After both tasks:
1. `supabase migration list` shows 20260318000000_create_notifications as applied
2. `grep setNotificationHandler app/_layout.tsx` returns the call outside any function
3. Dev tools alarm/nudge buttons (3-second delay) should produce a visible OS notification on device after rebuild
</verification>

<success_criteria>
- Pull-to-refresh in Notification Inbox no longer shows "Could not find the table 'public.notifications'"
- Dev tools trigger buttons deliver actual notifications to the device (local alarm/nudge appear after 3s delay)
- No regression to existing notification response listener or snooze handler
</success_criteria>

<output>
After completion, create `.planning/phases/16-push-notifications/16-04-SUMMARY.md`
</output>
