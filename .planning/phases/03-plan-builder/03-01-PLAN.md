---
phase: 03-plan-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260311000000_create_plans.sql
  - src/lib/supabase/types/database.ts
  - src/features/plans/types.ts
  - src/features/plans/constants.ts
  - src/stores/planStore.ts
  - src/features/plans/hooks/usePlans.ts
  - src/features/plans/components/PlanCard.tsx
  - src/features/plans/components/PlanEmptyState.tsx
  - app/(app)/(tabs)/plans.tsx
  - app/(app)/(tabs)/_layout.tsx
  - tests/plans/plan-store.test.ts
  - tests/plans/plan-days.test.ts
  - tests/plans/plan-exercises.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02

must_haves:
  truths:
    - "Plans tab appears in bottom navigation alongside Home and Exercises"
    - "Empty state shows when user has no plans with a Create button"
    - "Plan data persists in Supabase with RLS ownership enforcement"
    - "Plan store loads and caches plans locally via MMKV"
  artifacts:
    - path: "supabase/migrations/20260311000000_create_plans.sql"
      provides: "Three-table schema (workout_plans, plan_days, plan_day_exercises) with RLS and active plan trigger"
      contains: "CREATE TABLE public.workout_plans"
    - path: "src/features/plans/types.ts"
      provides: "TypeScript interfaces for Plan, PlanDay, PlanDayExercise, TargetSet"
      exports: ["Plan", "PlanDay", "PlanDayExercise", "TargetSet", "PlanSummary"]
    - path: "src/stores/planStore.ts"
      provides: "Zustand store with MMKV persistence for plans"
      exports: ["usePlanStore"]
    - path: "src/features/plans/hooks/usePlans.ts"
      provides: "CRUD hook bridging store and Supabase"
      exports: ["usePlans"]
    - path: "app/(app)/(tabs)/plans.tsx"
      provides: "Plan list screen with empty state"
      min_lines: 30
  key_links:
    - from: "src/stores/planStore.ts"
      to: "src/features/plans/types.ts"
      via: "import Plan type"
      pattern: "import.*Plan.*from.*features/plans/types"
    - from: "src/features/plans/hooks/usePlans.ts"
      to: "src/stores/planStore.ts"
      via: "usePlanStore for local state"
      pattern: "usePlanStore"
    - from: "app/(app)/(tabs)/plans.tsx"
      to: "src/features/plans/hooks/usePlans.ts"
      via: "usePlans hook for data"
      pattern: "usePlans"
---

<objective>
Set up the plan builder data foundation: Supabase schema (3 tables with RLS + active plan trigger), TypeScript types, Zustand planStore with MMKV persistence, usePlans CRUD hook, and the Plans tab with list screen and empty state.

Purpose: Establish the data layer and navigation entry point that Plan 02 (creation/detail) and Plan 03 (edit/delete) build upon.
Output: Working Plans tab showing empty state, schema deployed, store and hook ready for CRUD operations.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/stores/exerciseStore.ts
@src/features/exercises/types.ts
@src/lib/supabase/types/database.ts
@app/(app)/(tabs)/_layout.tsx
@src/features/exercises/components/EmptyState.tsx
@src/components/ui/Card.tsx

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/stores/exerciseStore.ts:
```typescript
// Follow this exact Zustand + MMKV pattern for planStore
export const useExerciseStore = create<ExerciseState & ExerciseActions>()(
  persist((set) => ({ ... }), {
    name: 'exercise-storage',
    storage: createJSONStorage(() => mmkvStorage),
  })
);
```

From src/lib/supabase/types/database.ts:
```typescript
export interface ExerciseRow {
  id: string;
  user_id: string | null;
  name: string;
  muscle_group: string;
  equipment: string;
  notes: string | null;
  created_at: string;
  updated_at: string;
}
// Extend Database interface with plan tables
```

From app/(app)/(tabs)/_layout.tsx:
```typescript
// Add Plans tab after Exercises tab
<Tabs.Screen name="exercises" options={{ title: 'Exercises', tabBarIcon: ... }} />
// Insert plans tab here
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema, types, planStore, usePlans hook, and Wave 0 test stubs</name>
  <files>
    supabase/migrations/20260311000000_create_plans.sql,
    src/features/plans/types.ts,
    src/features/plans/constants.ts,
    src/lib/supabase/types/database.ts,
    src/stores/planStore.ts,
    src/features/plans/hooks/usePlans.ts,
    tests/plans/plan-store.test.ts,
    tests/plans/plan-days.test.ts,
    tests/plans/plan-exercises.test.ts
  </files>
  <behavior>
    - planStore.addPlan: adds a plan to the store's plans array
    - planStore.updatePlan: updates fields on a specific plan by ID
    - planStore.removePlan: removes a plan from the array by ID
    - planStore.setActivePlan: sets one plan active and all others inactive
    - planStore starts with empty plans array and isLoading false
  </behavior>
  <action>
    1. Create `src/features/plans/types.ts` with interfaces from RESEARCH.md:
       - TargetSet: { weight: number, reps: number, rpe: number | null }
       - PlanDayExercise: { id, plan_day_id, exercise_id, sort_order, target_sets: TargetSet[], notes, unit_override: 'kg'|'lbs'|null, weight_progression: 'manual'|'carry_previous', created_at, exercise? }
       - PlanDay: { id, plan_id, day_name, weekday: number|null, sort_order, created_at, plan_day_exercises: PlanDayExercise[] }
       - Plan: { id, user_id, name, is_active, created_at, updated_at, plan_days: PlanDay[] }
       - PlanSummary: { id, user_id, name, is_active, created_at, updated_at, day_count, day_names }

    2. Create `src/features/plans/constants.ts`:
       - WEEKDAY_LABELS = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat']
       - DEFAULT_DAY_NAMES = ['Day A', 'Day B', 'Day C', 'Day D', 'Day E', 'Day F']
       - DEFAULT_TARGET_SET: TargetSet = { weight: 0, reps: 0, rpe: null }
       - DEFAULT_WEIGHT_PROGRESSION: 'manual' as const
       - WEIGHT_PROGRESSION_OPTIONS = [{ value: 'manual', label: 'Set Target', description: 'Set a specific weight goal each week' }, { value: 'carry_previous', label: 'Use Previous', description: 'Automatically use last session\'s weight' }]

    3. Create Supabase migration `supabase/migrations/20260311000000_create_plans.sql`:
       - workout_plans table (id UUID PK, user_id FK auth.users CASCADE, name TEXT NOT NULL, is_active BOOLEAN DEFAULT false, created_at, updated_at)
       - plan_days table (id UUID PK, plan_id FK workout_plans CASCADE, day_name TEXT NOT NULL, weekday SMALLINT nullable, sort_order SMALLINT DEFAULT 0, created_at)
       - plan_day_exercises table (id UUID PK, plan_day_id FK plan_days CASCADE, exercise_id FK exercises RESTRICT, sort_order SMALLINT DEFAULT 0, target_sets JSONB DEFAULT '[]', notes TEXT, unit_override TEXT, weight_progression TEXT NOT NULL DEFAULT 'manual' CHECK (weight_progression IN ('manual', 'carry_previous')), created_at)
       - RLS on all 3 tables: workout_plans checks auth.uid()=user_id; plan_days and plan_day_exercises check via JOIN to workout_plans.user_id
       - deactivate_other_plans() trigger on workout_plans: before UPDATE when NEW.is_active=true, deactivate all other plans for same user
       - updated_at trigger on workout_plans (reuse pattern from profiles migration)

    4. Update `src/lib/supabase/types/database.ts`:
       - Add WorkoutPlanRow, PlanDayRow, PlanDayExerciseRow interfaces
       - Add to Database.public.Tables: workout_plans, plan_days, plan_day_exercises

    5. Create `src/stores/planStore.ts` following exerciseStore pattern exactly:
       - Named MMKV instance: createMMKV({ id: 'plan-storage' })
       - State: plans: Plan[], isLoading, lastFetched
       - Actions: setPlans, addPlan, updatePlan, removePlan, setActivePlan, setLoading

    6. Create `src/features/plans/hooks/usePlans.ts`:
       - fetchPlans(): fetch from Supabase with `select('*, plan_days(day_name, weekday, sort_order)')`, update store
       - createPlan(name, days): insert workout_plan, then insert plan_days, return plan
       - deletePlan(id): delete from Supabase (cascade handles children), remove from store
       - setActivePlan(id): update is_active=true in Supabase (trigger handles deactivation), update store
       - Use `as any` pattern for Supabase .from() calls per project convention

    7. Write tests in `tests/plans/plan-store.test.ts`:
       - Test store CRUD operations (add, update, remove, setActive)
       - Mock Supabase for usePlans hook tests

    8. Create Wave 0 test stub `tests/plans/plan-days.test.ts`:
       - Import PlanDay, PlanDayExercise, TargetSet from types
       - Stub test: "plan day creation sets sort_order from array index" (placeholder assertion: expect(true).toBe(true))
       - Stub test: "plan day weekday mapping is optional" (placeholder assertion)
       - Comment: "// TODO: Plan 02 will flesh out these tests with real createPlan logic"

    9. Create Wave 0 test stub `tests/plans/plan-exercises.test.ts`:
       - Import PlanDayExercise, TargetSet from types
       - Stub test: "exercise target_sets default to empty array" (placeholder assertion: expect(true).toBe(true))
       - Stub test: "exercise sort_order reflects drag position" (placeholder assertion)
       - Comment: "// TODO: Plan 02/03 will flesh out these tests with real exercise assignment logic"
  </action>
  <verify>
    <automated>npx jest tests/plans/ --bail</automated>
  </verify>
  <done>
    - Migration file creates 3 tables with RLS and active plan trigger
    - TypeScript types exported for Plan, PlanDay, PlanDayExercise, TargetSet
    - planStore has CRUD actions with MMKV persistence
    - usePlans hook bridges Supabase and store
    - database.ts extended with plan table types
    - Tests pass for store operations
    - Wave 0 test stubs exist for plan-days and plan-exercises (used by Plans 02 and 03)
  </done>
</task>

<task type="auto">
  <name>Task 2: Plans tab, list screen, empty state, and plan card</name>
  <files>
    app/(app)/(tabs)/plans.tsx,
    app/(app)/(tabs)/_layout.tsx,
    src/features/plans/components/PlanEmptyState.tsx,
    src/features/plans/components/PlanCard.tsx
  </files>
  <action>
    1. Create `src/features/plans/components/PlanEmptyState.tsx`:
       - Match the exercise library empty state pattern (EmptyState.tsx)
       - Display clipboard/plan unicode icon, "Create your first workout plan" heading, descriptive subtitle
       - Prominent "Create Plan" Button that calls onCreatePress callback
       - Use StyleSheet.create, colors from @/constants/theme

    2. Create `src/features/plans/components/PlanCard.tsx`:
       - Reuse Card component from src/components/ui/Card.tsx as wrapper
       - Show: plan name (bold), active badge (accent-colored "Active" text if is_active), day count, comma-separated day names with weekday labels if mapped
       - onPress callback for navigation to plan detail
       - onLongPress callback for setting as active plan (per user decision)
       - Swipe-to-delete: use Swipeable from react-native-gesture-handler for swipe-left reveal of delete button (per user decision: "Delete available from swipe left on plan card in list")
       - NOTE: The swipe onDelete handler should accept an `onDelete` prop but treat it as a stub/no-op for now. Plan 03-03 will wire in the actual delete confirmation dialog. For now, onDelete can call console.warn('Delete confirmation wired in Plan 03-03') as a placeholder.
       - Use StyleSheet.create, dark theme colors

    3. Update `app/(app)/(tabs)/_layout.tsx`:
       - Add Plans tab after Exercises tab
       - Use clipboard unicode icon (e.g. '\u{1F4CB}' or simpler text like the existing tabs)
       - Title: "Plans"

    4. Create `app/(app)/(tabs)/plans.tsx`:
       - Use usePlans hook to fetch plans on mount
       - If plans empty + not loading: show PlanEmptyState
       - If plans exist: FlatList of PlanCard components
       - Floating action button (+ icon) in bottom-right for creating a new plan (navigates to plans/create route)
       - SafeAreaView wrapper, dark background
       - Navigation: router.push('/plans/create') for create, router.push(`/plans/${id}`) for detail
       - Header with "My Plans" title (inline, not using navigation header)
  </action>
  <verify>
    <automated>npx jest tests/plans/ --bail && npx expo export --platform web --output-dir /tmp/expo-export-test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Plans tab visible in bottom navigation bar between Exercises and after it
    - Empty state renders with Create Plan button when no plans exist
    - PlanCard shows plan name, active badge, day summary
    - PlanCard supports swipe-to-delete gesture (onDelete is a prop stub until Plan 03-03 wires confirmation)
    - Tab layout compiles without errors
  </done>
</task>

</tasks>

<verification>
- `npx jest tests/plans/ --bail` passes (plan-store, plan-days stubs, plan-exercises stubs)
- App compiles: `npx expo export --platform web 2>&1 | tail -3` shows success
- Plans tab appears in bottom nav
- Empty state renders on Plans tab (no plans yet)
</verification>

<success_criteria>
- Three-table Supabase migration with RLS and active plan trigger exists
- TypeScript types for Plan/PlanDay/PlanDayExercise/TargetSet are exported
- planStore with MMKV persistence follows exerciseStore pattern
- usePlans hook provides fetchPlans, createPlan, deletePlan, setActivePlan
- Plans tab in bottom navigation shows empty state
- PlanCard component ready for list rendering with swipe-to-delete
- Wave 0 test stubs exist for plan-days and plan-exercises
</success_criteria>

<output>
After completion, create `.planning/phases/03-plan-builder/03-01-SUMMARY.md`
</output>
