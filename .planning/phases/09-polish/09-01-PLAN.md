---
phase: 09-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constants/theme.ts
  - src/features/body-metrics/components/MeasurementForm.tsx
  - src/features/dashboard/components/TappableAvatar.tsx
  - src/features/workout/components/SetCard.tsx
  - src/features/workout/components/ActiveWorkoutBar.tsx
  - src/features/history/components/SessionExerciseCard.tsx
  - src/features/workout/components/WeightTargetPrompt.tsx
  - src/features/plans/components/PlanExerciseRow.tsx
  - src/features/plans/components/DaySlotEditor.tsx
  - src/features/plans/components/PlanDaySection.tsx
  - src/features/exercises/components/ExerciseBottomSheet.tsx
  - src/features/exercises/components/ExerciseFilterBar.tsx
  - src/features/plans/components/PlanCard.tsx
  - src/features/exercises/constants/muscleGroups.ts
  - src/features/auth/components/PRBaselineForm.tsx
  - src/components/layout/ConnectivityBanner.tsx
  - src/components/ui/Button.tsx
  - app/(app)/workout/index.tsx
  - app/(app)/plans/[id].tsx
  - app/(app)/workout/summary.tsx
  - app/(app)/(tabs)/plans.tsx
  - app/(app)/plans/create.tsx
  - app/(app)/(tabs)/exercises.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Accent color across all screens is magenta (#ec4899), not blue (#3b82f6)"
    - "No hardcoded hex color values exist in any component or screen file (only theme.ts)"
    - "Existing tests still pass after color centralization"
  artifacts:
    - path: "src/constants/theme.ts"
      provides: "Centralized color constants with magenta accent"
      contains: "accent: '#ec4899'"
    - path: "src/constants/theme.ts"
      provides: "White color constant replacing hardcoded #fff"
      contains: "white: '#ffffff'"
  key_links:
    - from: "all component files"
      to: "src/constants/theme.ts"
      via: "import { colors } from '@/constants/theme'"
      pattern: "colors\\.(accent|white|background|surface)"
---

<objective>
Swap the accent color from blue (#3b82f6) to magenta (#ec4899) and centralize all hardcoded color values into theme.ts across the entire codebase.

Purpose: The app should use a consistent, intentional color language -- no stray hex values, no blue remnants. Every color reference flows from theme.ts.
Output: Updated theme.ts with magenta accent + white constant, and 22 component/screen files using theme constants exclusively.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish/09-CONTEXT.md
@.planning/phases/09-polish/09-RESEARCH.md

@src/constants/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update theme.ts and swap accent color across all files</name>
  <files>src/constants/theme.ts, and all 22 files listed in files_modified above</files>
  <action>
1. Update src/constants/theme.ts:
   - Change accent from '#3b82f6' to '#ec4899' (magenta)
   - Change accentBright from '#60a5fa' to '#f472b6' (lighter magenta)
   - Add white: '#ffffff' (new constant for replacing hardcoded #fff/#ffffff)
   - Add any other hardcoded color values found during the audit as named constants (e.g., border: '#333333' if used)
   - Update the file comment to remove NativeWind/tailwind reference (project uses StyleSheet.create)

2. Systematically replace ALL hardcoded hex color values in each of the 22 files:
   - Replace #fff / #ffffff with colors.white
   - Replace #3b82f6 with colors.accent (if any direct references remain)
   - Replace #60a5fa with colors.accentBright
   - Replace #0a0a0a with colors.background
   - Replace #1a1a1a with colors.surface
   - Replace #252525 with colors.surfaceElevated
   - Replace #f5f5f5 with colors.textPrimary
   - Replace #a3a3a3 with colors.textSecondary
   - Replace #737373 with colors.textMuted
   - Replace #22c55e with colors.success
   - Replace #f59e0b with colors.warning
   - Replace #ef4444 with colors.error
   - For any other hardcoded hex values: add them to theme.ts as named constants first, then reference

3. For each file, ensure it imports from '@/constants/theme' if not already. Do NOT add imports to files that don't need them (e.g., if a file has no remaining hardcoded colors after checking).

4. Special cases:
   - muscleGroups.ts: May have color values for category badges -- centralize these as theme constants or keep as domain-specific if they are distinct per-group colors (use discretion)
   - Any rgba() values: leave in place but ensure the base color references a theme constant via a helper or comment

5. After all replacements, run the grep verification to confirm zero hardcoded colors remain outside theme.ts.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Desktop/Gym-App && grep -rn "#fff\b\|#ffffff\|#3b82f6\|#60a5fa\|#0a0a0a\|#1a1a1a\|#252525" src/ app/ --include="*.tsx" --include="*.ts" | grep -v "theme.ts" | grep -v "node_modules" | grep -v ".test." | wc -l</automated>
  </verify>
  <done>Zero hardcoded hex values remain in any component or screen file (only theme.ts contains raw hex). Accent color is magenta (#ec4899) everywhere.</done>
</task>

<task type="auto">
  <name>Task 2: Verify regression -- all existing tests pass</name>
  <files>No files modified -- verification only</files>
  <action>
Run the full test suite to confirm the color centralization changes did not break any existing tests. If any test fails due to color assertions (e.g., snapshot tests comparing hex values), update those test expectations to use the new magenta accent color.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Desktop/Gym-App && npx jest --bail 2>&1 | tail -20</automated>
  </verify>
  <done>All existing tests pass with zero failures.</done>
</task>

</tasks>

<verification>
- grep for hardcoded colors returns 0 matches outside theme.ts
- theme.ts contains accent: '#ec4899' and accentBright: '#f472b6'
- npx jest --bail passes with all tests green
</verification>

<success_criteria>
Every color in the app flows from theme.ts. The accent is magenta (#ec4899). No blue (#3b82f6) remnants. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-01-SUMMARY.md`
</output>
