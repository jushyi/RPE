/**
 * Workout feature constants.
 */

/** Horizontal swipe distance (px) to trigger set deletion */
export const SWIPE_THRESHOLD = -120;

/** Duration (ms) for swipe-to-delete animation */
export const SWIPE_ANIMATION_DURATION = 200;

/** Duration (ms) for PR celebration animation */
export const PR_CELEBRATION_DURATION = 2000;

/** Maximum allowed weight value */
export const MAX_WEIGHT = 9999.99;

/** Maximum allowed reps value */
export const MAX_REPS = 999;
