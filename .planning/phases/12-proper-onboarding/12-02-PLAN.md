---
phase: 12-proper-onboarding
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/features/onboarding/components/BodyStatsStep.tsx
  - src/features/onboarding/components/FirstPlanPromptStep.tsx
  - src/features/onboarding/components/OnboardingPager.tsx
autonomous: false
requirements: [OB-03, OB-04, OB-06]

must_haves:
  truths:
    - "User can enter bodyweight and body measurements on Step 3 with units defaulting to Step 1 choice"
    - "Step 3 is fully skippable and individual fields can be left empty"
    - "Step 4 explains what plans are and offers a Create Your First Plan button"
    - "Skipping Step 4 completes onboarding and lands on dashboard"
    - "Tapping Create Your First Plan navigates to plan builder and completes onboarding"
    - "Full 4-step flow works end-to-end: Units > PRs > Body Stats > Plan Prompt > Dashboard"
  artifacts:
    - path: "src/features/onboarding/components/BodyStatsStep.tsx"
      provides: "Step 3: bodyweight and measurement entry"
      min_lines: 60
    - path: "src/features/onboarding/components/FirstPlanPromptStep.tsx"
      provides: "Step 4: plan explanation and CTA"
      min_lines: 40
  key_links:
    - from: "src/features/onboarding/components/BodyStatsStep.tsx"
      to: "bodyweightStore/bodyMeasurementStore"
      via: "addEntry and addMeasurement hooks"
      pattern: "addEntry|addMeasurement"
    - from: "src/features/onboarding/components/FirstPlanPromptStep.tsx"
      to: "/(app)/plans"
      via: "router.push for plan creation"
      pattern: "plans/create|plans.*create"
    - from: "src/features/onboarding/components/OnboardingPager.tsx"
      to: "authStore"
      via: "setOnboardingComplete before plan builder navigation"
      pattern: "setOnboardingComplete"
---

<objective>
Build the remaining two onboarding steps (Body Stats Baseline and First Plan Prompt) and wire the complete 4-step flow end-to-end. Human verification of the full onboarding experience.

Purpose: Complete the onboarding flow so new users can optionally enter body baselines and get guided toward creating their first plan.
Output: Fully functional 4-step onboarding: Units > PRs > Body Stats > Plan Prompt > Dashboard.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-proper-onboarding/12-CONTEXT.md
@.planning/phases/12-proper-onboarding/12-RESEARCH.md
@.planning/phases/12-proper-onboarding/12-01-SUMMARY.md

@src/stores/authStore.ts
@src/features/onboarding/components/OnboardingPager.tsx
@src/features/body-metrics/components/MeasurementForm.tsx
@src/features/body-metrics/hooks/useBodyMeasurements.ts
@src/features/progress/hooks/useBodyweightData.ts

<interfaces>
<!-- Key types from Plan 01 output and existing stores -->

From src/stores/authStore.ts:
```typescript
setOnboardingComplete: () => void;
setPreferredUnit: (unit: 'kg' | 'lbs') => void;
setPreferredMeasurementUnit: (unit: 'in' | 'cm') => void;
preferredUnit: 'kg' | 'lbs';
preferredMeasurementUnit: 'in' | 'cm';
```

From src/features/progress/hooks/useBodyweightData.ts:
```typescript
addEntry(weight: number, unit: 'kg' | 'lbs', loggedAt?: string): void;
```

From src/features/body-metrics/hooks/useBodyMeasurements.ts:
```typescript
addMeasurement(data: { chest?: number; waist?: number; biceps?: number; quad?: number; body_fat_pct?: number; ... unit fields }): void;
```

OnboardingPager step props pattern (from Plan 01):
```typescript
// Each step receives:
onNext: () => void;   // advance to next page
onSkip: () => void;   // skip this step (advance or complete)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BodyStatsStep and FirstPlanPromptStep, wire into OnboardingPager</name>
  <files>
    src/features/onboarding/components/BodyStatsStep.tsx,
    src/features/onboarding/components/FirstPlanPromptStep.tsx,
    src/features/onboarding/components/OnboardingPager.tsx
  </files>
  <action>
**BodyStatsStep.tsx:** Step 3 screen. Collects bodyweight + 4 circumference measurements (chest, waist, biceps, quad). Read `preferredUnit` and `preferredMeasurementUnit` from authStore for default units. Layout: Title "Body Stats Baseline", subtitle "Track your starting point (optional)". Bodyweight field with kg/lbs unit toggle. Four circumference fields (chest, waist, biceps, quad) each with in/cm unit toggle. All fields optional -- any can be left empty. On Next: save only filled fields. Use `useBodyweightData().addEntry()` for bodyweight (only if value entered). Use `useBodyMeasurements().addMeasurement()` for circumference fields (only include fields with values). On Skip: advance without saving anything. Use Ionicons body-outline icon at top. Numeric keyboard for all inputs. StyleSheet.create, dark theme, no emojis. ScrollView wrapper for the form content in case it exceeds screen height.

**FirstPlanPromptStep.tsx:** Step 4 (final step). Title: "Create Your First Plan". Body text explaining what workout plans are: plans let you organize exercises by day of the week, set targets, and track progress against your plan. Mention that the app will remind you on planned training days. Use Ionicons clipboard-outline icon (matches Plans tab). Two buttons: "Create Your First Plan" (accent colored, primary) and "Skip for Now" (text button, secondary). On "Create Your First Plan": call `onCreatePlan()` prop which should: (1) call `setOnboardingComplete()` from authStore FIRST (per research pitfall #2 -- mark complete BEFORE navigating to plan builder), (2) then `router.push('/(app)/plans/create' as any)`. On "Skip for Now": call `onComplete()` prop which completes onboarding to dashboard. No emojis in any text.

**OnboardingPager.tsx update:** Replace placeholder pages 3 and 4 (indices 2 and 3) with BodyStatsStep and FirstPlanPromptStep components. For FirstPlanPromptStep: pass `onCreatePlan` callback that calls `setOnboardingComplete()` then navigates to plan creation, and `onComplete` that calls `setOnboardingComplete()` then replaces to dashboard. The bottom navigation buttons for Step 3 (index 2) should show "Skip" and "Next". For Step 3 (index 3 -- the final step), hide the bottom navigation bar entirely since FirstPlanPromptStep has its own buttons.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/Gym-App && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>BodyStatsStep shows bodyweight + 4 measurement fields with correct default units and saves via existing hooks. FirstPlanPromptStep explains plans and offers Create/Skip buttons. Full 4-step PagerView wired end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete onboarding flow</name>
  <files>none</files>
  <action>
Human verifies the complete 4-step onboarding flow works end-to-end.
  </action>
  <what-built>Complete 4-step onboarding flow: Units > PR Baselines > Body Stats > First Plan Prompt. PagerView with swipe navigation, step dots, per-step Skip/Next buttons. Route guard updated for new users.</what-built>
  <how-to-verify>
    1. Reset onboarding state: In Expo Go, clear app data or sign out and create a new account
    2. After sign-up + email confirmation, you should land on the NEW onboarding flow (not old PR-only screen)
    3. Step 1 (Units): Verify two toggle rows (kg/lbs, in/cm). Verify no Skip button. Tap Next.
    4. Step 2 (PRs): Verify Big 3 lifts shown with unit defaulting to Step 1 choice. Enter a value or skip. Tap Next.
    5. Step 3 (Body Stats): Verify bodyweight + 4 measurement fields. Verify units default correctly. Leave some empty, fill some. Tap Next.
    6. Step 4 (Plan Prompt): Verify explanation text and two buttons. Tap "Skip for Now" to complete.
    7. Verify you land on dashboard.
    8. Verify step dots indicator updates as you progress through steps.
    9. Verify swipe-back works on Steps 2-4.
    10. Test: Sign out, sign back in with existing account -- should go straight to dashboard (not onboarding).
    11. Test: From dashboard, tap PR card to edit PRs -- should still open old edit form (not full onboarding).
  </how-to-verify>
  <verify>Human approves onboarding flow</verify>
  <done>All 11 verification steps pass. Onboarding flow approved.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Full 4-step onboarding flow navigable via swipe and buttons
- Body stats save correctly via existing hooks
- Plan prompt navigates to plan builder or skips to dashboard
- Onboarding completes and sets hasCompletedOnboarding: true
- Existing users bypass the flow
- PR edit mode from dashboard still works
</verification>

<success_criteria>
- BodyStatsStep saves filled fields via bodyweightStore and bodyMeasurementStore
- FirstPlanPromptStep marks onboarding complete BEFORE navigating to plan builder
- Full flow: Units > PRs > Body Stats > Plan Prompt > Dashboard works end-to-end
- Human verification approves the onboarding experience
</success_criteria>

<output>
After completion, create `.planning/phases/12-proper-onboarding/12-02-SUMMARY.md`
</output>
