---
phase: 13-coaching-options
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/coaching/inviteCode.test.ts
  - tests/coaching/useCoaching.test.ts
  - tests/coaching/coachPlans.test.ts
  - tests/notifications/pushToken.test.ts
autonomous: true
requirements: [COACH-01, COACH-02, COACH-03, COACH-05]

must_haves:
  truths:
    - "Test stubs exist for all coaching logic modules before implementation begins"
    - "Jest can discover and run coaching test files without errors"
  artifacts:
    - path: "tests/coaching/inviteCode.test.ts"
      provides: "Test stubs for invite code generation and validation"
      contains: "generateInviteCode"
    - path: "tests/coaching/useCoaching.test.ts"
      provides: "Test stubs for coaching relationship CRUD"
      contains: "useCoaching"
    - path: "tests/coaching/coachPlans.test.ts"
      provides: "Test stubs for coach plan CRUD targeting trainees"
      contains: "useCoachPlans"
    - path: "tests/notifications/pushToken.test.ts"
      provides: "Test stubs for push token registration"
      contains: "registerPushToken"
  key_links: []
---

<objective>
Create test stub files for all coaching logic modules (Wave 0 Nyquist requirement). These stubs define behavioral expectations before implementation begins and ensure every subsequent plan task has an automated verify command.

Purpose: Satisfy Nyquist compliance -- every task has a test file to run against.
Output: 4 test stub files with pending/skipped tests ready for implementation to fill in.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-CONTEXT.md
@.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-RESEARCH.md

<interfaces>
From jest.config.js:
```javascript
// Tests live in tests/ directory (testMatch: ['**/tests/**/*.test.ts'])
// Module alias: '@/' maps to '<rootDir>/src/'
// Uses jest-expo preset
// Setup file: tests/setup.ts
```

Existing test pattern (from tests/alarms/alarmScheduler.test.ts):
```typescript
import { scheduleAlarmForDay, ... } from '@/features/alarms/utils/alarmScheduler';
describe('alarmScheduler', () => {
  it('should ...', () => { ... });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coaching and notification test stubs</name>
  <files>tests/coaching/inviteCode.test.ts, tests/coaching/useCoaching.test.ts, tests/coaching/coachPlans.test.ts, tests/notifications/pushToken.test.ts</files>
  <action>
Create 4 test stub files following the existing project test pattern (see tests/alarms/ for reference). Each file should import from the expected module path and contain describe blocks with `it.todo()` calls for each expected behavior.

1. **tests/coaching/inviteCode.test.ts**:
   ```typescript
   import { generateInviteCode, INVITE_CODE_EXPIRY_HOURS } from '@/features/coaching/utils/inviteCode';
   describe('inviteCode', () => {
     it.todo('generates a 6-character alphanumeric code');
     it.todo('uses only unambiguous characters (no I/O/0/1)');
     it.todo('generates unique codes on successive calls');
     it.todo('exports INVITE_CODE_EXPIRY_HOURS as 24');
   });
   ```

2. **tests/coaching/useCoaching.test.ts**:
   ```typescript
   // Stub for coaching relationship CRUD
   describe('useCoaching', () => {
     describe('generateCode', () => {
       it.todo('inserts invite code with 24h expiry');
       it.todo('retries on code collision');
     });
     describe('redeemCode', () => {
       it.todo('creates coaching relationship on valid code');
       it.todo('throws on expired code');
       it.todo('throws on already-redeemed code');
     });
     describe('disconnect', () => {
       it.todo('removes coaching relationship');
     });
     describe('fetchRelationships', () => {
       it.todo('separates trainees and coaches for current user');
     });
   });
   ```

3. **tests/coaching/coachPlans.test.ts**:
   ```typescript
   // Stub for coach plan CRUD targeting trainees
   describe('useCoachPlans', () => {
     describe('createPlanForTrainee', () => {
       it.todo('inserts plan with trainee user_id and coach coach_id');
       it.todo('does not schedule alarms for trainee');
     });
     describe('updateTraineePlan', () => {
       it.todo('updates only coach-owned plans');
       it.todo('inserts coach note when provided');
     });
     describe('fetchTraineePlans', () => {
       it.todo('returns both personal and coach-created plans');
     });
   });
   ```

4. **tests/notifications/pushToken.test.ts**:
   ```typescript
   // Stub for push token registration
   describe('registerPushToken', () => {
     it.todo('returns null on non-device (emulator)');
     it.todo('requests notification permissions');
     it.todo('upserts token to push_tokens table');
     it.todo('returns null and warns on failure (non-blocking)');
   });
   ```

NOTE: Use `it.todo()` (not `it.skip()`) so they show as pending without false passes. Do NOT include actual imports at the top yet if the source files do not exist -- comment out the imports with a `// TODO: uncomment when implemented` note. This prevents TypeScript/Jest import errors on the stubs before Plan 01 creates the source files.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/Gym-App && npx jest --testPathPattern coaching --passWithNoTests 2>&1 | tail -10 && npx jest --testPathPattern notifications/pushToken --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>4 test stub files exist in tests/coaching/ and tests/notifications/. Jest discovers them without errors. All tests show as todo/pending.</done>
</task>

</tasks>

<verification>
- All 4 test files exist at correct paths
- `npx jest --testPathPattern coaching --passWithNoTests` runs without errors
- `npx jest --testPathPattern notifications/pushToken --passWithNoTests` runs without errors
- Test stubs use it.todo() so they appear as pending
</verification>

<success_criteria>
- tests/coaching/inviteCode.test.ts exists with 4+ todo tests
- tests/coaching/useCoaching.test.ts exists with 7+ todo tests
- tests/coaching/coachPlans.test.ts exists with 5+ todo tests
- tests/notifications/pushToken.test.ts exists with 4+ todo tests
- Jest can run all coaching test files without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-coaching-options-create-plans-for-others-get-notifs-when-they-finish-and-update-their-plans-for-the-next-week/13-00-SUMMARY.md`
</output>
