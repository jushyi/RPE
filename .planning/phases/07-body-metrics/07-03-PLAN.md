---
phase: 07-body-metrics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260315000002_replace_hips_with_biceps_quad.sql
  - src/features/body-metrics/types.ts
  - src/features/body-metrics/utils/validation.ts
  - src/features/body-metrics/hooks/useBodyMetricsChartData.ts
  - src/features/body-metrics/hooks/useBodyMeasurements.ts
  - src/features/body-metrics/components/MeasurementForm.tsx
  - src/features/body-metrics/components/MeasurementHistoryItem.tsx
  - src/features/body-metrics/components/BodyCard.tsx
  - src/features/settings/utils/csvExport.ts
  - src/stores/bodyMeasurementStore.ts
  - app/(app)/body-metrics.tsx
  - tests/body-metrics/form-validation.test.ts
autonomous: true
gap_closure: true
requirements: [HIST-04]

must_haves:
  truths:
    - "Form shows bodyweight, chest, waist, biceps, quad, body fat fields (no hips)"
    - "Charts display biceps and quad trends (no hips chart)"
    - "History items show biceps and quad chips (no hips chip)"
    - "Dashboard BodyCard shows biceps and quad rows (no hips row)"
  artifacts:
    - path: "supabase/migrations/20260315000002_replace_hips_with_biceps_quad.sql"
      provides: "DB schema change: drop hips columns, add biceps+quad columns"
    - path: "src/features/body-metrics/types.ts"
      provides: "CircumferenceMetric = chest | waist | biceps | quad"
      contains: "biceps"
    - path: "src/features/body-metrics/components/MeasurementForm.tsx"
      provides: "Biceps and Quad field rows, no Hips field"
  key_links:
    - from: "src/features/body-metrics/types.ts"
      to: "all body-metrics files"
      via: "CircumferenceMetric union type"
      pattern: "biceps.*quad"
---

<objective>
Replace hips measurement field with biceps and quad across the entire body metrics vertical slice.

Purpose: User reported "we don't need hips just waist, replace with biceps. add quad as well" -- the field set is wrong in every layer from DB to UI.
Output: All 10+ files updated to use biceps/quad instead of hips, with new DB migration.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-body-metrics/07-01-SUMMARY.md
@.planning/phases/07-body-metrics/07-02-SUMMARY.md
@.planning/phases/07-body-metrics/07-UAT.md

<interfaces>
<!-- Current types that must change -->
From src/features/body-metrics/types.ts:
```typescript
export interface BodyMeasurement {
  id: string;
  user_id: string;
  chest: number | null;
  chest_unit: 'in' | 'cm' | null;
  waist: number | null;
  waist_unit: 'in' | 'cm' | null;
  hips: number | null;          // REMOVE
  hips_unit: 'in' | 'cm' | null; // REMOVE
  body_fat_pct: number | null;
  measured_at: string;
  created_at: string;
}
export type CircumferenceMetric = 'chest' | 'waist' | 'hips'; // CHANGE to 'chest' | 'waist' | 'biceps' | 'quad'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hips with biceps/quad in data layer (migration, types, store, hooks, validation, CSV)</name>
  <files>
    supabase/migrations/20260315000002_replace_hips_with_biceps_quad.sql,
    src/features/body-metrics/types.ts,
    src/features/body-metrics/utils/validation.ts,
    src/features/body-metrics/hooks/useBodyMetricsChartData.ts,
    src/features/body-metrics/hooks/useBodyMeasurements.ts,
    src/stores/bodyMeasurementStore.ts,
    src/features/settings/utils/csvExport.ts,
    tests/body-metrics/form-validation.test.ts
  </files>
  <action>
    1. **New migration** `20260315000002_replace_hips_with_biceps_quad.sql`:
       - ALTER TABLE body_measurements DROP COLUMN hips, DROP COLUMN hips_unit
       - ALTER TABLE body_measurements ADD COLUMN biceps NUMERIC(5,1), ADD COLUMN biceps_unit TEXT CHECK (biceps_unit IN ('in','cm'))
       - ALTER TABLE body_measurements ADD COLUMN quad NUMERIC(5,1), ADD COLUMN quad_unit TEXT CHECK (quad_unit IN ('in','cm'))
       - DROP existing at_least_one_measurement constraint, re-create it including biceps and quad (chest IS NOT NULL OR waist IS NOT NULL OR biceps IS NOT NULL OR quad IS NOT NULL OR body_fat_pct IS NOT NULL)
       - DROP hips_requires_unit constraint, add biceps_requires_unit and quad_requires_unit constraints
       - Note: This is a breaking migration. Existing hips data will be dropped. That is acceptable per user decision.

    2. **types.ts**: Replace hips/hips_unit with biceps/biceps_unit/quad/quad_unit in BodyMeasurement interface. Update CircumferenceMetric to `'chest' | 'waist' | 'biceps' | 'quad'`.

    3. **validation.ts**: Remove hips validation, add biceps and quad validation (value requires unit). Update the at-least-one-field check to include biceps and quad. Update MeasurementInput interface.

    4. **useBodyMetricsChartData.ts**: Update CIRCUMFERENCE_METRICS array to `['chest', 'waist', 'biceps', 'quad']` (remove 'hips').

    5. **useBodyMeasurements.ts**: In fetchMeasurements mapper and addMeasurement insert, replace all hips/hips_unit references with biceps/biceps_unit and quad/quad_unit.

    6. **bodyMeasurementStore.ts**: Bump persist version to 2 (the BodyMeasurement shape changed). The store itself is generic over BodyMeasurement so only the version bump is needed.

    7. **csvExport.ts** `generateBodyMetricsCSV`: Replace 'Hips' and 'Hips Unit' headers with 'Biceps', 'Biceps Unit', 'Quad', 'Quad Unit'. Update row mapping to use m.biceps, m.biceps_unit, m.quad, m.quad_unit.

    8. **form-validation.test.ts**: Remove hips test cases, add biceps and quad test cases (value without unit should fail, with unit should pass). Update "all fields explicitly null" test to include biceps and quad.
  </action>
  <verify>
    <automated>npx jest tests/body-metrics/form-validation.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>All data layer files reference biceps/quad instead of hips. Validation tests pass with new field set. CSV export includes Biceps/Quad columns.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hips with biceps/quad in UI layer (form, history item, BodyCard, body-metrics screen)</name>
  <files>
    src/features/body-metrics/components/MeasurementForm.tsx,
    src/features/body-metrics/components/MeasurementHistoryItem.tsx,
    src/features/body-metrics/components/BodyCard.tsx,
    app/(app)/body-metrics.tsx
  </files>
  <action>
    1. **MeasurementForm.tsx**:
       - Remove `hips` and `hipsUnit` state variables
       - Add `biceps`/`bicepsUnit` and `quad`/`quadUnit` state variables (same pattern as existing fields, default unit 'in')
       - Remove the Hips FieldRow, add Biceps and Quad FieldRows (between Waist and Body Fat)
       - Update the pre-fill useEffect (latestMeasurement) to use biceps_unit and quad_unit instead of hips_unit
       - Update the edit pre-fill useEffect to populate biceps/quad from editEntry
       - Update clearForm to reset biceps and quad
       - Update handleSave: parse biceps and quad values, include them in onSave call (biceps, biceps_unit, quad, quad_unit), remove hips/hips_unit
       - Update the "at least one field" validation check to include biceps and quad
       - Update the MeasurementFormProps onSave type signature: remove hips/hips_unit, add biceps/biceps_unit/quad/quad_unit

    2. **MeasurementHistoryItem.tsx**:
       - Remove the hips chip from the values array
       - Add biceps chip: if entry.biceps != null, push { label: 'Biceps', display: `${entry.biceps} ${entry.biceps_unit ?? ''}` }
       - Add quad chip: if entry.quad != null, push { label: 'Quad', display: `${entry.quad} ${entry.quad_unit ?? ''}` }

    3. **BodyCard.tsx**:
       - Remove the hips measurement row
       - Add biceps row: if latestMeasurement.biceps != null, render "Biceps" label with value and unit
       - Add quad row: if latestMeasurement.quad != null, render "Quad" label with value and unit

    4. **body-metrics.tsx** (main screen):
       - Remove `hipsData` chart data hook call, add `bicepsData` and `quadData` hook calls:
         `const bicepsData = useBodyMetricsChartData('biceps', circumferenceUnit);`
         `const quadData = useBodyMetricsChartData('quad', circumferenceUnit);`
       - Replace the Hips MeasurementChart with Biceps and Quad MeasurementCharts (same pattern as chest/waist, using circumferenceUnit toggle)
       - Update handleSave data type: remove hips/hips_unit, add biceps/biceps_unit/quad/quad_unit
       - Update the hasMeasurement check: replace data.hips with data.biceps or data.quad
       - Update measurementData object: replace hips/hips_unit with biceps/biceps_unit/quad/quad_unit
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Form shows Bodyweight, Chest, Waist, Biceps, Quad, Body Fat fields (no Hips). Charts tab shows Biceps and Quad trend charts. History items display biceps and quad chips. BodyCard shows biceps and quad rows. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npx jest tests/body-metrics/ --no-coverage` -- all body metrics tests pass
- `npx tsc --noEmit` -- no type errors
- Grep for "hips" in src/features/body-metrics/ and app/(app)/body-metrics.tsx returns zero matches
</verification>

<success_criteria>
- Zero references to "hips" in body-metrics feature code (migration file may reference it for DROP)
- CircumferenceMetric type is 'chest' | 'waist' | 'biceps' | 'quad'
- Form has 6 fields: bodyweight, chest, waist, biceps, quad, body fat
- All tests pass, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-body-metrics/07-03-SUMMARY.md`
</output>
