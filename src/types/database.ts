/**
 * Re-export database types from Supabase types directory.
 * This provides a convenient import path: `import type { Profile } from '@/types/database'`
 */
export type { Profile, PRBaseline, Database } from '@/lib/supabase/types/database';
