---
phase: 05-workout-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260313000000_add_estimated_1rm_to_set_logs.sql
  - src/features/history/types.ts
  - src/features/history/utils/epley.ts
  - src/features/history/utils/volumeCalc.ts
  - src/features/history/hooks/useHistory.ts
  - src/features/history/hooks/useSessionDetail.ts
  - src/stores/historyStore.ts
  - tests/history/epley.test.ts
  - tests/history/history-list.test.ts
  - tests/history/history-store.test.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-06

must_haves:
  truths:
    - "Epley formula correctly calculates estimated 1RM for normal sets, 1-rep sets, and edge cases"
    - "Volume calculation sums weight * reps across all sets in a session"
    - "History store caches session list with MMKV persistence"
    - "useHistory hook fetches sessions with nested exercises from Supabase"
    - "useSessionDetail hook fetches a single session with full set data"
    - "estimated_1rm column exists on set_logs with backfill migration"
  artifacts:
    - path: "src/features/history/utils/epley.ts"
      provides: "Epley 1RM calculation and best session 1RM"
      exports: ["calculateEpley1RM", "bestSessionE1RM"]
    - path: "src/features/history/utils/volumeCalc.ts"
      provides: "Volume and duration calculation helpers"
      exports: ["calculateTotalVolume", "calculateDurationMinutes"]
    - path: "src/features/history/types.ts"
      provides: "HistorySession, HistoryExercise, HistorySetLog, SessionListItem, ExerciseDelta types"
      exports: ["HistorySession", "HistoryExercise", "HistorySetLog", "SessionListItem", "ExerciseDelta"]
    - path: "src/stores/historyStore.ts"
      provides: "Zustand + MMKV history session cache"
      exports: ["useHistoryStore"]
    - path: "src/features/history/hooks/useHistory.ts"
      provides: "Session list fetching, delete session, pagination"
      exports: ["useHistory"]
    - path: "src/features/history/hooks/useSessionDetail.ts"
      provides: "Single session detail fetch with delta comparison"
      exports: ["useSessionDetail"]
    - path: "supabase/migrations/20260313000000_add_estimated_1rm_to_set_logs.sql"
      provides: "estimated_1rm column on set_logs with backfill and index"
      contains: "ALTER TABLE public.set_logs"
    - path: "tests/history/epley.test.ts"
      provides: "Tests for Epley formula edge cases"
    - path: "tests/history/history-list.test.ts"
      provides: "Tests for volume calc, exercise name truncation, duration"
    - path: "tests/history/history-store.test.ts"
      provides: "Tests for store CRUD operations"
  key_links:
    - from: "src/stores/historyStore.ts"
      to: "src/features/history/types.ts"
      via: "imports HistorySession type"
      pattern: "import.*from.*history/types"
    - from: "src/features/history/hooks/useHistory.ts"
      to: "src/stores/historyStore.ts"
      via: "reads/writes session cache"
      pattern: "useHistoryStore"
    - from: "src/features/history/hooks/useHistory.ts"
      to: "src/lib/supabase/client.ts"
      via: "Supabase nested select for sessions"
      pattern: "supabase.*from.*workout_sessions"
---

<objective>
Build the data layer for workout history: database migration, TypeScript types, utility functions (Epley 1RM, volume calculation), Zustand store with MMKV persistence, and data-fetching hooks.

Purpose: Establishes all data contracts and business logic that the UI layer (Plan 02) will consume. Separating data from UI keeps each plan focused and within context budget.
Output: Types, store, hooks, utils, migration, and unit tests -- everything needed for Plan 02 to build screens without writing data logic.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workout-history/05-CONTEXT.md
@.planning/phases/05-workout-history/05-RESEARCH.md

@src/stores/planStore.ts
@src/features/plans/hooks/usePlans.ts
@src/lib/supabase/client.ts
@src/constants/theme.ts

<interfaces>
<!-- Phase 4 database schema (from 04-01-PLAN.md) that Phase 5 depends on -->

Tables created by Phase 4:
- workout_sessions (id, user_id, plan_id, plan_day_id, started_at, ended_at, created_at)
- session_exercises (id, session_id, exercise_id, sort_order)
- set_logs (id, session_exercise_id, set_number, weight, reps, unit, is_pr, logged_at)

From src/stores/planStore.ts (pattern to follow for historyStore):
```typescript
// Zustand + MMKV persist pattern
import { create } from 'zustand';
import { persist, createJSONStorage } from 'zustand/middleware';
import { createMMKV } from 'react-native-mmkv';
```

From src/features/plans/hooks/usePlans.ts (Supabase nested select pattern):
```typescript
// as-any pattern for Supabase .from() calls
const { data, error } = await (supabase.from('table') as any)
  .select(`*, nested_table(*)`)
  .order('column', { ascending: false });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, utils, and unit tests</name>
  <files>src/features/history/types.ts, src/features/history/utils/epley.ts, src/features/history/utils/volumeCalc.ts, tests/history/epley.test.ts, tests/history/history-list.test.ts</files>
  <behavior>
    - calculateEpley1RM(100, 5) returns 116.7 (rounded to 1 decimal)
    - calculateEpley1RM(100, 1) returns 100 (1-rep = actual weight)
    - calculateEpley1RM(0, 5) returns 0 (zero weight guard)
    - calculateEpley1RM(100, 0) returns 0 (zero reps guard)
    - calculateEpley1RM(100, -1) returns 0 (negative reps guard)
    - bestSessionE1RM([{weight:100,reps:5},{weight:80,reps:10}]) returns max of the two
    - calculateTotalVolume with nested exercises/sets returns sum of weight*reps
    - calculateDurationMinutes returns minutes between start and end timestamps
    - calculateDurationMinutes with null endedAt returns null
  </behavior>
  <action>
    Create the history feature directory structure. Write types.ts with HistorySetLog, HistoryExercise, HistorySession, SessionListItem, and ExerciseDelta interfaces exactly as defined in RESEARCH.md TypeScript Interfaces section.

    Create epley.ts with calculateEpley1RM (Epley formula: weight * (1 + reps/30), returns weight for 1-rep, returns 0 for invalid inputs, rounds to 1 decimal) and bestSessionE1RM (max across sets).

    Create volumeCalc.ts with calculateTotalVolume (sum of weight*reps across all exercises/sets) and calculateDurationMinutes (difference in minutes, null if endedAt is null).

    Write tests first (RED), then implement (GREEN). Tests go in tests/history/.
  </action>
  <verify>
    <automated>npx jest tests/history/epley.test.ts tests/history/history-list.test.ts --bail</automated>
  </verify>
  <done>All Epley edge cases pass. Volume and duration calculations pass. Types export correctly with no TypeScript errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migration, store, and data hooks</name>
  <files>supabase/migrations/20260313000000_add_estimated_1rm_to_set_logs.sql, src/stores/historyStore.ts, src/features/history/hooks/useHistory.ts, src/features/history/hooks/useSessionDetail.ts, tests/history/history-store.test.ts</files>
  <behavior>
    - historyStore.setSessions sets sessions array and lastFetched timestamp
    - historyStore.removeSession filters out session by id
    - historyStore.setLoading toggles isLoading flag
    - Store persists to MMKV with key 'history-storage'
  </behavior>
  <action>
    Create Supabase migration to add estimated_1rm column to set_logs table:
    - ALTER TABLE public.set_logs ADD COLUMN estimated_1rm NUMERIC(6,2)
    - Backfill existing rows with Epley formula (CASE WHEN reps=1 THEN weight, WHEN reps>0 THEN ROUND(weight*(1+reps::numeric/30),2), ELSE NULL END)
    - CREATE INDEX idx_set_logs_exercise_1rm ON public.set_logs(session_exercise_id, estimated_1rm DESC) for Phase 6 chart queries

    Create historyStore.ts following the planStore.ts Zustand + MMKV persist pattern:
    - State: sessions (HistorySession[]), isLoading, lastFetched
    - Actions: setSessions, removeSession, setLoading
    - MMKV instance id: 'history-storage'

    Create useHistory.ts hook:
    - fetchSessions(): Supabase nested select on workout_sessions with session_exercises(id, exercise_id, sort_order, exercises(name, muscle_groups)) ordered by ended_at DESC, limit 30. Use as-any pattern. Updates historyStore.
    - deleteSession(id): Supabase delete from workout_sessions (cascade deletes children via FK), then removeSession from store.
    - toListItem(session): Derives SessionListItem from HistorySession (exercise names with +N more truncation, total volume, PR count, duration, plan name).
    - Paginate with offset parameter for infinite scroll support.

    Create useSessionDetail.ts hook:
    - fetchSession(id): Full nested select including set_logs(*) and exercises(name, muscle_groups, equipment, track_prs).
    - fetchPreviousSession(planDayId, currentSessionId, sessionDate): Finds previous session for same plan_day_id (returns null for freestyle sessions where planDayId is null).
    - calculateDeltas(currentExercises, previousExercises): Computes ExerciseDelta[] comparing max weight and total reps per exercise.
    - deleteSet(setId, sessionExerciseId): Deletes individual set from set_logs.

    Write historyStore tests first (RED), then implement store (GREEN).
  </action>
  <verify>
    <automated>npx jest tests/history/ --bail</automated>
  </verify>
  <done>Migration SQL file exists. historyStore persists sessions via MMKV. useHistory fetches and caches session data. useSessionDetail fetches full detail with delta comparison. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx jest tests/history/ --bail` -- all history tests pass
- `npx tsc --noEmit` -- no TypeScript errors in new files
- Migration SQL is syntactically valid
- Types, store, hooks, and utils all export correctly
</verification>

<success_criteria>
- Epley 1RM formula handles all edge cases (0 reps, 1 rep, negative, normal)
- Volume calculation aggregates correctly across nested exercise/set structures
- historyStore follows exact same Zustand + MMKV pattern as planStore
- useHistory fetches session list with nested exercise names via single Supabase query
- useSessionDetail fetches full session with sets and computes delta comparison
- Database migration adds estimated_1rm column with backfill and index
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-workout-history/05-01-SUMMARY.md`
</output>
