---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/_layout.tsx
  - app/index.tsx
  - app/(auth)/_layout.tsx
  - app/(auth)/login.tsx
  - app/(app)/_layout.tsx
  - app/(app)/(tabs)/dashboard.tsx
  - app/(app)/onboarding/pr-baseline.tsx
  - src/lib/supabase/client.ts
  - src/lib/supabase/types/database.ts
  - src/stores/authStore.ts
  - src/constants/theme.ts
  - src/types/database.ts
  - supabase/migrations/20260309000000_create_profiles.sql
  - supabase/migrations/20260309000001_create_pr_baselines.sql
  - tailwind.config.js
  - metro.config.js
  - global.css
  - .env.example
  - nativewind-env.d.ts
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05

must_haves:
  truths:
    - "Expo project compiles and runs on iOS/Android simulator with no errors"
    - "NativeWind dark theme classes apply correctly (dark backgrounds, light text)"
    - "Supabase client initializes without errors and connects to remote project"
    - "Zustand store persists data to MMKV across app restarts"
    - "Database migrations define profiles and pr_baselines tables with RLS enabled"
  artifacts:
    - path: "src/lib/supabase/client.ts"
      provides: "Supabase singleton with expo-sqlite/localStorage adapter and AppState listener"
      exports: ["supabase"]
    - path: "src/stores/authStore.ts"
      provides: "Zustand store with MMKV persistence for auth state and unit preference"
      exports: ["useAuthStore"]
    - path: "supabase/migrations/20260309000000_create_profiles.sql"
      provides: "Profiles table with RLS policies and auto-create trigger"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "supabase/migrations/20260309000001_create_pr_baselines.sql"
      provides: "PR baselines table with RLS policies"
      contains: "ENABLE ROW LEVEL SECURITY"
    - path: "tailwind.config.js"
      provides: "NativeWind v4 config with dark/bold color palette"
      contains: "nativewind/preset"
  key_links:
    - from: "src/lib/supabase/client.ts"
      to: ".env"
      via: "EXPO_PUBLIC_SUPABASE_URL and EXPO_PUBLIC_SUPABASE_ANON_KEY"
      pattern: "process\\.env\\.EXPO_PUBLIC_SUPABASE"
    - from: "src/stores/authStore.ts"
      to: "react-native-mmkv"
      via: "Zustand persist middleware with MMKV storage adapter"
      pattern: "createJSONStorage.*mmkvStorage"
---

<objective>
Scaffold the Expo SDK 55 project from scratch, install all Phase 1 dependencies, configure NativeWind v4 dark theme, set up the Supabase client with offline-safe auth, create the Zustand+MMKV store, write database migrations for profiles and PR baselines tables with RLS, and establish the Expo Router file structure with placeholder screens.

Purpose: Every subsequent plan in every phase depends on this scaffold existing and compiling. This is the absolute foundation.
Output: A running Expo app with dark theme, Supabase connection, persistent state, and database schema ready for auth.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Expo project, install dependencies, configure NativeWind + Supabase client + Zustand/MMKV store</name>
  <files>
    package.json, app.json, tsconfig.json,
    tailwind.config.js, metro.config.js, global.css, nativewind-env.d.ts,
    .env.example, .gitignore,
    src/lib/supabase/client.ts,
    src/lib/supabase/types/database.ts,
    src/stores/authStore.ts,
    src/constants/theme.ts,
    src/types/database.ts
  </files>
  <action>
    1. Create Expo project: `npx create-expo-app@latest . --template default@sdk-55` (run in the project root since it is an existing git repo -- may need to use a temp dir and move files if the tool requires an empty dir).

    2. Install all Phase 1 dependencies per RESEARCH.md installation commands:
       - Supabase + auth storage: `npx expo install @supabase/supabase-js expo-sqlite expo-secure-store @react-native-async-storage/async-storage`
       - State: `npx expo install zustand react-native-mmkv`
       - Connectivity: `npx expo install @react-native-community/netinfo`
       - Forms: `npm install react-hook-form zod @hookform/resolvers`
       - NativeWind (PINNED versions): `npm install nativewind@4.1.23 tailwindcss@3.4.17`
       - Image picker: `npx expo install expo-image-picker`
       - Dev client: `npx expo install expo-dev-client`

    3. Configure NativeWind v4:
       - Create `tailwind.config.js` with content paths `['./app/**/*.{js,ts,tsx}', './src/**/*.{js,ts,tsx}']`, preset `require('nativewind/preset')`, darkMode `'class'`, and the dark/bold color palette from RESEARCH.md (background #0a0a0a, surface #1a1a1a, surface-elevated #252525, accent #3b82f6, etc.)
       - Create `metro.config.js` with `withNativeWind(config, { input: './global.css' })`
       - Create `global.css` with @tailwind base/components/utilities directives
       - Create `nativewind-env.d.ts` with `/// <reference types="nativewind/types" />`

    4. Create Supabase client (`src/lib/supabase/client.ts`):
       - Import `'expo-sqlite/localStorage/install'` at top
       - Create singleton client with `localStorage` as auth storage adapter
       - Set `autoRefreshToken: true`, `persistSession: true`, `detectSessionInUrl: false`
       - Add AppState listener: `stopAutoRefresh()` when backgrounded, `startAutoRefresh()` when active (prevents offline logout)
       - Read URL and anon key from `process.env.EXPO_PUBLIC_SUPABASE_URL` and `process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY`
       - Follow exact pattern from RESEARCH.md Pattern 1

    5. Create placeholder database types (`src/lib/supabase/types/database.ts` and `src/types/database.ts`):
       - Define TypeScript interfaces for `Profile` and `PRBaseline` matching the migration schema
       - Export Database type for Supabase client generic parameter

    6. Create Zustand auth store (`src/stores/authStore.ts`):
       - Follow exact pattern from RESEARCH.md Pattern 3
       - State: `hasCompletedOnboarding: boolean`, `preferredUnit: 'kg' | 'lbs'`, `isAuthenticated: boolean`, `userId: string | null`
       - Actions: `setOnboardingComplete()`, `setPreferredUnit()`, `setAuthenticated()`, `clearAuth()`
       - Persist to MMKV via `createJSONStorage(() => mmkvStorage)`

    7. Create theme constants (`src/constants/theme.ts`):
       - Export color palette matching tailwind.config.js for use in non-NativeWind contexts (e.g., StatusBar, navigation headers)

    8. Create `.env.example` with `EXPO_PUBLIC_SUPABASE_URL=` and `EXPO_PUBLIC_SUPABASE_ANON_KEY=` placeholders. Ensure `.env` is in `.gitignore`.
  </action>
  <verify>
    <automated>npx expo export --platform web --output-dir /tmp/expo-check 2>&1 | head -20 || npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Project compiles with zero TypeScript errors
    - All dependencies installed and resolvable
    - NativeWind config files exist and are valid
    - Supabase client exports a singleton with offline-safe config
    - Zustand store exports useAuthStore with MMKV persistence
    - Theme constants match tailwind config colors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create database migrations and Expo Router file structure with placeholder screens</name>
  <files>
    supabase/migrations/20260309000000_create_profiles.sql,
    supabase/migrations/20260309000001_create_pr_baselines.sql,
    app/_layout.tsx,
    app/index.tsx,
    app/(auth)/_layout.tsx,
    app/(auth)/login.tsx,
    app/(app)/_layout.tsx,
    app/(app)/(tabs)/dashboard.tsx,
    app/(app)/onboarding/pr-baseline.tsx
  </files>
  <action>
    1. Create Supabase migration for profiles table (`supabase/migrations/20260309000000_create_profiles.sql`):
       - Copy exact SQL from RESEARCH.md "Supabase Schema: Profiles Table" section
       - Table: `public.profiles` with id (UUID PK referencing auth.users), display_name (TEXT NOT NULL), avatar_url (TEXT nullable), preferred_unit (TEXT CHECK kg/lbs default 'lbs'), created_at, updated_at
       - Enable RLS immediately
       - Create SELECT/UPDATE/INSERT policies scoped to `auth.uid() = id`
       - Create trigger function `handle_new_user()` that auto-inserts profile row on auth.users INSERT

    2. Create Supabase migration for PR baselines table (`supabase/migrations/20260309000001_create_pr_baselines.sql`):
       - Copy exact SQL from RESEARCH.md "Supabase Schema: PR Baselines Table" section
       - Table: `public.pr_baselines` with id (UUID PK), user_id (UUID FK to auth.users), exercise_name (TEXT), weight (NUMERIC(6,2)), unit (TEXT CHECK kg/lbs), created_at, updated_at
       - UNIQUE constraint on (user_id, exercise_name)
       - Enable RLS, create SELECT/INSERT/UPDATE policies scoped to `auth.uid() = user_id`
       - Create index on user_id for RLS performance

    3. Create Expo Router file structure:
       - `app/_layout.tsx`: Root layout with NativeWind `<StatusBar style="light" />`, wrap children in providers (later plans will add auth state). For now, simple Stack navigator with dark background. Import `global.css`.
       - `app/index.tsx`: Entry redirect -- for now, redirect to `/(auth)/login` (Plan 02 will add auth state check).
       - `app/(auth)/_layout.tsx`: Stack layout for auth group, headerShown false, dark background.
       - `app/(auth)/login.tsx`: Placeholder screen with dark background and "Sign In" text (Plan 02 replaces with real form).
       - `app/(app)/_layout.tsx`: Tab navigator layout (placeholder, one tab for dashboard).
       - `app/(app)/(tabs)/dashboard.tsx`: Placeholder empty state dashboard with dark background and "Dashboard" text (Plan 03 builds the real dashboard shell).
       - `app/(app)/onboarding/pr-baseline.tsx`: Placeholder screen with "PR Baseline" text (Plan 03 builds the real form).

    All placeholder screens should use NativeWind dark classes (className="flex-1 bg-background items-center justify-center") to verify NativeWind is working.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Two SQL migration files exist with RLS enabled on both tables
    - Profiles table has auto-create trigger on auth.users insert
    - PR baselines table has UNIQUE(user_id, exercise_name) constraint
    - All 7 route files exist and export valid React components
    - Placeholder screens use NativeWind dark theme classes
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All route files exist: `app/_layout.tsx`, `app/index.tsx`, `app/(auth)/_layout.tsx`, `app/(auth)/login.tsx`, `app/(app)/_layout.tsx`, `app/(app)/(tabs)/dashboard.tsx`, `app/(app)/onboarding/pr-baseline.tsx`
- Migration files exist: `supabase/migrations/20260309000000_create_profiles.sql`, `supabase/migrations/20260309000001_create_pr_baselines.sql`
- Both migrations contain `ENABLE ROW LEVEL SECURITY`
- `src/lib/supabase/client.ts` exports `supabase`
- `src/stores/authStore.ts` exports `useAuthStore`
</verification>

<success_criteria>
- Expo project compiles and TypeScript check passes
- NativeWind v4 configured with dark/bold palette
- Supabase client configured with offline-safe session handling
- Zustand + MMKV persistence working
- Database schema (profiles + PR baselines) defined with RLS
- Expo Router file structure established with all placeholder screens
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
