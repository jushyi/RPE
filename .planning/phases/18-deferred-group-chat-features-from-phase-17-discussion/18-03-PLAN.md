---
phase: 18-deferred-group-chat-features-from-phase-17-discussion
plan: 03
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - src/features/social/components/ChatScreen.tsx
  - src/features/social/components/MessageBubble.tsx
  - src/features/social/components/MessageInput.tsx
  - src/features/social/components/GroupTabs.tsx
  - src/features/social/hooks/useChat.ts
  - app/(app)/social/group-chat.tsx
  - app/(app)/social/group-feed.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-10

must_haves:
  truths:
    - "User can see a Chat tab alongside Feed tab within each group screen"
    - "User can type and send text messages in group chat"
    - "Messages appear instantly via Supabase Realtime postgres_changes subscription"
    - "Messages show sender name, avatar, timestamp, and correct left/right alignment"
    - "Older messages load on scroll-up with cursor-based pagination"
  artifacts:
    - path: "src/features/social/components/ChatScreen.tsx"
      provides: "Full chat view with message list and input"
      min_lines: 80
    - path: "src/features/social/components/MessageBubble.tsx"
      provides: "Message bubble with sender info, timestamp, alignment"
      min_lines: 50
    - path: "src/features/social/components/MessageInput.tsx"
      provides: "Expandable text input with send button"
      min_lines: 40
    - path: "src/features/social/hooks/useChat.ts"
      provides: "Chat hook with Realtime subscription, send, loadMessages"
      exports: ["useChat"]
    - path: "src/features/social/components/GroupTabs.tsx"
      provides: "Feed | Chat tab switcher within group screen"
      min_lines: 25
  key_links:
    - from: "src/features/social/hooks/useChat.ts"
      to: "supabase Realtime postgres_changes"
      via: "subscribes to messages table INSERT/UPDATE"
      pattern: "postgres_changes.*messages"
    - from: "src/features/social/components/ChatScreen.tsx"
      to: "src/features/social/hooks/useChat.ts"
      via: "uses chat hook for messages and send"
      pattern: "useChat"
    - from: "src/features/social/components/GroupTabs.tsx"
      to: "ChatScreen and group feed"
      via: "tab switcher renders Chat or Feed content"
      pattern: "Chat|Feed"
---

<objective>
Build the core group chat UI with real-time messaging via Supabase Realtime.

Purpose: Users can send and receive text messages in group chats with instant delivery, making the app a real communication tool for the friend group.
Output: ChatScreen, MessageBubble, MessageInput components, useChat hook, GroupTabs switcher, group-chat route.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-RESEARCH.md
@.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-01-SUMMARY.md

@src/features/social/types/chat.ts
@src/stores/chatStore.ts
@src/lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: useChat hook + GroupTabs + group-chat route</name>
  <files>src/features/social/hooks/useChat.ts, src/features/social/components/GroupTabs.tsx, app/(app)/social/group-chat.tsx, app/(app)/social/group-feed.tsx</files>
  <action>
    1. Create `useChat(groupId)` hook:
       - `loadMessages(cursor?: string)`: Fetch 30 messages from Supabase, ordered by created_at DESC, with profile join (display_name, avatar_url). If cursor provided, fetch older messages (lt created_at). Store via chatStore.setMessages. Return reversed array for chronological display.
       - `loadMore()`: Use oldest message's created_at as cursor, prepend results.
       - `sendMessage(content, mediaUrl?, mediaType?)`: Insert into messages table via `(supabase.from as any)('messages').insert(...)`. Use optimistic local add with temp ID, replace on Realtime confirmation.
       - Realtime subscription in useEffect: Subscribe to `group-chat:${groupId}` channel with postgres_changes for INSERT and UPDATE on messages table filtered by group_id. On INSERT, call chatStore.addMessage. On UPDATE, call chatStore.updateMessage. Cleanup: removeChannel on unmount.
       - Use useRef for channel to prevent stale closures.
       - Return { messages, loadMessages, loadMore, sendMessage, loading, hasMore }.

    2. Create `GroupTabs` component:
       - Two tabs: "Feed" and "Chat" using a simple Pressable-based tab bar at the top.
       - Active tab highlighted with magenta underline (consistent with app theme).
       - Renders feed content or ChatScreen based on active tab.
       - Receives groupId as prop.

    3. Modify `app/(app)/social/group-feed.tsx` (created in Phase 17) to use GroupTabs instead of rendering the feed directly. Add `app/(app)/social/group-chat.tsx` as a route if needed, or integrate ChatScreen inline via GroupTabs within the existing group-feed screen.
  </action>
  <verify>
    <automated>npx jest --bail 2>&1 | tail -5</automated>
  </verify>
  <done>useChat hook connects to Supabase Realtime, sends and receives messages. GroupTabs renders Feed/Chat toggle. Group screen shows both tabs.</done>
</task>

<task type="auto">
  <name>Task 2: ChatScreen + MessageBubble + MessageInput</name>
  <files>src/features/social/components/ChatScreen.tsx, src/features/social/components/MessageBubble.tsx, src/features/social/components/MessageInput.tsx</files>
  <action>
    1. Create `MessageBubble` component:
       - Props: message (Message type), isMine (boolean), senderName, senderAvatar, readStatus ('sent'|'delivered'|'read').
       - Own messages: right-aligned, magenta/accent background, no avatar shown.
       - Others' messages: left-aligned, dark gray background, avatar + name shown above bubble.
       - Content: text wraps naturally. If deleted_at is set, show "This message was deleted" in italics (lighter color).
       - If edited_at is set (and not deleted), show small "edited" label below message text.
       - Timestamp shown below bubble text in smaller font.
       - Read receipt icons for own messages: single checkmark (Ionicons checkmark-outline) for delivered, double checkmark (Ionicons checkmark-done-outline) for read. Blue tint for read.
       - Long-press on own messages triggers onLongPress callback (for edit/delete actions in Plan 04).

    2. Create `MessageInput` component:
       - Expandable TextInput (maxHeight ~120dp, grows with content).
       - Send button (Ionicons send) on the right, disabled when input is empty.
       - Media attachment button (Ionicons attach) on the left (placeholder for Plan 04 media support -- renders but shows "Coming soon" alert for now).
       - Dark input background, light text, rounded corners.
       - KeyboardAvoidingView wrapping or use react-native KeyboardAvoidingView behavior.
       - onSend callback clears input and calls sendMessage.

    3. Create `ChatScreen` component:
       - Uses useChat(groupId) hook.
       - FlatList inverted={true} for chat-style scroll (newest at bottom).
       - renderItem renders MessageBubble for each message.
       - onEndReached triggers loadMore for older messages.
       - Empty state: "No messages yet. Start the conversation!" centered text.
       - MessageInput fixed at bottom.
       - useEffect calls loadMessages on mount.
       - Loading spinner while initial messages load.
       - Uses KeyboardAvoidingView (behavior="padding" on iOS, undefined on Android).
  </action>
  <verify>
    <automated>npx jest --bail 2>&1 | tail -5</automated>
  </verify>
  <done>ChatScreen renders message list with bubbles. Own messages right-aligned with read receipts. Others' messages left-aligned with avatar. MessageInput sends text. FlatList paginates on scroll.</done>
</task>

</tasks>

<verification>
- Group screen shows Feed | Chat tabs
- Chat tab renders message list with correct bubble alignment
- Sending a message inserts to Supabase and appears via Realtime
- Older messages load on scroll-up
- Empty state shown for new chats
- `npx jest --bail` passes
</verification>

<success_criteria>
Users can navigate to Chat tab in any group, send text messages, and see them appear in real-time. Messages are correctly styled with sender info, timestamps, and alignment.
</success_criteria>

<output>
After completion, create `.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-03-SUMMARY.md`
</output>
