import type { PlanDay, PlanDayExercise, TargetSet } from '@/features/plans/types';

// TODO: Plan 02 will flesh out these tests with real createPlan logic

describe('plan-days', () => {
  it('plan day creation sets sort_order from array index', () => {
    expect(true).toBe(true);
  });

  it('plan day weekday mapping is optional', () => {
    expect(true).toBe(true);
  });
});
