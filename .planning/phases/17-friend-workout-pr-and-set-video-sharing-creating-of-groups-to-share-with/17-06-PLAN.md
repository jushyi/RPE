---
phase: 17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with
plan: 06
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/features/social/components/HandleSetup.tsx
  - src/features/settings/components/ProfileSection.tsx
  - src/features/onboarding/components/HandleStep.tsx
  - src/features/onboarding/components/OnboardingPager.tsx
  - app/(app)/(tabs)/settings.tsx
autonomous: true
requirements:
  - SOCL-12
  - SOCL-01

must_haves:
  truths:
    - "User can set and edit their handle in Settings profile section"
    - "New users see handle setup step in onboarding flow"
    - "Handle validation enforces 3-20 chars, lowercase alphanumeric + underscores, starts with letter"
    - "Handle uniqueness is checked before save"
    - "Existing users without handle see a prompt to set one"
  artifacts:
    - path: "src/features/social/components/HandleSetup.tsx"
      provides: "Reusable handle input with validation and uniqueness check"
    - path: "src/features/onboarding/components/HandleStep.tsx"
      provides: "Onboarding step for handle selection"
  key_links:
    - from: "src/features/social/components/HandleSetup.tsx"
      to: "src/stores/friendshipStore.ts"
      via: "setMyHandle action"
      pattern: "setMyHandle"
    - from: "src/features/onboarding/components/HandleStep.tsx"
      to: "src/features/social/components/HandleSetup.tsx"
      via: "component reuse"
      pattern: "HandleSetup"
---

<objective>
Add handle (username) setup in two places: the Settings profile section and the onboarding flow. Handle is required for friend discovery via search.

Purpose: Handles enable remote friend discovery beyond in-person invite codes.
Output: HandleSetup component, Settings integration, onboarding HandleStep.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-CONTEXT.md
@.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-RESEARCH.md
@.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-01-SUMMARY.md

<interfaces>
From src/features/social/utils/handleValidation.ts (Plan 01):
```typescript
export const HANDLE_REGEX: RegExp;
export function validateHandle(handle: string): string | null; // null = valid, string = error message
```

From src/stores/friendshipStore.ts (Plan 01):
```typescript
// State: myHandle (string | null)
// Actions: setMyHandle(handle: string), fetchMyHandle()
```

From src/features/onboarding/components/OnboardingPager.tsx (existing):
```typescript
// Currently 4 steps: UnitPreferencesStep, PRBaselineStep, BodyStatsStep, FirstPlanPromptStep
// Uses conditional rendering (not PagerView) with step index
// Each step has onNext and onSkip callbacks
```

From src/features/settings/components/ProfileSection.tsx or app/(app)/(tabs)/settings.tsx (existing):
```typescript
// Settings screen with ProfileHeader, PreferencesSection, etc.
// Profile section shows avatar, display name
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HandleSetup component + Settings profile integration</name>
  <files>
    src/features/social/components/HandleSetup.tsx,
    app/(app)/(tabs)/settings.tsx
  </files>
  <action>
    1. Create `src/features/social/components/HandleSetup.tsx`:
       - Reusable component for setting/editing handle
       - Props: currentHandle (string | null), onSave (handle: string) => Promise<void>, mode ('inline' | 'step')
       - TextInput with @ prefix display, auto-lowercases input
       - Real-time validation using validateHandle() from utils
       - Debounced uniqueness check (500ms): query Supabase profiles table for existing handle (excluding self)
       - Visual feedback: red error text below input for validation errors, green checkmark for available handle
       - "Save" button (in 'inline' mode) or just calls onSave callback
       - Loading state during save
       - If currentHandle exists: shows current handle with "Edit" toggle to switch to edit mode

    2. Update settings screen to show handle in the profile section:
       - Read the settings.tsx to understand current layout
       - Add HandleSetup component in the profile section below the display name
       - Shows current handle (from friendshipStore.myHandle) or "Set your handle" prompt
       - onSave calls friendshipStore.setMyHandle which updates the profiles table handle column
       - Fetch handle on mount via friendshipStore.fetchMyHandle
  </action>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <done>HandleSetup component works with validation and uniqueness check. Settings shows handle with edit capability.</done>
</task>

<task type="auto">
  <name>Task 2: Handle step in onboarding flow</name>
  <files>
    src/features/onboarding/components/HandleStep.tsx,
    src/features/onboarding/components/OnboardingPager.tsx
  </files>
  <action>
    1. Create `src/features/onboarding/components/HandleStep.tsx`:
       - Follows same pattern as UnitPreferencesStep, PRBaselineStep (onNext, onSkip props)
       - Title: "Choose Your Handle"
       - Subtitle: "Friends can find you by your unique handle"
       - Uses HandleSetup component in 'step' mode
       - "Next" button saves handle then calls onNext
       - "Skip" button calls onSkip (handle is optional during onboarding; user can set later in Settings)
       - Per Phase 12 pattern: duplicates local state management rather than extracting shared component

    2. Update `src/features/onboarding/components/OnboardingPager.tsx`:
       - Add HandleStep as the first step (before UnitPreferencesStep)
       - Update step count from 4 to 5
       - Update StepDots to reflect 5 steps
       - HandleStep is step 0, then UnitPreferences (1), PRBaseline (2), BodyStats (3), FirstPlanPrompt (4)
       - Handle step uses onSkip to advance to next step without saving
  </action>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <done>Onboarding shows handle setup as first step with 5 total steps. Handle is optional (skippable). Settings handle edit works.</done>
</task>

</tasks>

<verification>
- HandleSetup validates format (3-20 chars, lowercase alphanumeric + underscore, starts with letter)
- HandleSetup checks uniqueness against Supabase
- Settings profile section shows handle with edit
- Onboarding has 5 steps with handle as first
- Handle is optional in onboarding (skippable)
- Full test suite passes: `npx jest --bail`
</verification>

<success_criteria>
- Handle setup available in both Settings and onboarding
- Validation and uniqueness checking works
- Existing users can set handle from Settings
- New users see handle step first in onboarding (can skip)
</success_criteria>

<output>
After completion, create `.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-06-SUMMARY.md`
</output>
