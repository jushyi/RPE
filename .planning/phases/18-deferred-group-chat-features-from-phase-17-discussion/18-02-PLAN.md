---
phase: 18-deferred-group-chat-features-from-phase-17-discussion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/social/components/ContentTypeCheckboxes.tsx
  - src/features/social/components/RetroShareButton.tsx
  - src/features/social/hooks/useRetroactiveShare.ts
  - app/(app)/history/[sessionId].tsx
autonomous: true
requirements:
  - CHAT-08
  - CHAT-09

must_haves:
  truths:
    - "User can select which content types to share via checkboxes (workout summary, individual PRs, individual videos)"
    - "User can share a past workout from the history detail screen"
    - "Retroactively shared feed cards show both share date and original workout date"
  artifacts:
    - path: "src/features/social/components/ContentTypeCheckboxes.tsx"
      provides: "Checkbox UI for selecting workout summary, individual PRs, individual videos"
      min_lines: 40
    - path: "src/features/social/components/RetroShareButton.tsx"
      provides: "Share button for history detail screen"
      min_lines: 20
    - path: "src/features/social/hooks/useRetroactiveShare.ts"
      provides: "Hook to load shareable data from a historical session"
      exports: ["useRetroactiveShare"]
  key_links:
    - from: "src/features/social/components/ContentTypeCheckboxes.tsx"
      to: "src/features/social/types/chat.ts"
      via: "uses ShareableContent type"
      pattern: "import.*ShareableContent"
    - from: "src/features/social/hooks/useRetroactiveShare.ts"
      to: "src/features/history"
      via: "fetches session data from history/Supabase"
      pattern: "supabase.*from.*workout_sessions|set_logs"
---

<objective>
Enhance the share flow with per-content-type checkboxes and add retroactive sharing from workout history.

Purpose: Users get granular control over what they share (workout summary, specific PRs, specific videos) and can share past workouts at any time, not just at completion.
Output: ContentTypeCheckboxes component, RetroShareButton, useRetroactiveShare hook.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-RESEARCH.md

@src/features/social/types.ts
@src/features/history/components/SessionDetail.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ContentTypeCheckboxes component + enhanced share flow</name>
  <files>src/features/social/components/ContentTypeCheckboxes.tsx</files>
  <action>
    Create a ContentTypeCheckboxes component that renders within the existing Phase 17 SharePrompt:

    1. "Workout Summary" toggle (on by default) -- shares full workout summary card.
    2. "PRs" section -- if the session had PRs, each PR is shown as a separate selectable checkbox with exercise name + weight. All checked by default.
    3. "Videos" section -- if the session had set videos, each video is shown as a selectable checkbox with exercise name + set number. All checked by default.

    Props: session data (exercises, PRs, videos), onChange callback returning ShareableContent object.

    Use Ionicons checkmark-circle / ellipse-outline for checkbox states. Follow dark theme with magenta accent for selected items. Use Pressable for each row.

    If no PRs exist, hide the PRs section. If no videos exist, hide the Videos section. Workout summary is always shown.

    Integrate into the existing SharePrompt component from Phase 17 -- the checkboxes should appear above the group selection. Modify the SharePrompt's onShare callback to pass the ShareableContent along with selected groupIds.

    Per user decision: "Full workout summary only (no individual exercise selection)" for the summary toggle. Individual selection only for PRs and videos.
  </action>
  <verify>
    <automated>npx jest --bail 2>&1 | tail -5</automated>
  </verify>
  <done>ContentTypeCheckboxes renders workout summary toggle, individual PR checkboxes, individual video checkboxes. SharePrompt passes ShareableContent to onShare.</done>
</task>

<task type="auto">
  <name>Task 2: Retroactive sharing from history detail</name>
  <files>src/features/social/hooks/useRetroactiveShare.ts, src/features/social/components/RetroShareButton.tsx, app/(app)/history/[sessionId].tsx</files>
  <action>
    1. Create `useRetroactiveShare` hook:
       - Takes a sessionId
       - Fetches session data from Supabase (workout_sessions + session_exercises + set_logs with video_url)
       - Returns { session, prs, videos, loading } where prs are exercises that had PR flags, videos are set_logs with non-null video_url
       - For retroactive shares, always fetch video_url from set_logs in Supabase (not local storage -- videos already uploaded per Pitfall 7 in RESEARCH.md)

    2. Create `RetroShareButton` component:
       - Renders a share icon button (Ionicons share-outline) on the history session detail screen header
       - On press, opens the same SharePrompt modal from Phase 17 with ContentTypeCheckboxes
       - Pre-populates with session data from useRetroactiveShare
       - When sharing: creates shared_items entries with payload including `workout_date` field (original session date) in addition to the standard share payload
       - Feed cards for retroactive shares: timeline position = created_at (share time), label shows "Workout from [original date]"

    3. Wire RetroShareButton into `app/(app)/history/[sessionId].tsx` in the header-right position. The button should only appear when groups exist (user has at least one group to share to).

    Per user decision: "No time limit -- any past workout can be shared" and "Re-sharing allowed."
  </action>
  <verify>
    <automated>npx jest --bail --testPathPattern="retroactiveShare|chat" 2>&1 | tail -5</automated>
  </verify>
  <done>Share button visible on history detail screen. Tapping opens SharePrompt with content type checkboxes. Retroactive shares include workout_date in payload. Same workout can be shared to multiple groups.</done>
</task>

</tasks>

<verification>
- ContentTypeCheckboxes renders with correct toggle behavior
- RetroShareButton appears on history detail screen
- SharePrompt enhanced with content type selection
- Retroactive share payload includes workout_date field
- `npx jest --bail` passes
</verification>

<success_criteria>
Share flow enhanced with granular content selection. Users can share past workouts from history with both dates shown on feed cards.
</success_criteria>

<output>
After completion, create `.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-02-SUMMARY.md`
</output>
