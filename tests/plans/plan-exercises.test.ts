import type { PlanDayExercise, TargetSet } from '@/features/plans/types';

// TODO: Plan 02/03 will flesh out these tests with real exercise assignment logic

describe('plan-exercises', () => {
  it('exercise target_sets default to empty array', () => {
    expect(true).toBe(true);
  });

  it('exercise sort_order reflects drag position', () => {
    expect(true).toBe(true);
  });
});
