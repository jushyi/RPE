---
phase: 18-deferred-group-chat-features-from-phase-17-discussion
plan: 05
type: execute
wave: 3
depends_on:
  - 18-03
  - 18-04
files_modified:
  - supabase/functions/send-push/index.ts
  - src/features/social/hooks/useChat.ts
  - src/features/social/components/ChatScreen.tsx
  - src/features/social/components/MessageInput.tsx
autonomous: false
requirements:
  - CHAT-07
  - CHAT-10

must_haves:
  truths:
    - "Group members receive push notification when a new chat message is sent"
    - "Muted groups do not generate chat push notifications"
    - "User viewing the active chat does not receive a push notification for messages in that chat"
    - "Chat tab and Feed tab work seamlessly within the group screen"
    - "All chat features (typing, receipts, media, edit/delete) are wired together"
  artifacts:
    - path: "supabase/functions/send-push/index.ts"
      provides: "Extended Edge Function handling chat message push notifications"
    - path: "src/features/social/components/ChatScreen.tsx"
      provides: "Fully integrated chat screen with all features wired"
  key_links:
    - from: "supabase/functions/send-push/index.ts"
      to: "push_tokens table"
      via: "looks up tokens for non-muted group members"
      pattern: "push_tokens|group_members.*muted"
    - from: "src/features/social/components/ChatScreen.tsx"
      to: "useChat + useTypingIndicator + useReadReceipts"
      via: "composes all chat hooks"
      pattern: "useChat|useTypingIndicator|useReadReceipts"
---

<objective>
Wire push notifications for chat messages and integrate all chat features into a polished experience.

Purpose: Complete the chat feature with push notifications so users know when messages arrive, and verify the full end-to-end experience works correctly.
Output: Push notification support for chat, fully integrated chat screen, human verification of the complete feature.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-RESEARCH.md
@.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-03-SUMMARY.md
@.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-04-SUMMARY.md

@supabase/functions/send-push/index.ts
@src/features/social/hooks/useChat.ts
@src/features/social/components/ChatScreen.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push notifications for chat messages + final integration</name>
  <files>supabase/functions/send-push/index.ts, src/features/social/hooks/useChat.ts, src/features/social/components/ChatScreen.tsx, src/features/social/components/MessageInput.tsx</files>
  <action>
    1. Extend `send-push` Edge Function to handle chat message notifications:
       - New notification type: "chat_message" with payload { group_id, group_name, sender_name, message_preview (first 100 chars) }.
       - Look up group members from group_members table, exclude sender.
       - Check per-group mute status: filter out members where `muted = true` on their group_members row.
       - Send push to remaining members' push tokens via Expo Push API.
       - Push body: "[sender_name]: [message_preview]" with title "[group_name]".

    2. Trigger push notification from useChat.sendMessage:
       - After successful message insert, call send-push Edge Function with type "chat_message".
       - Fire-and-forget (don't block message display on push delivery).
       - Client-side: suppress notifications for the currently active group chat. Use a ref to track activeGroupId. When a push notification arrives for the active group, dismiss it silently (consistent with Pitfall 4 in RESEARCH.md, option b).

    3. Final ChatScreen integration:
       - Wire TypingIndicator component between message list and MessageInput.
       - Wire useReadReceipts: auto-markAsRead on mount and when new messages arrive while chat is focused.
       - Wire ChatMediaPicker into MessageInput (replace placeholder attach button).
       - Wire long-press edit/delete actions on MessageBubble.
       - Ensure Realtime channel is shared between useChat and useTypingIndicator (use same channel name `group-chat:${groupId}`) -- or coordinate via separate channels if needed for clean separation.
       - Handle keyboard properly: KeyboardAvoidingView on iOS (behavior="padding"), adjust FlatList contentInsetAdjustmentBehavior.

    4. Unread count badge on Chat tab in GroupTabs:
       - Compare latest message ID with user's last_read_message_id from group_read_receipts.
       - Show unread count badge on Chat tab when messages exist beyond last read position.
       - Update badge in real-time as messages arrive.
  </action>
  <verify>
    <automated>npx jest --bail 2>&1 | tail -5</automated>
  </verify>
  <done>Push notifications sent to non-muted group members on new messages. Active chat suppresses own notifications. Unread badge shows on Chat tab. All chat features wired together.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete group chat and enhanced share flow</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 18 feature set across two devices/accounts:
    - Group chat with real-time messaging, typing indicators, read receipts
    - Image and video support in chat
    - Message edit (within 15 min) and delete
    - Push notifications for chat (respects mute)
    - Enhanced share flow with content-type checkboxes
    - Retroactive sharing from workout history
  </action>
  <verify>
    1. Navigate to Social tab, open a group
    2. Verify Feed and Chat tabs are visible
    3. Tap Chat tab -- verify empty state message
    4. Send a text message -- verify it appears with correct alignment (right side, magenta)
    5. On a second device/account: verify message appears instantly with left alignment and sender avatar
    6. Type in input -- verify typing indicator shows on other device ("[Name] is typing...")
    7. Stop typing -- verify indicator disappears after ~2 seconds
    8. Check read receipts: single checkmark on sent message, double blue checkmarks after other user views
    9. Long-press own message within 15 minutes -- verify Edit and Delete options appear
    10. Edit a message -- verify "edited" label appears, content updates on both devices
    11. Delete a message -- verify "This message was deleted" placeholder on both devices
    12. Send an image via attach button -- verify it renders inline in chat
    13. Send a video via attach button -- verify thumbnail with play icon, tap plays fullscreen
    14. Verify push notification arrives on other device (with group name and message preview)
    15. Mute the group -- verify no push notification for subsequent messages
    16. Complete a workout, on share screen verify content-type checkboxes (summary, individual PRs, individual videos)
    17. Go to History, open a past workout, tap Share button -- verify same share flow opens
    18. Share retroactively -- verify feed card shows "Workout from [original date]" label
  </verify>
  <done>All 18 verification steps pass. Chat messaging, media, notifications, and enhanced sharing work end-to-end.</done>
</task>

</tasks>

<verification>
- Push notifications arrive for chat messages in non-muted groups
- Active chat suppresses notifications for current group
- Unread badge visible on Chat tab
- All chat features (typing, receipts, media, edit/delete) work end-to-end
- Share flow has content-type checkboxes
- Retroactive sharing works from history
- Full suite: `npx jest --bail` passes
</verification>

<success_criteria>
Complete group chat messaging feature is operational with real-time delivery, presence indicators, media support, push notifications, and enhanced sharing. Human verification confirms all user-facing behaviors work correctly across devices.
</success_criteria>

<output>
After completion, create `.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-05-SUMMARY.md`
</output>
