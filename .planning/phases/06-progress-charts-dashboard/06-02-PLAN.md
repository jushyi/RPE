---
phase: 06-progress-charts-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/(app)/progress/[exerciseId].tsx
  - app/(app)/progress/_layout.tsx
  - src/features/progress/components/ExerciseChart.tsx
  - src/features/progress/components/ChartTimeRangeSelector.tsx
  - src/features/progress/components/ChartMetricTabs.tsx
  - src/features/progress/components/Sparkline.tsx
  - src/features/progress/components/ChartEmptyState.tsx
  - src/features/dashboard/components/BodyweightCard.tsx
  - src/features/exercises/components/ExerciseListItem.tsx
autonomous: false
requirements:
  - HIST-02
  - HIST-03

must_haves:
  truths:
    - "User can open any exercise and see a line chart of estimated 1RM over time"
    - "User can switch chart metric between estimated 1RM, max weight, and total volume via tabs"
    - "User can switch chart time range between 1M/3M/6M/1Y/All"
    - "Chart shows view-only trend line with no tappable data points"
    - "Empty state shows when no workout data exists for an exercise"
    - "Single data point shows a message rather than trying to draw a line"
    - "Bodyweight dashboard card shows latest weight, mini sparkline trend, and quick-add button"
    - "User can log bodyweight with unit selector directly from the dashboard card"
    - "Sparkline component is reusable for dashboard progress summary"
    - "User can navigate to exercise chart from the exercises tab via a chart icon on each exercise card"
  artifacts:
    - path: "app/(app)/progress/[exerciseId].tsx"
      provides: "Full-screen exercise chart route"
      contains: "ExerciseChart"
    - path: "src/features/progress/components/ExerciseChart.tsx"
      provides: "Victory Native CartesianChart with Line for exercise data"
      exports: ["ExerciseChart"]
    - path: "src/features/progress/components/ChartTimeRangeSelector.tsx"
      provides: "1M/3M/6M/1Y/All toggle chips"
      exports: ["ChartTimeRangeSelector"]
    - path: "src/features/progress/components/ChartMetricTabs.tsx"
      provides: "Est. 1RM / Max Weight / Volume tab selector"
      exports: ["ChartMetricTabs"]
    - path: "src/features/progress/components/Sparkline.tsx"
      provides: "Minimal CartesianChart with no axes for inline trends"
      exports: ["Sparkline"]
    - path: "src/features/dashboard/components/BodyweightCard.tsx"
      provides: "Dashboard card with latest weight, sparkline, and quick-add"
      exports: ["BodyweightCard"]
  key_links:
    - from: "app/(app)/progress/[exerciseId].tsx"
      to: "src/features/progress/hooks/useExerciseChartData.ts"
      via: "fetches chart data for the exercise"
      pattern: "useExerciseChartData"
    - from: "src/features/progress/components/ExerciseChart.tsx"
      to: "victory-native"
      via: "CartesianChart + Line rendering"
      pattern: "CartesianChart"
    - from: "src/features/dashboard/components/BodyweightCard.tsx"
      to: "src/features/progress/hooks/useBodyweightData.ts"
      via: "fetches and logs bodyweight entries"
      pattern: "useBodyweightData"
    - from: "src/features/dashboard/components/BodyweightCard.tsx"
      to: "src/features/progress/components/Sparkline.tsx"
      via: "renders mini bodyweight trend"
      pattern: "Sparkline"
    - from: "src/features/exercises/components/ExerciseListItem.tsx"
      to: "app/(app)/progress/[exerciseId].tsx"
      via: "chart icon navigates to exercise progress chart"
      pattern: "router.push.*progress"
---

<objective>
Build the chart UI layer: full-screen exercise progress chart with metric tabs and time range selectors, reusable Sparkline component, bodyweight dashboard card with inline logging, and exercise chart navigation from the exercises tab.

Purpose: Delivers the visual progress feedback that makes the app motivating -- users can see their strength trending upward and track bodyweight over time. Creates the Sparkline component reused by Plan 03's dashboard cards. Wires the dual chart access path (history detail view + exercises tab) per user decision.
Output: Exercise chart route, chart components, Sparkline, BodyweightCard, exercise list chart navigation -- all wired to Plan 01's data hooks.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-progress-charts-dashboard/06-CONTEXT.md
@.planning/phases/06-progress-charts-dashboard/06-RESEARCH.md
@.planning/phases/06-progress-charts-dashboard/06-01-SUMMARY.md

@src/constants/theme.ts
@src/components/ui/Card.tsx
@src/components/ui/Button.tsx
@src/components/ui/Input.tsx

<interfaces>
<!-- From Plan 01 (types and hooks this plan consumes) -->

From src/features/progress/types.ts:
```typescript
export type TimeRange = '1M' | '3M' | '6M' | '1Y' | 'all';
export type ChartMetric = 'estimated_1rm' | 'max_weight' | 'total_volume';
export interface ChartPoint {
  date: number; // Unix timestamp ms
  estimated_1rm: number;
  max_weight: number;
  total_volume: number;
}
export interface BodyweightEntry {
  id: string;
  weight: number;
  unit: 'kg' | 'lbs';
  logged_at: string;
  created_at: string;
}
export interface SparklineData {
  date: number;
  value: number;
}
```

From src/features/progress/hooks/useExerciseChartData.ts:
```typescript
export function useExerciseChartData(exerciseId: string, timeRange: TimeRange): {
  data: ChartPoint[];
  loading: boolean;
  refetch: () => void;
}
```

From src/features/progress/hooks/useBodyweightData.ts:
```typescript
export function useBodyweightData(): {
  entries: BodyweightEntry[];
  isLoading: boolean;
  fetchEntries: () => Promise<void>;
  addEntry: (weight: number, unit: 'kg' | 'lbs') => Promise<void>;
  deleteEntry: (id: string) => Promise<void>;
  latest: BodyweightEntry | null;
  sparklineData: SparklineData[];
}
```

From src/features/progress/utils/chartHelpers.ts:
```typescript
export function formatChartDate(timestamp: number): string;
export function getTimeRangeStart(range: TimeRange): Date | null;
export function convertWeight(value: number, from: 'kg' | 'lbs', to: 'kg' | 'lbs'): number;
```

From assets/fonts/Inter-Regular.ttf: Font file for Skia chart axis labels.
Load with: `const font = useFont(require("@/assets/fonts/Inter-Regular.ttf"), 12);`
from `@shopify/react-native-skia`.

<!-- Existing ExerciseListItem interface (modified in Task 3) -->

From src/features/exercises/components/ExerciseListItem.tsx:
```typescript
interface ExerciseListItemProps {
  exercise: Exercise;
  onPress?: () => void;
}
export const ExerciseListItem = React.memo(function ExerciseListItem({ exercise, onPress }: ExerciseListItemProps) { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exercise chart screen with metric tabs, time range, and empty states</name>
  <files>app/(app)/progress/[exerciseId].tsx, app/(app)/progress/_layout.tsx, src/features/progress/components/ExerciseChart.tsx, src/features/progress/components/ChartTimeRangeSelector.tsx, src/features/progress/components/ChartMetricTabs.tsx, src/features/progress/components/ChartEmptyState.tsx</files>
  <action>
    Create app/(app)/progress/_layout.tsx:
    - Simple Stack layout. Stack.Screen for [exerciseId] with headerShown: false (custom header in screen).

    Create app/(app)/progress/[exerciseId].tsx:
    - Get exerciseId from useLocalSearchParams. Also accept exerciseName as a param for the header title.
    - State: selectedMetric (ChartMetric, default 'estimated_1rm'), selectedRange (TimeRange, default 'all').
    - Use useExerciseChartData(exerciseId, selectedRange) for data.
    - Layout: SafeAreaView with dark background.
      - Back button (Ionicons "arrow-back") + exercise name as title at top.
      - ChartMetricTabs below title.
      - ExerciseChart component (main chart area, ~250px height).
      - ChartTimeRangeSelector below chart.
    - Pass selectedMetric and data to ExerciseChart.
    - If loading, show ActivityIndicator centered.
    - If data.length === 0, show ChartEmptyState.
    - If data.length === 1, show message "Log more workouts to see a trend line" with the single data point value displayed as text.

    Build ExerciseChart component:
    - Props: { data: ChartPoint[], metric: ChartMetric, height?: number }
    - Uses CartesianChart from victory-native with:
      - xKey="date", yKeys=[metric]
      - padding: { left: 10, right: 10, bottom: 5, top: 10 }
      - xAxis: font from useFont(Inter-Regular.ttf, 12), tickCount: 5, labelColor: colors.textMuted, lineColor: colors.surfaceElevated, formatXLabel using formatChartDate
      - yAxis: same font, tickCount: 4, labelColor: colors.textMuted, lineColor: colors.surfaceElevated, formatYLabel rounds to integer (for weight/1RM) or formats with "k" suffix for volume > 1000
      - frame: lineColor: colors.surfaceElevated, lineWidth: 1
    - Child: Line component with points[metric], color: colors.accent for 1RM, colors.success for max weight, colors.warning for volume. strokeWidth: 2, curveType: "natural".
    - Guard: if font is null (still loading), return null. If data.length < 2, don't render (caller handles).
    - View-only: no gesture handlers, no tooltips, no tappable points (per user decision).
    - Wrap CartesianChart in a View with explicit height (default 250).

    Build ChartMetricTabs component:
    - Props: { selected: ChartMetric, onSelect: (metric: ChartMetric) => void }
    - Three tab buttons in a row: "Est. 1RM", "Max Weight", "Volume"
    - Selected tab: colors.accent background at 20% opacity, colors.accent text, rounded pill shape.
    - Unselected: transparent background, colors.textSecondary text.
    - Estimated 1RM is the primary/default (per user decision: "Estimated 1RM is the primary chart line").

    Build ChartTimeRangeSelector component:
    - Props: { selected: TimeRange, onSelect: (range: TimeRange) => void }
    - Row of 5 chips: "1M", "3M", "6M", "1Y", "All"
    - Same visual pattern as ChartMetricTabs but smaller chips.
    - "All" is selected by default (per user decision: "Default to all time").

    Build ChartEmptyState component:
    - Centered in chart area. Ionicons "analytics-outline" icon in colors.textMuted.
    - Text: "No workout data yet" in colors.textSecondary.
    - Subtitle: "Log workouts with this exercise to see progress" in colors.textMuted.
    - No CTA button (context is already within an exercise).

    All components use StyleSheet.create. Dark theme. No emojis -- Ionicons for all icons.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Exercise chart screen renders at /progress/[exerciseId] with Victory Native line chart showing estimated 1RM by default. Metric tabs switch between 1RM/max weight/volume. Time range chips filter to 1M/3M/6M/1Y/All. Empty state shows when no data. Single-point state shows text message. Chart is view-only with no interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Sparkline component and BodyweightCard with inline logging</name>
  <files>src/features/progress/components/Sparkline.tsx, src/features/dashboard/components/BodyweightCard.tsx</files>
  <action>
    Build Sparkline component:
    - Props: { data: SparklineData[], color: string, width: number, height: number }
    - Renders CartesianChart in a View with exact width/height dimensions.
    - xKey="date", yKeys=["value"], padding=0 (or { left: 2, right: 2, top: 2, bottom: 2 } for edge safety).
    - No xAxis, no yAxis, no frame -- pure line only.
    - Line: points.value, color prop, strokeWidth: 1.5, curveType: "natural".
    - Guard: if data.length < 2, render nothing (a sparkline needs at least 2 points).
    - This is a generic reusable component used by BodyweightCard and ProgressSummaryCard.

    Build BodyweightCard component (dashboard card per user decisions):
    - Uses Card component wrapper with title "Bodyweight".
    - Uses useBodyweightData() hook. Calls fetchEntries on mount (useFocusEffect pattern).
    - Layout top section:
      - Left: Latest weight displayed large (e.g., "185.5") with unit label ("lbs"). If no entries, show "--".
      - Right: Sparkline component (width: 80, height: 32, color: colors.accent) showing sparklineData from hook.
    - Layout bottom section:
      - "Log Weight" button (Button component, variant="secondary", compact size).
      - On press: expand inline input area (use useState toggle, not bottom sheet -- simpler for a card).
    - Inline input area (when expanded):
      - NumericInput: TextInput with keyboardType="decimal-pad", placeholder "Weight", one decimal precision.
      - Unit selector: Two chips side by side ("kg" / "lbs"). Default to latest entry's unit, or "lbs" if no history. Unit selector shown on EVERY input per user decision ("always ask, not inferred").
      - "Save" button: Calls addEntry(weight, unit). Collapses input area on success.
      - "Cancel" text button to collapse without saving.
    - One decimal precision enforced: regex validation on input (allow digits and at most one decimal point with one digit after).
    - If entry already exists for today (upsert), the button should read "Update Weight" instead of "Log Weight".
    - No separate full-screen page per user decision -- all logging happens on this card.

    All components use StyleSheet.create. No emojis. Dark theme with colors constants.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sparkline renders a minimal line chart without axes. BodyweightCard shows latest weight with sparkline trend and inline quick-add with unit selector. One decimal precision enforced. Today's entry shows "Update Weight" label. Both components are reusable and wired to Plan 01 hooks.</done>
</task>

<task type="auto">
  <name>Task 3: Add chart navigation icon to ExerciseListItem for exercises tab access path</name>
  <files>src/features/exercises/components/ExerciseListItem.tsx</files>
  <action>
    Add a chart icon tap target to ExerciseListItem so users can navigate to the exercise progress chart directly from the exercises tab. This implements the locked decision: "Both paths: accessible from exercise in history detail view AND from a dedicated progress section. Progress section accessible from dashboard or exercises tab for proactive progress checking."

    Modify ExerciseListItem:
    1. Import useRouter from expo-router and Ionicons from @expo/vector-icons.
    2. Add an optional prop `showChartIcon?: boolean` (default true) to ExerciseListItemProps. This allows callers that use ExerciseListItem in non-navigation contexts (e.g., plan builder exercise picker) to hide the icon.
    3. Add a chart icon button (Ionicons "stats-chart-outline") on the right side of the exercise card.
       - Position: absolute right, vertically centered in the card. Or use flexbox: row with name/badges on the left (flex: 1) and icon on the right.
       - Icon color: colors.textMuted, size 20.
       - Wrap in a Pressable with hitSlop for easy tapping (hitSlop: { top: 8, bottom: 8, left: 8, right: 8 }).
       - On press: call router.push({ pathname: '/(app)/progress/[exerciseId]', params: { exerciseId: exercise.id, exerciseName: exercise.name } }).
       - IMPORTANT: stopPropagation on the icon press so it does not also trigger the card's onPress (which opens the exercise detail bottom sheet). Use a separate Pressable for the icon that captures the event.
    4. Style the icon area with a small padding to separate it visually from the exercise name/badges.
    5. When showChartIcon is false, do not render the icon or import router.

    The existing onPress behavior (opening the bottom sheet for exercise details) remains unchanged. The chart icon is an additional, independent tap target on the same card.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ExerciseListItem shows a chart icon on the right side. Tapping the icon navigates to /progress/[exerciseId] with exercise name. Tapping elsewhere on the card still opens the exercise detail bottom sheet. Icon can be hidden via showChartIcon={false} prop.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify exercise charts, bodyweight card, and exercises tab chart access</name>
  <action>Human verification of exercise charts, bodyweight card, and dual chart access paths.</action>
  <what-built>Exercise progress chart screen with metric switching and time ranges, reusable Sparkline component, bodyweight dashboard card with inline logging, and chart navigation icon on exercise cards in the exercises tab.</what-built>
  <how-to-verify>
    1. Navigate to the History tab and tap a session detail
    2. Tap an exercise card -- it should navigate to /progress/[exerciseId] showing a chart
    3. If exercise has workout data: verify line chart renders with estimated 1RM as default metric
    4. Tap "Max Weight" and "Volume" tabs -- chart should switch metric and line color
    5. Tap different time range chips (1M, 3M, 6M, 1Y, All) -- chart should update
    6. If exercise has no data: verify empty state shows "No workout data yet"
    7. Navigate to the Exercises tab
    8. Verify each exercise card shows a small chart icon on the right side
    9. Tap the chart icon on any exercise -- it should navigate to /progress/[exerciseId] with the exercise chart
    10. Tap the exercise card body (not the icon) -- it should still open the exercise detail bottom sheet (not the chart)
    11. Navigate to Dashboard tab
    12. Find the Bodyweight card -- it should show "--" if no entries or latest weight with sparkline
    13. Tap "Log Weight" button -- inline input should expand with numeric input and kg/lbs selector
    14. Enter a weight (e.g., 185.5) and select unit, tap Save
    15. Verify the card updates to show the logged weight
    16. Verify sparkline appears after logging at least 2 entries on different dates
  </how-to-verify>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <done>User confirms exercise charts render correctly with metric switching and time ranges, chart is accessible from both history detail AND exercises tab, and bodyweight card allows inline logging with unit selection.</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` -- no TypeScript errors
- `npx jest --bail` -- full test suite passes
- Exercise chart renders with Victory Native CartesianChart
- Metric tabs switch chart line between 1RM/max weight/volume
- Time range chips filter data
- Empty and single-point states handled
- Sparkline renders minimal trend line
- BodyweightCard logs with unit selector and shows sparkline trend
- Exercise chart accessible from exercises tab via chart icon on each exercise card
- Chart icon tap and card body tap are independent (icon -> chart, body -> detail sheet)
</verification>

<success_criteria>
- Exercise chart screen accessible at /progress/[exerciseId]
- CartesianChart renders line chart with Skia (Inter font for axis labels)
- Metric tabs: Est. 1RM (primary, accent blue), Max Weight (green), Volume (amber)
- Time range: 1M/3M/6M/1Y/All chips, All default
- View-only chart: no tooltips, no tap interaction
- Empty state: icon + message when no data
- Single point: text message instead of chart
- Sparkline: reusable, no-axis mini chart component
- BodyweightCard: latest weight + sparkline + inline log with unit selector
- One decimal precision on bodyweight input
- Dual access path: exercise chart reachable from history detail view AND exercises tab
- ExerciseListItem shows chart icon that navigates to progress chart
- All components use StyleSheet.create, Ionicons for icons, no emojis
</success_criteria>

<output>
After completion, create `.planning/phases/06-progress-charts-dashboard/06-02-SUMMARY.md`
</output>
