---
phase: 12-proper-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/onboarding/index.tsx
  - app/_layout.tsx
  - src/features/onboarding/components/OnboardingPager.tsx
  - src/features/onboarding/components/StepDots.tsx
  - src/features/onboarding/components/UnitPreferencesStep.tsx
  - src/features/onboarding/components/PRBaselineStep.tsx
autonomous: true
requirements: [OB-01, OB-02, OB-05]

must_haves:
  truths:
    - "New user after sign-up sees 4-step onboarding flow starting with unit preferences"
    - "User can select weight unit (kg/lbs) and measurement unit (in/cm) on Step 1"
    - "User can enter PR baselines for Big 3 lifts on Step 2 with units defaulting to Step 1 choice"
    - "Step dots indicator shows current position across 4 steps"
    - "Existing users with hasCompletedOnboarding:true bypass new flow entirely"
  artifacts:
    - path: "src/features/onboarding/components/OnboardingPager.tsx"
      provides: "PagerView wrapper with navigation and step dots"
      min_lines: 60
    - path: "src/features/onboarding/components/StepDots.tsx"
      provides: "Step progress dots indicator"
      min_lines: 20
    - path: "src/features/onboarding/components/UnitPreferencesStep.tsx"
      provides: "Step 1: weight and measurement unit selection"
      min_lines: 40
    - path: "src/features/onboarding/components/PRBaselineStep.tsx"
      provides: "Step 2: Big 3 lift PR entry"
      min_lines: 40
    - path: "app/(app)/onboarding/index.tsx"
      provides: "Onboarding entry screen rendering OnboardingPager"
      min_lines: 15
  key_links:
    - from: "app/_layout.tsx"
      to: "/(app)/onboarding"
      via: "route guard redirect for !hasCompletedOnboarding"
      pattern: "onboarding(?!.*pr-baseline)"
    - from: "src/features/onboarding/components/OnboardingPager.tsx"
      to: "react-native-pager-view"
      via: "PagerView ref-based navigation"
      pattern: "PagerView"
    - from: "src/features/onboarding/components/UnitPreferencesStep.tsx"
      to: "src/stores/authStore.ts"
      via: "setPreferredUnit and setPreferredMeasurementUnit"
      pattern: "setPreferredUnit|setPreferredMeasurementUnit"
---

<objective>
Build the onboarding flow structure (PagerView with step dots, navigation buttons) and the first two data-collection steps: Unit Preferences (required) and PR Baselines (skippable). Update root layout route guard to point new users to the new flow.

Purpose: Replace the single-screen PR-only onboarding with a proper multi-step flow that collects unit preferences first, then PR baselines with correct default units.
Output: Working 4-page PagerView (pages 3-4 placeholder), Steps 1-2 fully functional, route guard updated.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-proper-onboarding/12-CONTEXT.md
@.planning/phases/12-proper-onboarding/12-RESEARCH.md

@src/stores/authStore.ts
@src/features/workout/components/ExercisePager.tsx
@src/features/settings/components/PreferencesSection.tsx
@src/features/auth/components/PRBaselineForm.tsx
@app/(app)/onboarding/_layout.tsx
@app/(app)/onboarding/pr-baseline.tsx
@app/_layout.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/stores/authStore.ts:
```typescript
interface AuthState {
  hasCompletedOnboarding: boolean;
  preferredUnit: 'kg' | 'lbs';
  preferredMeasurementUnit: 'in' | 'cm';
  isAuthenticated: boolean;
  userId: string | null;
  deletionScheduledAt: string | null;
}

interface AuthActions {
  setOnboardingComplete: () => void;
  setPreferredUnit: (unit: 'kg' | 'lbs') => void;
  setPreferredMeasurementUnit: (unit: 'in' | 'cm') => void;
  setAuthenticated: (userId: string) => void;
  setDeletionScheduledAt: (date: string | null) => void;
  clearAuth: () => void;
}
```

From app/_layout.tsx (line 66 - route guard to update):
```typescript
if (!hasCompletedOnboarding) {
  router.replace('/(app)/onboarding/pr-baseline');
}
```

From app/(app)/onboarding/pr-baseline.tsx (keep for edit mode):
```typescript
// pr-baseline.tsx handles mode=edit from dashboard PR editing
// Do NOT delete this file - only the NEW index.tsx replaces it as onboarding entry
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OnboardingPager, StepDots, and UnitPreferencesStep</name>
  <files>
    src/features/onboarding/components/OnboardingPager.tsx,
    src/features/onboarding/components/StepDots.tsx,
    src/features/onboarding/components/UnitPreferencesStep.tsx
  </files>
  <action>
Create the onboarding directory `src/features/onboarding/components/`.

**StepDots.tsx:** Reusable step dots indicator following ExercisePager dot pattern. Props: `total: number`, `current: number`. Render horizontal row of small circles, active dot uses `colors.accent`, inactive uses `colors.surface`. Use StyleSheet.create.

**UnitPreferencesStep.tsx:** Step 1 screen. Two sections: "Weight Unit" with kg/lbs toggle, "Measurement Unit" with in/cm toggle. Use the SegmentedToggle pattern from PreferencesSection (duplicate locally or extract). Read initial values from authStore (`preferredUnit`, `preferredMeasurementUnit`). This step has NO skip button -- only a "Next" button. On Next press: call `setPreferredUnit(weightUnit)` and `setPreferredMeasurementUnit(measurementUnit)` on authStore, then call `onNext()` prop. Title: "Choose Your Units". Subtitle explaining this affects all weight/measurement displays. Use Ionicons for a scale/ruler icon at the top. Dark theme, StyleSheet.create, no emojis.

**OnboardingPager.tsx:** Main wrapper component. Props: none (self-contained). Uses PagerView ref with 4 pages. State: `currentStep` (0-3). Renders StepDots at top. Renders 4 pages inside PagerView: UnitPreferencesStep, PRBaselineStep (placeholder View for now), BodyStatsStep (placeholder View), FirstPlanPromptStep (placeholder View). Bottom area has navigation buttons: Step 0 shows only "Next", Steps 1-2 show "Skip" and "Next", Step 3 shows "Skip" and "Create Plan". Back navigation via PagerView swipe (scrollEnabled={true}). The `onNext` callback for each step calls `pagerRef.current?.setPage(currentStep + 1)`. The `onSkip` callback also advances to next page (or completes if on last step). Track page changes via `onPageSelected`. When on last step, skip calls `onComplete` prop. Pass `onComplete` prop for when the full flow finishes.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/Gym-App && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>StepDots renders dot indicators, UnitPreferencesStep shows two unit toggles with Next button that saves to authStore, OnboardingPager wraps all steps in PagerView with navigation</done>
</task>

<task type="auto">
  <name>Task 2: Create PRBaselineStep, wire into OnboardingPager, update route guard</name>
  <files>
    src/features/onboarding/components/PRBaselineStep.tsx,
    src/features/onboarding/components/OnboardingPager.tsx,
    app/(app)/onboarding/index.tsx,
    app/_layout.tsx
  </files>
  <action>
**PRBaselineStep.tsx:** Step 2 screen for Big 3 lift PR baselines (bench press, squat, deadlift). Read `preferredUnit` from authStore to set default unit toggle value. For each lift: show exercise name, weight input (numeric keyboard), and a small kg/lbs unit toggle that defaults to the unit chosen in Step 1. Use the `usePRBaselines` hook pattern from PRBaselineForm for saving. The step is skippable -- user can tap Skip to advance without entering any values. On "Next" press: save any entered values via usePRBaselines hook, then call `onNext()`. Title: "Set Your PRs". Subtitle: "Enter your current personal records (optional)". Use Ionicons trophy-outline icon at top. StyleSheet.create, dark theme, no emojis.

**OnboardingPager.tsx update:** Replace placeholder page 2 (index 1) with PRBaselineStep component. Pass `onNext` and `onSkip` props.

**app/(app)/onboarding/index.tsx:** New screen file. Renders SafeAreaView with OnboardingPager. On `onComplete`: call `setOnboardingComplete()` from authStore, then `router.replace('/(app)/(tabs)/dashboard')`. Keep `pr-baseline.tsx` untouched (it handles edit mode from dashboard).

**app/_layout.tsx:** Update line 66 route guard from `router.replace('/(app)/onboarding/pr-baseline')` to `router.replace('/(app)/onboarding')`. This makes new users hit index.tsx (new multi-step flow) instead of the old PR-only screen. Existing users with `hasCompletedOnboarding: true` are unaffected.
  </action>
  <verify>
    <automated>cd C:/Users/maser/Projects/Gym-App && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>PRBaselineStep shows Big 3 lifts with unit toggle defaulting to Step 1 choice, route guard routes new users to multi-step onboarding, pr-baseline.tsx still works for edit mode</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- New user sign-up flow routes to `/(app)/onboarding` (index.tsx)
- Steps 1 and 2 render correctly in PagerView with dot indicators
- Unit preferences saved to authStore on Step 1 Next
- PR baselines use correct default unit from Step 1
- Existing pr-baseline.tsx edit mode unaffected
</verification>

<success_criteria>
- OnboardingPager renders 4-page PagerView with StepDots
- UnitPreferencesStep saves units to authStore and is not skippable
- PRBaselineStep shows Big 3 lifts with correct default unit
- Route guard updated to point to new onboarding index
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/12-proper-onboarding/12-01-SUMMARY.md`
</output>
