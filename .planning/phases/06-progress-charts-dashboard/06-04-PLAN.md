---
phase: 06-progress-charts-dashboard
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/progress/components/Sparkline.tsx
  - src/features/progress/components/ExerciseChart.tsx
  - src/features/dashboard/hooks/useProgressSummary.ts
  - src/features/dashboard/components/ProgressSummaryCard.tsx
  - src/features/workout/hooks/useSyncQueue.ts
autonomous: true
gap_closure: true
requirements: [HIST-02, HIST-03, DASH-01]

must_haves:
  truths:
    - "Progress Summary sparklines display clear, readable trend lines or a not-enough-data placeholder"
    - "Exercise chart lines render fully within chart bounds with visible axis labels"
    - "Est. 1RM chart shows correct non-zero weight values for logged sets"
    - "New set_logs inserted via sync queue include computed estimated_1rm"
  artifacts:
    - path: "src/features/progress/components/Sparkline.tsx"
      provides: "Sparkline with linear curve, yDomain padding, and insufficient-data placeholder"
    - path: "src/features/progress/components/ExerciseChart.tsx"
      provides: "Chart with adequate padding for axis labels"
    - path: "src/features/workout/hooks/useSyncQueue.ts"
      provides: "estimated_1rm computed client-side in set_logs insert payload"
    - path: "src/features/dashboard/hooks/useProgressSummary.ts"
      provides: "3+ data point threshold for sparkline rendering"
    - path: "src/features/dashboard/components/ProgressSummaryCard.tsx"
      provides: "Conditional sparkItem rendering only when Sparkline has sufficient data"
  key_links:
    - from: "src/features/workout/hooks/useSyncQueue.ts"
      to: "set_logs table"
      via: "estimated_1rm field in insert payload"
      pattern: "estimated_1rm.*calculateEpley"
    - from: "src/features/dashboard/hooks/useProgressSummary.ts"
      to: "src/features/progress/components/Sparkline.tsx"
      via: "3+ data point threshold matching Sparkline minimum"
      pattern: "chartData.length.*>=.*3"
---

<objective>
Fix two UAT-reported bugs in Phase 06: (1) sparklines look weird with sparse data, and (2) exercise chart lines overflow bounds with incorrect Est. 1RM values.

Purpose: Close the two remaining UAT gaps so progress charts display correctly.
Output: Patched Sparkline, ExerciseChart, useProgressSummary, ProgressSummaryCard, and useSyncQueue.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and existing code the executor needs -->

From src/features/history/utils/epley.ts:
```typescript
export function calculateEpley1RM(weight: number, reps: number): number;
```

From src/features/workout/types.ts:
```typescript
export interface SetLog {
  id: string;
  set_number: number;
  weight: number;
  reps: number;
  rpe: number | null;
  unit: 'kg' | 'lbs';
  is_pr: boolean;
  logged_at: string;
}
```

From src/features/progress/types.ts (SparklineData):
```typescript
export interface SparklineData {
  date: number;
  value: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sparkline rendering for sparse data</name>
  <files>src/features/progress/components/Sparkline.tsx, src/features/dashboard/hooks/useProgressSummary.ts, src/features/dashboard/components/ProgressSummaryCard.tsx</files>
  <action>
Fix three compounding sparkline issues:

1. **Sparkline.tsx** — Make these changes:
   - Raise the minimum data threshold from `data.length < 2` to `data.length < 3`
   - When data has fewer than 3 points, render a small placeholder instead of returning null. Show a short Text element with "Not enough data" in `colors.textMuted` at fontSize 10, centered in the width/height container.
   - Change `curveType` from `"natural"` to `"linear"` — cubic interpolation exaggerates sparse data
   - Add `yDomain` padding to prevent visual exaggeration. Compute yMin and yMax from data values, then apply 10% padding: `domain={{ y: [yMin - padding, yMax + padding] }}` where `padding = (yMax - yMin) * 0.1 || 1`. Pass this via the `domain` prop on CartesianChart.

2. **useProgressSummary.ts** — In `fetchKeyLiftSparklines`, change the threshold from `chartData.length > 0` (line 194) to `chartData.length >= 3` before adding to sparklineMap. This prevents exercises with only 1-2 data points from appearing in the sparklines section.

3. **ProgressSummaryCard.tsx** — The sparklines section already conditionally renders based on `hasSparklines` (Object.keys check), which is sufficient since useProgressSummary now filters at 3+ points. No changes needed here — but verify the sparkItem container does NOT render when Sparkline returns the placeholder (it should still render, the placeholder IS the content now).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Sparklines show linear trends with yDomain padding for 3+ data points, and a "Not enough data" placeholder for fewer. No ghost labels for exercises with insufficient data.</done>
</task>

<task type="auto">
  <name>Task 2: Fix chart overflow and Est. 1RM null values</name>
  <files>src/features/progress/components/ExerciseChart.tsx, src/features/workout/hooks/useSyncQueue.ts</files>
  <action>
Fix two bugs causing chart lines to go off-screen and Est. 1RM showing wrong values:

1. **ExerciseChart.tsx** — Change the `padding` prop on CartesianChart (line 54) from `{ left: 10, right: 10, bottom: 5, top: 10 }` to `{ left: 50, right: 16, bottom: 25, top: 16 }`. The left padding of 50 accommodates y-axis weight labels (e.g., "225", "1.2k"). The bottom padding of 25 accommodates date labels. This ensures the polyline renders within the visible chart area.

2. **useSyncQueue.ts** — In the `enqueueCompletedSession` function, add `estimated_1rm` to the set_logs insert payload. Import `calculateEpley1RM` from `@/features/history/utils/epley`:
   ```
   import { calculateEpley1RM } from '@/features/history/utils/epley';
   ```
   Then in the set_logs data object (around line 120-129), add:
   ```
   estimated_1rm: calculateEpley1RM(set.weight, set.reps),
   ```
   This ensures all NEW set_logs have estimated_1rm computed client-side. The existing migration already backfilled historical data, but new inserts were missing the value (all NULL, displayed as 0 on charts).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Chart padding prevents line overflow. New set_logs include computed estimated_1rm via Epley formula. Est. 1RM chart shows real weight values for all sessions (existing backfilled + new computed).</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. Sparkline component renders linear curves with y-axis padding for 3+ points
3. Sparkline shows "Not enough data" placeholder for <3 points
4. ExerciseChart padding is {left:50, right:16, bottom:25, top:16}
5. useSyncQueue set_logs insert includes estimated_1rm field
6. useProgressSummary filters sparklines at 3+ data points
</verification>

<success_criteria>
- Both UAT gaps closed: sparklines render clearly, chart lines stay within bounds, Est. 1RM shows correct values
- No TypeScript errors introduced
- No regression to existing chart functionality (metric tabs, time ranges, empty states all still work)
</success_criteria>

<output>
After completion, create `.planning/phases/06-progress-charts-dashboard/06-04-SUMMARY.md`
</output>
