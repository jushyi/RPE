---
phase: 18-deferred-group-chat-features-from-phase-17-discussion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260319000001_create_messages.sql
  - src/features/social/types/chat.ts
  - src/features/social/utils/chatUtils.ts
  - src/features/social/utils/shareContentSelection.ts
  - src/stores/chatStore.ts
  - tests/chat/editWindow.test.ts
  - tests/chat/readReceipts.test.ts
  - tests/chat/shareContentSelection.test.ts
  - tests/chat/retroactiveShare.test.ts
  - tests/chat/chatMediaUpload.test.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-03
  - CHAT-05
  - CHAT-06
  - CHAT-08
  - CHAT-09

must_haves:
  truths:
    - "Database tables exist for messages and group_read_receipts with RLS"
    - "chat-media storage bucket exists with RLS"
    - "TypeScript types cover Message, GroupReadReceipt, ShareableContent"
    - "Pure utility functions are tested for edit window, read status, share content selection"
    - "Chat store manages messages by group with add/update/set operations"
  artifacts:
    - path: "supabase/migrations/20260319000001_create_messages.sql"
      provides: "messages table, group_read_receipts table, chat-media bucket, RLS policies, Realtime publication"
      contains: "messages"
    - path: "src/features/social/types/chat.ts"
      provides: "Message, GroupReadReceipt, ShareableContent, ChatMediaType types"
      exports: ["Message", "GroupReadReceipt", "ShareableContent", "ChatMediaType"]
    - path: "src/features/social/utils/chatUtils.ts"
      provides: "canEditMessage, getMessageReadStatus, formatMessageTime pure functions"
      exports: ["canEditMessage", "getMessageReadStatus", "formatMessageTime"]
    - path: "src/stores/chatStore.ts"
      provides: "Chat message state management with Zustand + MMKV"
  key_links:
    - from: "src/features/social/types/chat.ts"
      to: "supabase/migrations/20260319000001_create_messages.sql"
      via: "types mirror DB schema"
      pattern: "interface.*Message|GroupReadReceipt"
    - from: "src/stores/chatStore.ts"
      to: "src/features/social/types/chat.ts"
      via: "store uses chat types"
      pattern: "import.*Message.*from"
---

<objective>
Create the database schema, TypeScript types, pure utility functions with tests, and Zustand store for group chat messaging.

Purpose: Establish the data layer foundation that all chat UI, real-time subscriptions, and share enhancements build upon.
Output: Migration file, type definitions, tested utility functions, chat store.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-RESEARCH.md

@supabase/migrations/20260318000001_create_social.sql
@src/features/social/types.ts
@src/stores/socialStore.ts
@src/stores/friendshipStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration + TypeScript types</name>
  <files>supabase/migrations/20260319000001_create_messages.sql, src/features/social/types/chat.ts</files>
  <behavior>
    - Types: Message interface with id, group_id, sender_id, content, media_url, media_type, created_at, edited_at, deleted_at
    - Types: GroupReadReceipt with group_id, user_id, last_read_message_id, updated_at
    - Types: ShareableContent with workoutSummary boolean, selectedPRs string[], selectedVideos string[]
    - Types: ChatMediaType union of 'image' | 'video'
  </behavior>
  <action>
    Create the Supabase migration with:
    1. `messages` table: id (UUID PK), group_id (FK groups), sender_id (FK auth.users), content (TEXT nullable), media_url (TEXT nullable), media_type (TEXT CHECK image/video), created_at, edited_at, deleted_at. Constraint: content OR media_url must be non-null. Indexes on (group_id, created_at DESC) and (sender_id).
    2. RLS policies on messages: members can SELECT via `is_group_member(group_id)`, members can INSERT (sender_id = auth.uid()), sender can UPDATE with 15-minute edit window OR soft delete (no time limit).
    3. `group_read_receipts` table: (group_id, user_id) PK, last_read_message_id (FK messages), updated_at. RLS: members can view, users can insert/update own.
    4. `chat-media` storage bucket (public read, user-folder write/delete).
    5. `ALTER PUBLICATION supabase_realtime ADD TABLE public.messages;`

    Create `src/features/social/types/chat.ts` with all type definitions. Follow the `as-any` pattern for Supabase from() calls on new tables.

    Reference RESEARCH.md Database Schema section for exact SQL. Use migration timestamp 20260319000001.
  </action>
  <verify>
    <automated>npx jest --bail --testPathPattern=chat 2>&1 | head -20</automated>
  </verify>
  <done>Migration file exists with messages + read_receipts tables + RLS + bucket. Types file exports Message, GroupReadReceipt, ShareableContent, ChatMediaType.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure utility functions with tests + chat store</name>
  <files>src/features/social/utils/chatUtils.ts, src/features/social/utils/shareContentSelection.ts, src/stores/chatStore.ts, tests/chat/editWindow.test.ts, tests/chat/readReceipts.test.ts, tests/chat/shareContentSelection.test.ts, tests/chat/retroactiveShare.test.ts, tests/chat/chatMediaUpload.test.ts</files>
  <behavior>
    - canEditMessage: returns true if sender_id matches AND deleted_at is null AND elapsed < 15 minutes; false otherwise
    - canEditMessage: returns false for other user's message, deleted message, message older than 15 min
    - getMessageReadStatus: returns 'read' when all members' last_read_message_id >= messageId, 'delivered' otherwise
    - getMessageReadStatus: handles empty receipts as 'delivered'
    - buildSharePayload: constructs payload with workoutSummary flag, selected PR IDs, selected video set IDs
    - buildRetroactiveSharePayload: includes original workout_date alongside share date
    - formatMessageTime: returns "HH:MM" for today, "Yesterday HH:MM", or "MMM D HH:MM" for older
    - chatMediaUpload stub: pickImage returns { mediaUrl, mediaType: 'image' } for a valid file; pickVideo returns { mediaUrl, thumbnailUrl, mediaType: 'video' } for a valid file
  </behavior>
  <action>
    Write tests FIRST (RED phase), then implement:

    1. `tests/chat/editWindow.test.ts`: Test canEditMessage with various scenarios (own message < 15min, own message > 15min, other user's message, deleted message).
    2. `tests/chat/readReceipts.test.ts`: Test getMessageReadStatus with all members read, some read, no receipts.
    3. `tests/chat/shareContentSelection.test.ts`: Test buildSharePayload with various checkbox combinations.
    4. `tests/chat/retroactiveShare.test.ts`: Test buildRetroactiveSharePayload includes workout_date field.
    5. `tests/chat/chatMediaUpload.test.ts`: Stub test for useChatMedia hook — mock expo-image-picker and Supabase storage upload. Test that pickImage resolves with { mediaUrl: string, mediaType: 'image' } and pickVideo resolves with { mediaUrl: string, thumbnailUrl: string, mediaType: 'video' }. Test that uploading=true during upload, false after. (Stub implementation — hook created in Plan 04.)

    Then implement in `chatUtils.ts` and `shareContentSelection.ts`.

    Finally create `chatStore.ts` (Zustand + MMKV, id: 'chat-store') with: messagesByGroup record, unreadCounts record, addMessage, updateMessage, setMessages, setUnreadCount, clearGroupMessages actions. Follow existing store patterns from socialStore.ts.
  </action>
  <verify>
    <automated>npx jest --bail --testPathPattern=chat</automated>
  </verify>
  <done>All 5 test files pass (4 utility tests + 1 chatMediaUpload stub). chatStore exports useChatStore with message management actions. canEditMessage enforces 15-min window. getMessageReadStatus computes delivered/read from receipts.</done>
</task>

</tasks>

<verification>
- `npx jest --bail --testPathPattern=chat` -- all chat utility tests pass
- Migration file contains messages table, group_read_receipts table, RLS policies, chat-media bucket, Realtime publication
- Types file exports Message, GroupReadReceipt, ShareableContent
- Chat store exports useChatStore with addMessage, updateMessage, setMessages
</verification>

<success_criteria>
Data layer foundation complete: migration ready for deployment, types defined, pure functions tested, store operational. All subsequent plans can build UI and hooks against these contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/18-deferred-group-chat-features-from-phase-17-discussion/18-01-SUMMARY.md`
</output>
