---
phase: 17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - app/(app)/social/create-group.tsx
  - app/(app)/social/group-detail.tsx
  - src/features/social/components/GroupCard.tsx
  - src/features/social/components/GroupMemberList.tsx
  - src/features/social/hooks/useGroups.ts
autonomous: true
requirements:
  - SOCL-06
  - SOCL-07

must_haves:
  truths:
    - "User can create a group with a name and selected friends"
    - "User can view their groups list on the Social tab"
    - "Group creator can add and remove members"
    - "Any member can leave a group"
    - "User can toggle mute on a group to silence notifications"
  artifacts:
    - path: "app/(app)/social/create-group.tsx"
      provides: "Group creation screen with name input and friend picker"
    - path: "app/(app)/social/group-detail.tsx"
      provides: "Group detail screen with member management"
    - path: "src/features/social/hooks/useGroups.ts"
      provides: "Hook wrapping socialStore group actions"
  key_links:
    - from: "app/(app)/social/create-group.tsx"
      to: "src/stores/socialStore.ts"
      via: "useGroups hook -> createGroup"
      pattern: "createGroup"
    - from: "app/(app)/social/group-detail.tsx"
      to: "src/stores/socialStore.ts"
      via: "useGroups hook -> addMember, removeMember, leaveGroup, toggleMute"
      pattern: "leaveGroup|toggleMute|addMember|removeMember"
---

<objective>
Build the group system: create groups, manage members, leave groups, mute notifications per group. Wire into the Social tab's groups section.

Purpose: Groups are the sharing target -- all content sharing goes through groups.
Output: Create-group screen, group detail/management screen, groups list on Social tab.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-CONTEXT.md
@.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-RESEARCH.md
@.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-01-SUMMARY.md

<interfaces>
From src/features/social/types.ts (created in Plan 01):
```typescript
export interface Group { id: string; name: string; created_by: string; created_at: string; }
export interface GroupMember { id: string; group_id: string; user_id: string; muted: boolean; joined_at: string; }
export interface FriendProfile { id: string; display_name: string; avatar_url: string | null; handle: string | null; }
```

From src/stores/socialStore.ts (created in Plan 01):
```typescript
// State: groups, groupMembers
// Actions: fetchGroups(), createGroup(name, memberIds), leaveGroup(groupId), addMemberToGroup(groupId, userId), removeMemberFromGroup(groupId, userId), toggleMuteGroup(groupId)
```

From src/stores/friendshipStore.ts (created in Plan 01):
```typescript
// State: friends (FriendProfile[])
// Used for friend picker when creating groups
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useGroups hook + GroupCard component + groups section on Social tab</name>
  <files>
    src/features/social/hooks/useGroups.ts,
    src/features/social/components/GroupCard.tsx,
    app/(app)/(tabs)/social.tsx
  </files>
  <action>
    1. Create `src/features/social/hooks/useGroups.ts`:
       - Thin wrapper around socialStore group-related state and actions
       - Calls fetchGroups on mount (useEffect)
       - Returns: groups, loading, createGroup, leaveGroup, addMember, removeMember, toggleMute

    2. Create `src/features/social/components/GroupCard.tsx`:
       - Card showing group name, member count, muted indicator (notifications-off-outline icon if muted)
       - Tappable -- onPress navigates to group-detail screen with groupId param
       - Dark theme card styling consistent with existing app cards (surface bg, surfaceElevated border, rounded)

    3. Update `app/(app)/(tabs)/social.tsx`:
       - Replace the "Groups" placeholder section with actual groups data from useGroups
       - "Groups" section header with "Create Group" button (add-circle-outline icon)
       - FlatList/ScrollView of GroupCard items
       - "Create Group" navigates to create-group screen
       - Empty state for groups: "No groups yet. Create one to start sharing."
  </action>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <done>Groups section on Social tab renders group cards or empty state. Create Group button navigates to create-group screen.</done>
</task>

<task type="auto">
  <name>Task 2: Create group screen + group detail with member management</name>
  <files>
    app/(app)/social/create-group.tsx,
    app/(app)/social/group-detail.tsx,
    src/features/social/components/GroupMemberList.tsx
  </files>
  <action>
    1. Create `app/(app)/social/create-group.tsx` as a stack route:
       - TextInput for group name (required, 1-50 chars)
       - Friend picker: FlatList of friends from friendshipStore with checkbox selection
       - Each row shows friend avatar, display_name, @handle, and a checkmark toggle
       - "Create" button calls createGroup(name, selectedFriendIds)
       - createGroup in socialStore must auto-add the creator as a member (Pitfall 3)
       - On success, navigate back to social tab
       - Loading state while creating

    2. Create `src/features/social/components/GroupMemberList.tsx`:
       - Takes groupId, members array, isCreator boolean
       - Renders each member: avatar, display_name, @handle
       - If isCreator: show remove button (close-circle-outline) on non-self members
       - "Add Member" row at bottom (if isCreator) navigates to a friend picker (reuse inline friend list with add button)
       - Current user shown with "(You)" label

    3. Create `app/(app)/social/group-detail.tsx` as a stack route:
       - Receives groupId via route params
       - Header: group name
       - GroupMemberList component showing all members
       - Action buttons at bottom:
         - "Mute Notifications" toggle (notifications-outline / notifications-off-outline) calls toggleMuteGroup
         - "Leave Group" button (exit-outline icon, danger color) with confirmation Alert, calls leaveGroup
       - If user is creator: can add/remove members via GroupMemberList
       - Placeholder section for "Group Feed" (will be wired in Plan 04)
  </action>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <done>User can create groups with selected friends, view group members, add/remove members (creator only), mute/unmute, and leave groups.</done>
</task>

</tasks>

<verification>
- Groups list renders on Social tab
- Create group screen with friend picker works
- Group detail shows members with management actions
- Mute toggle and leave group work
- Full test suite passes: `npx jest --bail`
</verification>

<success_criteria>
- Groups appear on Social tab with member count
- Creator can manage membership (add/remove)
- Any member can leave or mute a group
- Auto-add creator as member on group creation
</success_criteria>

<output>
After completion, create `.planning/phases/17-friend-workout-pr-and-set-video-sharing-creating-of-groups-to-share-with/17-03-SUMMARY.md`
</output>
