---
phase: 08-alarms-accountability
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/features/workout/hooks/useWorkoutSession.ts
  - src/features/alarms/hooks/useAlarmScheduler.ts
  - app/(app)/settings.tsx
  - app/(app)/(tabs)/_layout.tsx
  - tests/alarms/nudgeCancel.test.ts
autonomous: true
requirements:
  - ALRM-03

must_haves:
  truths:
    - "Nudge notification fires 4 hours after alarm time if no workout is logged"
    - "Nudge auto-cancels when user completes a workout for that day"
    - "Nudge only fires for active plan's scheduled days"
    - "Global pause toggle disables all alarm and nudge notifications"
    - "Settings screen is accessible and contains the pause toggle"
  artifacts:
    - path: "src/features/workout/hooks/useWorkoutSession.ts"
      provides: "Nudge cancellation on workout completion"
      contains: "cancelNudge"
    - path: "app/(app)/settings.tsx"
      provides: "Settings screen with global alarm pause toggle"
      contains: "Pause all alarms"
    - path: "tests/alarms/nudgeCancel.test.ts"
      provides: "Test for nudge cancellation logic"
  key_links:
    - from: "src/features/workout/hooks/useWorkoutSession.ts"
      to: "src/features/alarms/hooks/useAlarmScheduler.ts"
      via: "import cancelNudge, cancelTodaysNudge"
      pattern: "cancelNudge|cancelTodaysNudge"
    - from: "app/(app)/settings.tsx"
      to: "src/stores/alarmStore.ts"
      via: "import useAlarmStore for pause toggle"
      pattern: "useAlarmStore|isPaused"
---

<objective>
Wire nudge auto-cancel into the workout completion flow, create a settings screen with global alarm pause toggle, and ensure nudges only fire for the active plan.

Purpose: Complete the accountability loop -- users get nudged if they skip a planned workout, but the nudge cancels automatically when they train. Global pause gives control when needed.
Output: Nudge cancel on workout completion, settings screen with pause toggle, nudge cancel test.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-alarms-accountability/08-CONTEXT.md
@.planning/phases/08-alarms-accountability/08-RESEARCH.md
@.planning/phases/08-alarms-accountability/08-01-SUMMARY.md
@.planning/phases/08-alarms-accountability/08-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 and 02 outputs -->

From src/features/alarms/hooks/useAlarmScheduler.ts:
```typescript
export function cancelNudge(planDayId: string): Promise<void>;
export function schedulePlanAlarms(planDays: PlanDay[]): Promise<void>;
export function cancelPlanAlarms(planDays: PlanDay[]): Promise<void>;
export function syncActiveAlarms(plans: Plan[]): Promise<void>;
```

From src/features/alarms/utils/notificationIds.ts:
```typescript
export function nudgeNotificationId(planDayId: string): string;
```

From src/stores/alarmStore.ts:
```typescript
export const useAlarmStore: {
  isPaused: boolean;
  setPaused: (paused: boolean) => void;
};
```

From src/features/workout/hooks/useWorkoutSession.ts:
```typescript
// finishWorkout calls finishSessionAction() then saveCompletedSession()
// Need to add nudge cancellation here
// The session knows plan_day_id if started from a plan
```

From src/features/plans/types.ts:
```typescript
export interface PlanDay {
  id: string;
  weekday: number | null;
  alarm_time: string | null;
  alarm_enabled: boolean;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nudge auto-cancel on workout completion and cancel helper</name>
  <files>
    src/features/alarms/hooks/useAlarmScheduler.ts
    src/features/workout/hooks/useWorkoutSession.ts
    tests/alarms/nudgeCancel.test.ts
  </files>
  <behavior>
    - cancelTodaysNudges(plans, todayWeekday) cancels nudge for the active plan's day matching today's weekday
    - When no active plan exists, no cancellation is attempted
    - When active plan has no day matching today's weekday, no cancellation is attempted
  </behavior>
  <action>
    1. Add to `src/features/alarms/hooks/useAlarmScheduler.ts`:
       - cancelTodaysNudges(plans: Plan[], todayWeekday: number): Promise&lt;void&gt;
         - Find the active plan (plan.is_active === true)
         - Find plan day(s) where weekday === todayWeekday AND alarm_enabled === true
         - For each matching day, call cancelNudge(day.id)
         - todayWeekday uses the app's 0=Mon..6=Sun convention (get from new Date().getDay() converted: (jsDay + 6) % 7)

    2. Write test `tests/alarms/nudgeCancel.test.ts` RED first:
       - Test: cancelTodaysNudges with active plan matching today -> calls cancelScheduledNotificationAsync with nudge ID
       - Test: cancelTodaysNudges with no active plan -> does not call cancel
       - Test: cancelTodaysNudges with active plan but no matching weekday -> does not call cancel

    3. Update `src/features/workout/hooks/useWorkoutSession.ts`:
       - Import cancelTodaysNudges from '@/features/alarms/hooks/useAlarmScheduler'
       - Import usePlanStore from '@/stores/planStore'
       - In finishWorkout callback, after saveCompletedSession(completed) succeeds:
         - Get current plans from usePlanStore.getState().plans
         - Get today's weekday: const jsDay = new Date().getDay(); const todayWeekday = (jsDay + 6) % 7;
         - Call cancelTodaysNudges(plans, todayWeekday) — fire-and-forget, wrapped in try/catch (nudge cancel failure should not block workout save)
  </action>
  <verify>
    <automated>npx jest tests/alarms/nudgeCancel.test.ts --bail</automated>
  </verify>
  <done>cancelTodaysNudges correctly cancels nudge for today's active plan day. finishWorkout calls cancelTodaysNudges after saving completed session. Test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Settings screen with global alarm pause toggle</name>
  <files>
    app/(app)/settings.tsx
    app/(app)/(tabs)/_layout.tsx
  </files>
  <action>
    1. Create `app/(app)/settings.tsx` as a new Expo Router screen (not a tab — a stack screen accessible from dashboard or tabs layout):
       - Screen title: "Settings"
       - Content: A single section "Notifications" with a row containing:
         - Ionicons "notifications-off-outline" icon
         - "Pause all alarms" label
         - Switch toggle bound to alarmStore.isPaused / alarmStore.setPaused
       - When toggled ON (pause): call cancelPlanAlarms for all plans in planStore (cancel all scheduled notifications). Show brief inline text: "Alarms and reminders are paused"
       - When toggled OFF (unpause): call syncActiveAlarms(plans) to reschedule active plan's alarms
       - StyleSheet.create styling: dark background (colors.background), section card (colors.surface), consistent with app design
       - Import useAlarmStore from '@/stores/alarmStore'
       - Import usePlanStore from '@/stores/planStore'
       - Import cancelPlanAlarms, syncActiveAlarms from '@/features/alarms/hooks/useAlarmScheduler'

    2. Add navigation to settings from the tabs layout or dashboard:
       - In `app/(app)/(tabs)/_layout.tsx`, add a header-right button (Ionicons "settings-outline") on the dashboard tab that navigates to '/(app)/settings'
       - Use router.push('/(app)/settings') for navigation
       - This is a minimal settings screen stub — Phase 11 will expand it with sign out, delete account, etc.

    3. Ensure the settings screen is properly registered in the Expo Router file structure. Since it's at `app/(app)/settings.tsx`, it should be automatically available as a route.
  </action>
  <verify>
    <automated>npx jest --bail</automated>
  </verify>
  <done>Settings screen exists with "Pause all alarms" toggle. Toggle pauses/unpauses all alarm scheduling. Settings accessible from dashboard header. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx jest --bail` — full test suite passes
- `grep -r "cancelTodaysNudges" src/features/workout/hooks/useWorkoutSession.ts` confirms nudge cancel in workout flow
- `grep -r "Pause all alarms" app/(app)/settings.tsx` confirms settings screen
- `grep -r "settings" app/(app)/(tabs)/_layout.tsx` confirms navigation to settings
</verification>

<success_criteria>
- Nudge notification is canceled when user completes a workout on a scheduled day
- Nudge cancellation is fire-and-forget (does not block workout save)
- Settings screen accessible from dashboard header
- Global pause toggle pauses all alarms and nudges
- Unpausing reschedules active plan's alarms
- Nudge cancel test passes
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-alarms-accountability/08-03-SUMMARY.md`
</output>
