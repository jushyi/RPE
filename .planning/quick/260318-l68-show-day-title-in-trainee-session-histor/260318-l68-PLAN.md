---
phase: quick
plan: 260318-l68
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/plans/trainee-history.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "When viewing a trainee's session history, sessions from a plan show both the plan name AND the day title"
    - "Freestyle sessions still show 'Freestyle' with no day subtitle"
    - "Sessions with a plan but no day still show the plan name without a subtitle"
  artifacts:
    - path: "app/(app)/plans/trainee-history.tsx"
      provides: "Updated session card rendering showing day title"
  key_links: []
---

<objective>
Show the day title alongside the plan name in trainee session history cards.

Purpose: Currently the trainee history screen shows only the plan name (e.g., "Push Pull Legs") as the session title. The day title (e.g., "Push Day", "Leg Day") is not shown, making it hard for coaches to distinguish which day of a plan the trainee completed. The data is already fetched via the Supabase query (`plan_days(day_name)`) -- this is a display-only change.

Output: Updated trainee-history.tsx with day title displayed below the plan name.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/(app)/plans/trainee-history.tsx
@src/features/coaching/hooks/useTraineeHistory.ts

<interfaces>
<!-- The data is ALREADY fetched. No hook or query changes needed. -->

From src/features/coaching/hooks/useTraineeHistory.ts:
```typescript
export interface TraineeSession {
  id: string;
  started_at: string;
  ended_at: string | null;
  plan_id: string | null;
  plan_day_id: string | null;
  workout_plans: { name: string } | null;   // Plan name - already fetched
  plan_days: { day_name: string } | null;    // Day name - already fetched but NOT displayed
  session_exercises: { ... }[];
}
```

Current display logic (lines 74-78 of trainee-history.tsx):
```typescript
// PROBLEM: Uses fallback chain - shows plan name OR day name, never both
const sessionLabel = item.workout_plans?.name
  ?? item.plan_days?.day_name
  ?? (isPlanSession ? 'Plan Workout' : 'Freestyle');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show day title below plan name in trainee session history cards</name>
  <files>app/(app)/plans/trainee-history.tsx</files>
  <action>
In the `renderSession` function of `trainee-history.tsx`, update the session label logic and rendering to show both the plan name and day title:

1. Replace the current `sessionLabel` fallback chain (lines 74-78) with two separate values:
   - `planLabel`: The plan name from `item.workout_plans?.name`, falling back to `(isPlanSession ? 'Plan Workout' : 'Freestyle')`
   - `dayLabel`: The day name from `item.plan_days?.day_name` (can be null/undefined)

2. In the JSX inside `<View style={s.cardInfo}>`, keep the date text as-is, then:
   - Show `planLabel` as the main title (existing `s.titleText` style)
   - When `dayLabel` exists, add a new `<Text>` element below the title showing the day name in a subtitle style (use `colors.textSecondary`, fontSize 13, fontWeight '500'). This mirrors how the regular history shows supplementary info.

3. Add a new style `dayText` to the StyleSheet:
   ```
   dayText: {
     color: colors.textSecondary,
     fontSize: 13,
     fontWeight: '500',
     marginTop: 1,
   }
   ```

Do NOT modify the `useTraineeHistory` hook or the Supabase query -- the data is already being fetched correctly.
Do NOT use emojis anywhere in the UI per project instructions.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>
    - Plan-based sessions show the plan name as the title AND the day name (e.g., "Push Day") as a subtitle below it
    - Freestyle sessions show "Freestyle" with no day subtitle
    - Plan sessions without a day name show the plan name with no subtitle
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Visual: On trainee history screen, sessions from a plan show both plan name and day title
</verification>

<success_criteria>
- Trainee session history cards display day title (e.g., "Push Day") below the plan name
- Freestyle sessions unchanged
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260318-l68-show-day-title-in-trainee-session-histor/260318-l68-SUMMARY.md`
</output>
