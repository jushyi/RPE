---
phase: 07-body-metrics
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - app/(app)/_layout.tsx
  - app/(app)/body-metrics.tsx
  - src/features/body-metrics/components/MeasurementForm.tsx
  - src/features/body-metrics/components/MeasurementChart.tsx
  - src/features/body-metrics/components/MeasurementHistoryList.tsx
  - src/features/body-metrics/components/MeasurementHistoryItem.tsx
autonomous: false
requirements:
  - HIST-04

must_haves:
  truths:
    - "User can open a full body metrics screen from the dashboard Body card"
    - "Charts tab shows an entry form with all 4 measurement fields plus bodyweight, each with unit selector (except body fat which is unitless percentage)"
    - "Charts tab shows per-measurement trend charts below the form (bodyweight, chest, waist, hips, body fat %)"
    - "User can fill any subset of fields and save -- at least one field must be filled"
    - "Date picker defaults to today and allows selecting a past date"
    - "History tab shows reverse-chronological list of past measurement entries"
    - "User can edit an existing entry from the history tab"
    - "User can delete an entry with confirmation dialog (Alert.alert, consistent with Phase 5)"
    - "Charts handle single data point gracefully (show value as text, no line)"
    - "Swipeable Charts/History tabs using PagerView consistent with Phase 5"
  artifacts:
    - path: "app/(app)/body-metrics.tsx"
      provides: "Stack screen with PagerView tabs (Charts | History)"
      contains: "PagerView"
    - path: "src/features/body-metrics/components/MeasurementForm.tsx"
      provides: "All-at-once form with 4 measurements + bodyweight, per-input unit toggle, date picker"
      exports: ["MeasurementForm"]
    - path: "src/features/body-metrics/components/MeasurementChart.tsx"
      provides: "Single metric trend chart reusing Phase 6 Victory Native pattern"
      exports: ["MeasurementChart"]
    - path: "src/features/body-metrics/components/MeasurementHistoryList.tsx"
      provides: "Reverse-chronological scrollable list of measurement entries"
      exports: ["MeasurementHistoryList"]
    - path: "src/features/body-metrics/components/MeasurementHistoryItem.tsx"
      provides: "Single history row with edit/delete actions"
      exports: ["MeasurementHistoryItem"]
  key_links:
    - from: "app/(app)/body-metrics.tsx"
      to: "src/features/body-metrics/components/MeasurementForm.tsx"
      via: "renders in Charts tab above charts"
      pattern: "MeasurementForm"
    - from: "app/(app)/body-metrics.tsx"
      to: "src/features/body-metrics/hooks/useBodyMeasurements.ts"
      via: "fetches measurement data on mount"
      pattern: "useBodyMeasurements"
    - from: "src/features/body-metrics/components/MeasurementForm.tsx"
      to: "src/features/body-metrics/hooks/useBodyMeasurements.ts"
      via: "addMeasurement on form submit"
      pattern: "addMeasurement"
    - from: "src/features/body-metrics/components/MeasurementChart.tsx"
      to: "src/features/body-metrics/hooks/useBodyMetricsChartData.ts"
      via: "gets chart-ready data with unit normalization"
      pattern: "useBodyMetricsChartData"
    - from: "src/features/body-metrics/components/MeasurementHistoryItem.tsx"
      to: "src/features/body-metrics/hooks/useBodyMeasurements.ts"
      via: "deleteMeasurement and updateMeasurement"
      pattern: "deleteMeasurement"
---

<objective>
Build the full body metrics detail screen with Charts and History tabs. The Charts tab contains the measurement entry form and per-measurement trend charts. The History tab shows all past entries with edit and delete capability.

Purpose: Delivers the primary UI for HIST-04 -- users can log measurements, see trends, and manage their history. This is the screen navigated to from the BodyCard built in Plan 07-01.

Output: Stack screen, measurement form, trend charts, history list with edit/delete.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-body-metrics/07-CONTEXT.md
@.planning/phases/07-body-metrics/07-RESEARCH.md
@.planning/phases/07-body-metrics/07-01-SUMMARY.md

@src/constants/theme.ts
@app/(app)/_layout.tsx

<interfaces>
<!-- From Plan 07-01 (must be complete before this plan executes) -->

From src/features/body-metrics/types.ts:
```typescript
export interface BodyMeasurement {
  id: string;
  user_id: string;
  chest: number | null;
  chest_unit: 'in' | 'cm' | null;
  waist: number | null;
  waist_unit: 'in' | 'cm' | null;
  hips: number | null;
  hips_unit: 'in' | 'cm' | null;
  body_fat_pct: number | null;
  measured_at: string;
  created_at: string;
}
export type CircumferenceMetric = 'chest' | 'waist' | 'hips';
export type BodyMetric = CircumferenceMetric | 'body_fat_pct';
export type CircumferenceUnit = 'in' | 'cm';
```

From src/features/body-metrics/hooks/useBodyMeasurements.ts:
```typescript
export function useBodyMeasurements(): {
  measurements: BodyMeasurement[];
  isLoading: boolean;
  fetchMeasurements: () => Promise<void>;
  addMeasurement: (data: Partial<BodyMeasurement>) => Promise<BodyMeasurement>;
  updateMeasurement: (id: string, updates: Partial<BodyMeasurement>) => Promise<void>;
  deleteMeasurement: (id: string) => Promise<void>;
}
```

From src/features/body-metrics/hooks/useBodyMetricsChartData.ts:
```typescript
export function useBodyMetricsChartData(
  metric: BodyMetric,
  displayUnit: CircumferenceUnit
): { date: number; value: number }[];
```

From src/features/body-metrics/utils/unitConversion.ts:
```typescript
export function convertMeasurement(value: number, fromUnit: CircumferenceUnit, toUnit: CircumferenceUnit): number;
```

From Phase 6 (assumes complete):
- Victory Native CartesianChart + Line for trend charts
- useBodyweightData() hook for bodyweight CRUD
- Sparkline / chart rendering patterns
- @shopify/react-native-skia font loading pattern
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Body metrics detail screen with Charts and History tabs</name>
  <files>
    app/(app)/_layout.tsx,
    app/(app)/body-metrics.tsx,
    src/features/body-metrics/components/MeasurementForm.tsx,
    src/features/body-metrics/components/MeasurementChart.tsx,
    src/features/body-metrics/components/MeasurementHistoryList.tsx,
    src/features/body-metrics/components/MeasurementHistoryItem.tsx
  </files>
  <action>
    1. Register route in app/(app)/_layout.tsx: Add a Stack.Screen for "body-metrics" with title "Body Metrics" and headerShown: true (or consistent with other stack screens like history/[sessionId]).

    2. Create `app/(app)/body-metrics.tsx` as the main detail screen:
       - PagerView with two pages: Charts (index 0) and History (index 1)
       - Tab bar at top with "Charts" and "History" pressable labels, active tab highlighted (reuse Phase 5 sub-tab pattern exactly)
       - Fetch measurements on mount via useBodyMeasurements().fetchMeasurements()
       - Charts page: ScrollView containing MeasurementForm at top, then 5 MeasurementChart components (bodyweight, chest, waist, hips, body fat %)
       - History page: MeasurementHistoryList
       - SafeAreaView with dark background from theme

    3. Create `src/features/body-metrics/components/MeasurementForm.tsx`:
       - All-at-once form showing 5 fields: bodyweight, chest, waist, hips, body fat %
       - Each circumference field (chest, waist, hips) has a Pressable unit toggle beside the input showing "in" or "cm" -- pressing toggles between the two. Default unit: "in" (or read user's last-used unit from most recent measurement entry if available)
       - Bodyweight field has unit toggle showing "lbs" or "kg" (consistent with Phase 6 bodyweight pattern)
       - Body fat % field has NO unit toggle -- just shows "%" label beside input
       - All inputs use keyboardType="decimal-pad" and placeholder="--"
       - Date picker: Pressable showing formatted date, on press opens @react-native-community/datetimepicker (install with `npx expo install @react-native-community/datetimepicker`). Defaults to today.
       - "Save" button at bottom. Disabled unless at least one field has a value.
       - On save:
         - If bodyweight filled, call Phase 6 useBodyweightData().logWeight() to write to bodyweight_logs table
         - If any measurement filled, call useBodyMeasurements().addMeasurement() to write to body_measurements table
         - Clear form after successful save
         - Show brief success feedback (e.g., form clears + haptic)
       - Validation: At least one field must be filled. Show inline error text "Enter at least one measurement" if user taps Save with all fields empty.
       - Form layout: vertical stack of field rows, each row is label + input + unit toggle. StyleSheet.create with dark theme colors.
       - If editing an existing entry (receives optional `editEntry` prop), pre-fill form with entry values and change button to "Update"

    4. Create `src/features/body-metrics/components/MeasurementChart.tsx`:
       - Props: metric (BodyMetric), label (string), displayUnit (CircumferenceUnit), data from useBodyMetricsChartData
       - If data.length < 2: show the single value as text with message "Log more entries to see trends"
       - If data.length === 0: show empty state "No {label} data yet"
       - If data.length >= 2: render Victory Native CartesianChart + Line (reuse Phase 6 chart pattern exactly)
         - X axis: dates, Y axis: values
         - Use Skia font from Phase 6 (Inter-Regular.ttf or system font)
         - Chart height ~180px
         - Line color: colors.accent
       - Display unit toggle (for circumference metrics only) -- Pressable that toggles between "in" and "cm", re-renders chart with converted data
       - Body fat chart has no unit toggle
       - Bodyweight chart: use Phase 6 useBodyweightData for data instead of useBodyMetricsChartData. Show unit toggle for lbs/kg.

    5. Create `src/features/body-metrics/components/MeasurementHistoryList.tsx`:
       - FlatList of measurement entries sorted by measured_at DESC (most recent first)
       - Each entry rendered by MeasurementHistoryItem
       - Empty state: "No measurements logged yet" text
       - Pull-to-refresh via RefreshControl calling fetchMeasurements

    6. Create `src/features/body-metrics/components/MeasurementHistoryItem.tsx`:
       - Shows date (formatted), and all non-null measurement values in a compact layout
       - Swipe-to-delete or tap to expand with Edit/Delete buttons (choose simpler approach -- recommend tap to show action buttons)
       - Edit: opens MeasurementForm in edit mode (scroll to top of Charts tab with form pre-filled, or use an inline edit approach)
       - Delete: Alert.alert confirmation dialog with "Delete Entry?" title, destructive Delete button, Cancel button. On confirm: haptic feedback via expo-haptics, call deleteMeasurement(id). Consistent with Phase 5 delete pattern.
       - StyleSheet.create with dark theme, card-like appearance per project convention
  </action>
  <verify>
    <automated>npx expo export --platform web 2>&1 | head -5</automated>
  </verify>
  <done>
    Body metrics screen opens from dashboard BodyCard tap. Charts tab shows form at top and trend charts below. History tab shows past entries. User can log measurements, see trends, edit, and delete entries. Date picker works. Unit toggles work on circumference fields. Body fat has no unit toggle. At least one field must be filled to save. Charts handle 0 and 1 data points gracefully.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete body metrics feature</name>
  <files></files>
  <action>
    Human verification of the complete body metrics feature built across Plans 07-01 and 07-02.
  </action>
  <verify>User confirms all verification steps pass</verify>
  <done>User types "approved"</done>
  <what-built>Complete body metrics feature: combined Body dashboard card, full detail screen with Charts/History tabs, measurement entry form with per-input unit toggles, trend charts, history list with edit/delete</what-built>
  <how-to-verify>
    1. Open the app and confirm the dashboard shows a "Body" card (should show latest bodyweight if any logged, or empty state)
    2. Tap the Body card -- confirm it navigates to the Body Metrics detail screen
    3. On the Charts tab, verify the form shows: bodyweight, chest, waist, hips, body fat % fields
    4. Verify circumference fields (chest, waist, hips) each have an "in"/"cm" toggle
    5. Verify body fat % field has NO unit toggle -- just shows "%"
    6. Fill in a few fields (e.g., chest: 42 in, body fat: 15.5%) and tap Save
    7. Confirm the form clears after save
    8. Scroll down on Charts tab to see trend charts -- with only one data point, they should show the value as text
    9. Log a second entry with different values -- charts should now show a line
    10. Swipe to History tab -- confirm entries appear in reverse chronological order
    11. Try editing an entry from the history list
    12. Try deleting an entry -- confirm Alert dialog appears with Cancel/Delete options
    13. Return to dashboard -- confirm Body card shows the latest logged values
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx expo export --platform web 2>&1 | head -5` -- no build errors
- Body metrics screen accessible from dashboard Body card
- Measurement form validates at least one field filled
- Charts render with 2+ data points, graceful fallback for 0-1 points
- History list shows entries with edit/delete capability
- Delete confirmation uses Alert.alert consistent with Phase 5
- No emojis in any UI text (per CLAUDE.md)
</verification>

<success_criteria>
- Full body metrics detail screen with swipeable Charts/History tabs
- Measurement form with all 4 fields + bodyweight, per-input unit toggles, date picker
- Bodyweight field writes to Phase 6 bodyweight_logs table (single data source)
- Trend charts for each metric using Victory Native (Phase 6 pattern)
- History list with edit and delete (confirmation dialog)
- All UI uses StyleSheet.create, dark theme colors, no emojis
- Human verification confirms full flow works
</success_criteria>

<output>
After completion, create `.planning/phases/07-body-metrics/07-02-SUMMARY.md`
</output>
