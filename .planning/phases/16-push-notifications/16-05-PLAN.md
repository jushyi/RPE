---
phase: 16-push-notifications
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(app)/_layout.tsx
  - app/(app)/dev-tools.tsx
autonomous: true
gap_closure: true
requirements: [NOTIF-02, NOTIF-05]

must_haves:
  truths:
    - "Notifications screen header matches the style of other screens (bold title, correct font size)"
    - "Dev tools screen header matches other screen headers (no duplicate title in body)"
  artifacts:
    - path: "app/(app)/_layout.tsx"
      provides: "headerTitleStyle applied to notifications and dev-tools Stack.Screen entries"
      contains: "headerTitleStyle"
    - path: "app/(app)/dev-tools.tsx"
      provides: "Dev tools screen body without duplicate heading/subheading"
  key_links:
    - from: "app/(app)/_layout.tsx"
      to: "Stack.Screen notifications and dev-tools options"
      via: "headerTitleStyle: { fontWeight: '700', fontSize: 18 }"
      pattern: "headerTitleStyle"
---

<objective>
Fix cosmetic UAT gap: the Notifications and Dev Tools screen headers do not match the style of other screens in the app. The Stack.Screen options for both screens are missing headerTitleStyle, and dev-tools.tsx renders a duplicate heading and subheading inside the scroll content that duplicates the Stack header title.

Purpose: Visual consistency — all screen headers should use the same bold title weight/size as the rest of the app.
Output: Both screens show matching header style; dev-tools body has no duplicate title text.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-push-notifications/16-02-SUMMARY.md
@.planning/phases/16-push-notifications/16-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add headerTitleStyle to notifications and dev-tools Stack.Screen options</name>
  <files>app/(app)/_layout.tsx</files>
  <action>
    In `app/(app)/_layout.tsx`, the `notifications` and `dev-tools` Stack.Screen entries currently only have a `title` option. Add `headerTitleStyle: { fontWeight: '700', fontSize: 18 }` to both entries to match the visual weight used by other headers in the app.

    Current notifications entry (line ~103):
    ```tsx
    <Stack.Screen
      name="notifications"
      options={{ title: 'Notifications' }}
    />
    ```

    Update to:
    ```tsx
    <Stack.Screen
      name="notifications"
      options={{
        title: 'Notifications',
        headerTitleStyle: { fontWeight: '700', fontSize: 18 },
      }}
    />
    ```

    Current dev-tools entry (line ~107):
    ```tsx
    <Stack.Screen
      name="dev-tools"
      options={{ title: 'Dev Tools' }}
    />
    ```

    Update to:
    ```tsx
    <Stack.Screen
      name="dev-tools"
      options={{
        title: 'Dev Tools',
        headerTitleStyle: { fontWeight: '700', fontSize: 18 },
      }}
    />
    ```

    No other changes to this file.
  </action>
  <verify>
    <automated>grep -A3 "name=\"notifications\"" /c/Users/maser/Projects/Gym-App/app/\(app\)/_layout.tsx | grep "headerTitleStyle"</automated>
  </verify>
  <done>Both notifications and dev-tools Stack.Screen entries contain headerTitleStyle: { fontWeight: '700', fontSize: 18 }</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate heading and subheading from dev-tools.tsx body</name>
  <files>app/(app)/dev-tools.tsx</files>
  <action>
    In `app/(app)/dev-tools.tsx`, the ScrollView content currently starts with two Text elements that duplicate the Stack header:

    ```tsx
    <Text style={styles.heading}>Dev Tools</Text>
    <Text style={styles.subheading}>Notification Testing</Text>
    ```

    Remove both of these Text elements. The Stack header (set in _layout.tsx) already displays "Dev Tools" as the title — showing it again inside the scroll content creates a redundant duplicate header.

    Also remove the corresponding style entries `heading` and `subheading` from the `StyleSheet.create` call at the bottom of the file if they are no longer referenced elsewhere in the component. Check for any remaining usage before deleting.

    The first visible content inside the ScrollView should become the `buttonGrid` View directly, with the existing `marginBottom: 32` providing the necessary spacing.
  </action>
  <verify>
    <automated>grep -n "style={styles.heading}\|style={styles.subheading}" /c/Users/maser/Projects/Gym-App/app/\(app\)/dev-tools.tsx</automated>
  </verify>
  <done>No `styles.heading` or `styles.subheading` references remain in dev-tools.tsx JSX; the duplicate "Dev Tools" and "Notification Testing" text elements are removed</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep headerTitleStyle app/(app)/_layout.tsx` returns two matches (one per screen)
2. `grep "styles.heading\|styles.subheading" app/(app)/dev-tools.tsx` returns no JSX usage
3. Visual check: Notifications and Dev Tools headers are bold and consistent with other app screens
</verification>

<success_criteria>
- Notifications screen header title appears bold and matches size/weight of other screen headers
- Dev Tools screen header title appears bold and matches other screens
- Dev Tools scroll content does not show a duplicate "Dev Tools" heading or "Notification Testing" subheading
- No regression to other screens or functionality
</success_criteria>

<output>
After completion, create `.planning/phases/16-push-notifications/16-05-SUMMARY.md`
</output>
