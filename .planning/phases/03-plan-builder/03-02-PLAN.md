---
phase: 03-plan-builder
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/(app)/plans/create.tsx
  - app/(app)/plans/[id].tsx
  - app/(app)/plans/_layout.tsx
  - src/features/plans/components/DaySlotEditor.tsx
  - src/features/plans/components/PlanDaySection.tsx
  - src/features/plans/components/PlanExerciseRow.tsx
  - src/features/plans/components/SetRow.tsx
  - src/features/plans/components/ExercisePicker.tsx
  - src/features/plans/hooks/usePlanDetail.ts
  - src/features/plans/hooks/usePlans.ts
  - tests/plans/plan-days.test.ts
  - tests/plans/plan-exercises.test.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04

must_haves:
  truths:
    - "User can create a named plan with day slots and optional weekday mapping"
    - "User can add exercises from the library to each day via bottom sheet picker"
    - "User can set target weight, reps, RPE for each set and add notes per exercise"
    - "User can drag to reorder exercises within a day"
    - "User can view plan detail with all days in a scrollable collapsible layout"
    - "Created plans appear in the plan list"
  artifacts:
    - path: "app/(app)/plans/create.tsx"
      provides: "Plan creation screen with name input and day slot editor"
      min_lines: 50
    - path: "app/(app)/plans/[id].tsx"
      provides: "Plan detail screen with collapsible day sections"
      min_lines: 80
    - path: "src/features/plans/components/ExercisePicker.tsx"
      provides: "Bottom sheet exercise picker reusing Phase 2 components"
      min_lines: 30
    - path: "src/features/plans/components/PlanExerciseRow.tsx"
      provides: "Inline exercise row with set editing"
      min_lines: 40
  key_links:
    - from: "src/features/plans/components/ExercisePicker.tsx"
      to: "src/features/exercises/components/ExerciseListItem.tsx"
      via: "reuses exercise list item in bottom sheet"
      pattern: "ExerciseListItem|ExerciseFilterBar"
    - from: "app/(app)/plans/create.tsx"
      to: "src/features/plans/hooks/usePlans.ts"
      via: "createPlan to save new plan"
      pattern: "createPlan"
    - from: "app/(app)/plans/[id].tsx"
      to: "src/features/plans/hooks/usePlanDetail.ts"
      via: "loads full plan with nested days and exercises"
      pattern: "usePlanDetail"
---

<objective>
Build the plan creation flow and plan detail screen: users create named plans with flexible day slots (optional weekday mapping), add exercises from the library via bottom sheet picker, configure target sets/reps/weight/RPE/notes per exercise, and drag to reorder exercises within days.

Purpose: This is the core plan builder UX -- where users define their training structure. Without this, plans are empty shells.
Output: Working plan creation and detail screens with full exercise configuration.
</objective>

<execution_context>
@C:/Users/maser/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-plan-builder/03-01-SUMMARY.md

@src/features/exercises/components/ExerciseListItem.tsx
@src/features/exercises/components/ExerciseFilterBar.tsx
@src/features/exercises/hooks/useExercises.ts
@src/components/ui/Button.tsx
@src/components/ui/Input.tsx
@src/components/ui/Card.tsx
@src/constants/theme.ts

<interfaces>
<!-- From Plan 01 outputs (types created in Task 1) -->

From src/features/plans/types.ts:
```typescript
export interface TargetSet {
  weight: number;
  reps: number;
  rpe: number | null;
}
export interface PlanDayExercise {
  id: string;
  plan_day_id: string;
  exercise_id: string;
  sort_order: number;
  target_sets: TargetSet[];
  notes: string | null;
  unit_override: 'kg' | 'lbs' | null;
  created_at: string;
  exercise?: { id: string; name: string; muscle_group: string; equipment: string; };
}
export interface PlanDay {
  id: string;
  plan_id: string;
  day_name: string;
  weekday: number | null;
  sort_order: number;
  created_at: string;
  plan_day_exercises: PlanDayExercise[];
}
export interface Plan {
  id: string;
  user_id: string;
  name: string;
  is_active: boolean;
  created_at: string;
  updated_at: string;
  plan_days: PlanDay[];
}
```

From src/stores/planStore.ts:
```typescript
export const usePlanStore = create<PlanState & PlanActions>()(...)
// Actions: setPlans, addPlan, updatePlan, removePlan, setActivePlan, setLoading
```

From src/features/plans/hooks/usePlans.ts:
```typescript
export function usePlans() {
  // fetchPlans, createPlan, deletePlan, setActivePlan
}
```

From src/features/plans/constants.ts:
```typescript
export const WEEKDAY_LABELS = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
export const DEFAULT_DAY_NAMES = ['Day A', 'Day B', 'Day C', 'Day D', 'Day E', 'Day F'];
export const DEFAULT_TARGET_SET: TargetSet = { weight: 0, reps: 0, rpe: null };
```

From src/features/exercises/hooks/useExercises.ts:
```typescript
export function useExercises() {
  // exercises, fetchExercises, isLoading + CRUD
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plan creation screen with day slots and exercise assignment</name>
  <files>
    app/(app)/plans/_layout.tsx,
    app/(app)/plans/create.tsx,
    src/features/plans/components/DaySlotEditor.tsx,
    src/features/plans/components/ExercisePicker.tsx,
    src/features/plans/components/PlanExerciseRow.tsx,
    src/features/plans/components/SetRow.tsx,
    src/features/plans/hooks/usePlans.ts,
    tests/plans/plan-days.test.ts,
    tests/plans/plan-exercises.test.ts
  </files>
  <action>
    1. Create `app/(app)/plans/_layout.tsx`:
       - Stack navigator for plans group (create, [id])
       - headerShown: false (screens manage their own headers)

    2. Create `src/features/plans/components/SetRow.tsx`:
       - Single row: "Set N" label, weight TextInput (numeric), reps TextInput (numeric), RPE TextInput (numeric, optional)
       - Compact horizontal layout with labeled fields
       - Delete set button (X icon/text) on right side
       - onChange callback returns updated TargetSet
       - StyleSheet.create, dark theme, small font sizes for compact display

    3. Create `src/features/plans/components/PlanExerciseRow.tsx`:
       - Displays exercise name + muscle group badge at top
       - Lists all target_sets as SetRow components below
       - "+ Add Set" button at bottom of set list (adds DEFAULT_TARGET_SET)
       - Notes field: single-line TextInput for exercise-specific notes
       - Unit override picker: small segmented control or dropdown (kg/lbs/default)
       - Remove exercise button (trash icon/text)
       - All onChange callbacks bubble up: onSetsChange, onNotesChange, onUnitChange, onRemove
       - Props: exercise data (PlanDayExercise with joined exercise info), isEditing boolean, callbacks

    4. Create `src/features/plans/components/ExercisePicker.tsx`:
       - BottomSheetModal wrapping exercise list (per RESEARCH.md pattern 5)
       - MUST use BottomSheetFlatList (NOT regular FlatList) for scroll inside sheet
       - Reuse ExerciseFilterBar for search/filter at top of sheet
       - Reuse ExerciseListItem for each exercise row
       - onSelect callback: when user taps exercise, calls onSelect(exercise) and dismisses sheet
       - snapPoints: ['75%', '90%']
       - Props: ref (BottomSheetModalRef), onSelect callback
       - Import and use useExercises hook to get exercise data

    5. Create `src/features/plans/components/DaySlotEditor.tsx`:
       - Manages array of day slots: { tempId: string, day_name: string, weekday: number | null }
       - Each day slot shows: editable name TextInput, optional weekday dropdown/picker (7 options from WEEKDAY_LABELS + "None"), remove day button
       - "+ Add Day" button at bottom (uses next DEFAULT_DAY_NAMES entry)
       - "Add Exercise" button per day: opens ExercisePicker bottom sheet, adds selected exercise to that day's exercises array
       - Exercises listed under each day using PlanExerciseRow
       - Drag-to-reorder exercises within each day using DraggableFlatList from react-native-draggable-flatlist
       - Install react-native-draggable-flatlist first: `npx expo install react-native-draggable-flatlist`
       - Props: days state array, onChange callback

    6. Create `app/(app)/plans/create.tsx`:
       - ScrollView with dark background, SafeAreaView
       - Header: "Create Plan" title with back button (router.back()) and Save button
       - Plan name Input at top (required, validated non-empty)
       - DaySlotEditor below name input
       - Starts with one day slot ("Day A") with no exercises
       - Save flow:
         a. Validate name is non-empty
         b. Call usePlans().createPlan(name, days) -- this needs to be extended to also save plan_day_exercises
         c. CRITICAL: When inserting plan_days, set sort_order = array index (0, 1, 2...) so drag order persists. When inserting plan_day_exercises for each day, set sort_order = array index within that day's exercises array. Without this, reorder is lost on reload.
         d. On success: router.back() to plan list
       - The createPlan function in usePlans hook needs to be updated to handle full plan creation:
         Insert workout_plan -> for each day insert plan_day (with sort_order = index) -> for each exercise in day insert plan_day_exercise (with sort_order = index)
         Use sequential inserts with try/catch (per RESEARCH.md open question 1)
       - Cancel: router.back() discards all state (local component state only, nothing persisted until Save)

    7. Update `src/features/plans/hooks/usePlans.ts`:
       - Extend createPlan to accept full day data including exercises:
         createPlan(name: string, days: { day_name: string, weekday: number | null, exercises: { exercise_id: string, target_sets: TargetSet[], notes: string | null, unit_override: 'kg' | 'lbs' | null }[] }[])
       - Insert workout_plan row first, then for each day insert plan_day with sort_order = day's array index, then for each exercise insert plan_day_exercise with sort_order = exercise's array index within the day
       - Return the full created plan

    8. Flesh out `tests/plans/plan-days.test.ts` (stub created in Plan 01):
       - Replace placeholder assertions with real tests
       - Test: creating plan days assigns sort_order from array index (0, 1, 2)
       - Test: weekday mapping is optional (null weekday is valid)
       - Test: day_name defaults from DEFAULT_DAY_NAMES sequence
       - Mock Supabase insert calls

    9. Flesh out `tests/plans/plan-exercises.test.ts` (stub created in Plan 01):
       - Replace placeholder assertions with real tests
       - Test: exercise target_sets defaults to empty array
       - Test: exercise sort_order reflects position in day's exercises array
       - Test: adding a set appends DEFAULT_TARGET_SET to target_sets
       - Mock Supabase insert calls
  </action>
  <verify>
    <automated>npx jest tests/plans/ --bail && npx expo export --platform web --output-dir /tmp/expo-export-test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Plan creation screen renders with name input and day slot editor
    - User can add/remove day slots with names and optional weekday mapping
    - User can open exercise picker bottom sheet and select exercises
    - Selected exercises appear under their day with set rows for weight/reps/RPE
    - User can add/remove sets per exercise
    - User can drag to reorder exercises within a day
    - Saving creates plan + days + exercises in Supabase with sort_order = array index at each level
    - plan-days and plan-exercises tests pass with real assertions
  </done>
</task>

<task type="auto">
  <name>Task 2: Plan detail screen with collapsible day sections</name>
  <files>
    app/(app)/plans/[id].tsx,
    src/features/plans/components/PlanDaySection.tsx,
    src/features/plans/hooks/usePlanDetail.ts
  </files>
  <action>
    1. Create `src/features/plans/hooks/usePlanDetail.ts`:
       - usePlanDetail(planId: string) hook
       - Fetches full plan with nested data using Supabase nested select:
         `supabase.from('workout_plans').select('*, plan_days(*, plan_day_exercises(*, exercise:exercises(id, name, muscle_group, equipment)))').eq('id', planId).single()`
       - Sort plan_days by sort_order, sort plan_day_exercises by sort_order within each day
       - Returns: { plan, isLoading, error, refetch }
       - Use `as any` for Supabase .from() calls per project convention

    2. Create `src/features/plans/components/PlanDaySection.tsx`:
       - Collapsible section component for a single day within plan detail
       - Header: day_name + weekday label if mapped (e.g., "Day A - Monday"), chevron indicator for expand/collapse
       - Use LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut) for toggle animation (simpler approach per RESEARCH.md open question 3)
       - Expanded state: list of exercises with their target sets displayed read-only
       - Each exercise shows: name, muscle group badge, sets table (Set #, Weight, Reps, RPE), notes if present
       - Default: all sections expanded
       - Props: day (PlanDay), defaultExpanded boolean

    3. Create `app/(app)/plans/[id].tsx`:
       - Read planId from route params via useLocalSearchParams
       - Use usePlanDetail(planId) to load full plan
       - Header: plan name as title, back button, "Edit" button (for Plan 03 -- render but disabled/no-op for now with a TODO comment)
       - Active badge displayed under plan name if is_active
       - ScrollView containing PlanDaySection for each plan_day
       - Loading state while fetching
       - Error state if plan not found
       - StyleSheet.create, dark theme, SafeAreaView
  </action>
  <verify>
    <automated>npx jest tests/plans/ --bail && npx expo export --platform web --output-dir /tmp/expo-export-test 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Plan detail screen loads full plan with nested days and exercises from Supabase
    - Collapsible day sections show exercise details (sets, reps, weight, RPE, notes)
    - Days sorted by sort_order, exercises sorted within each day
    - Navigation from plan list card to detail screen works
    - Edit button placeholder present for Plan 03
  </done>
</task>

</tasks>

<verification>
- `npx jest tests/plans/ --bail` passes (all plan test files including plan-days and plan-exercises)
- App compiles: `npx expo export --platform web 2>&1 | tail -3`
- Plan creation: navigate to create screen, enter name, add days, add exercises, set targets, save
- Plan detail: tap plan card in list, see all days with exercises and set details
- Exercise picker: bottom sheet opens with search/filter, selecting exercise adds it to the day
</verification>

<success_criteria>
- Plan creation screen with name + day slots + weekday mapping + exercise picker + inline set editing
- Plan detail screen with collapsible day sections showing all exercise configuration
- ExercisePicker bottom sheet reuses Phase 2 exercise library components
- DraggableFlatList enables exercise reorder within days
- Full plan data (plan + days + exercises) persists to Supabase on save with sort_order = array index
- Plan list navigates to detail, detail shows complete plan structure
- plan-days and plan-exercises tests pass with behavioral assertions
</success_criteria>

<output>
After completion, create `.planning/phases/03-plan-builder/03-02-SUMMARY.md`
</output>
